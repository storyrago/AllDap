# AGENTS.md

AI 코딩 에이전트(Claude Code, Cursor 등)를 위한 저장소 가이드입니다.
**이 파일이 원본**이고, 도구별 파일은 이 파일을 가리키기만 합니다 —
`CLAUDE.md`는 `@AGENTS.md` 한 줄뿐입니다(Claude Code의 import 문법).
내용을 고칠 때는 항상 이 파일을 고치세요. 양쪽에 복사하면 반드시 어긋납니다.
`web/`도 같은 구조입니다(`create-next-app`이 만든 기본형).

## 이 프로젝트가 뭔가

**AllDap** — 문서를 올리면 출처가 표시되는 한국어 RAG 챗봇을 만들어주고,
그 챗봇의 답변 품질을 자동 평가해 리포트로 보여주는 서비스.

**목적이 중요합니다: 이건 취업 포트폴리오 프로젝트입니다.**
따라서 "동작하는 코드"보다 **"설명할 수 있는 코드"**가 우선입니다.

## 개발자에 대해

- 백엔드 신입 지원 준비 중이나 방향은 **풀스택 지향**. **Java/Spring이 상대적으로 익숙**하고(프로젝트 경험 수준),
  **Python은 아직 약함** — 이 프로젝트의 AI 서비스가 Python이라 학습을 겸한다.
- **프론트엔드(TypeScript·React·Next.js)도 약함.** 프론트를 직접 만들어보는 것 자체가 이 프로젝트의 목적 중 하나다.
- 온디바이스 AI·모델 경량화(양자화)는 **아직 배우지 않았음**. 관심 단계.
  → 이 프로젝트에 경량화를 억지로 끼워넣지 말 것. 별도 학습 트랙임.
- 한국어로 설명해주세요. 어려운 용어를 나열하기보다 이해되게.

**중요:** **Python 코드와 TypeScript/React 코드**를 작성할 때는 왜 그렇게 쓰는지
(문법·관용구 포함) 간단히라도 설명을 붙여주세요. 개발자가 읽고 이해할 수 있어야 합니다.
"돌아가니까 넘어가자"는 이 프로젝트의 목적에 어긋납니다.

프론트에서 특히 설명이 필요한 것들 — 묻지 않아도 주석·설명으로 남길 것:
- `"use client"`를 왜 이 파일에만 붙였는가 (서버 컴포넌트와의 경계)
- 이 데이터를 왜 서버에서 가져오는가 / 왜 클라이언트에서 가져오는가
- `useEffect`의 의존성 배열에 왜 그 값이 들어갔는가
- 이 타입을 왜 `interface`/`type`으로 정의했고, 백엔드 응답과 어떻게 맞췄는가

Java/Spring은 상대적으로 익숙하므로 설명 밀도를 낮춰도 되지만,
**설계 결정(왜 이 구조인가)은 언어와 무관하게 항상 설명할 것.**

> ⚠️ **Next.js 16.2.12 / React 19.2.4를 쓴다 — 대부분의 모델이 학습한 버전보다 최신이다.**
> `web/AGENTS.md`가 경고하듯 API·관례·파일 구조가 달라졌을 수 있다.
> 프론트 코드를 쓰기 전에 **`web/node_modules/next/dist/docs/`를 먼저 읽을 것.**
> 기억에 의존해 구버전 문법으로 짜지 말 것.

## 아키텍처 (하이브리드 — 임의로 바꾸지 말 것)

### 컴포넌트 구성

```
 [고객 사이트]  <script src=".../alldap-widget.js"></script>  ← 한 줄 설치 (widget/, W2 초안)
        │  로더가 iframe 으로 /w/[publicKey] 를 띄운다
        ▼
 [브라우저]  (관리자는 그냥 :3000 에 접속)
        │
        ▼
 ┌──────────────────────────────────────────┐
 │  Next.js (App Router)   :3000   [W2]     │  ← 프론트엔드. TypeScript
 │  ├ 관리자 대시보드 (인증 필요)              │     /dashboard
 │  │  봇 생성·문서 업로드·품질 리포트·로그      │     /bot/[botId]/{documents,chat,quality,logs,settings}
 │  └ 위젯 페이지 (공개, 인증 없음)            │     /w/[publicKey]
 └──────────────────────────────────────────┘
        │ HTTPS (REST + JWT)
        ▼
 ┌──────────────────────────────────────────┐
 │  Spring Boot API        :8080   [W2]     │  ← 외부에 노출되는 유일한 API 서비스
 │  인증(JWT)·권한·봇/문서 메타·대화 로그      │
 └──────────────────────────────────────────┘
        │ REST (내부망 전용, 인증 없음)
        ▼
 ┌──────────────────────────────────────────┐
 │  Python AI Service      :8001   [W1]     │  ← 외부 비노출. /internal/* 만 제공
 │  파싱·청킹·임베딩·검색·생성·평가            │
 └──────────────────────────────────────────┘
        │                    │
        │ SQL                │ HTTPS
        ▼                    ▼
 ┌───────────────┐   ┌──────────────────────────────┐
 │ PostgreSQL 16 │   │ 외부 AI API (제공자 2곳)      │
 │  + pgvector   │   │  임베딩 → Cloudflare bge-m3  │
 │    :5432      │   │  생성   → Google Gemini      │
 └───────────────┘   └──────────────────────────────┘
        ▲
        │ SQL (JPA)
        └──── Spring Boot API
```

**핵심 원칙**
- 외부 **API** 트래픽은 **Spring만** 받는다. Python 서비스는 내부망에만 열린다.
- 그래서 `/internal/*` 엔드포인트에는 인증이 없다. **절대 외부에 노출하지 말 것.**
- 외부 LLM API는 Python 서비스만 호출한다. Spring도 Next.js도 API 키를 갖지 않는다.
- **Next.js는 API 게이트웨이가 아니다.** 브라우저에서 Python(:8001)을 직접 부르지 말 것.
  인증·권한·bot_id 스코프 격리는 전부 Spring이 책임진다.
  Next.js에서 비즈니스 로직을 처리하거나 DB에 직접 붙는 코드를 만들지 말 것.

**디렉터리 구조 (2026-07-31 저장소 루트로 평탄화됨)**

```
AllDap/  ← 저장소 루트
├── web/         Next.js 16 (App Router) + TypeScript  🚧 라우트·타입만 (디자인 미착수)
├── api/         Spring Boot (:8080)                   🚧 인증·봇 CRUD 완료 · 나머지 TODO
│   └── src/main/resources/db/migration/V1__init.sql   ← 스키마 원본 (Flyway)
├── ai-service/  Python FastAPI (:8001)                ✅ W1 완료 조건 실측 통과
├── widget/      임베드 위젯 스크립트                    🚧 초안 (alldap-widget.js)
├── .github/     CI · PR 템플릿
└── docs/{PRD_v0.4.md, decisions.md}
```

**루트 `db/` 디렉터리는 없어졌다.** 2026-07-31에 Flyway로 이관하면서
`api/src/main/resources/db/migration/`으로 옮겼다. 옛 경로를 안내하지 말 것.

예전의 `AllDap/dadap/*` 중첩 구조는 없어졌다. 경로를 안내할 때 `dadap/`을 붙이지 말 것.
제품명도 "다답/DaDap"이 아니라 **AllDap**이다. 옛 이름을 되살리지 말 것.

### 테이블 소유권 (중요 — 어기면 데이터가 깨진다)

두 서비스가 **같은 DB를 공유**한다. 누가 어느 테이블을 쓰는지 반드시 지킬 것.

| 테이블 | 쓰기(owner) | 읽기 | 비고 |
|---|---|---|---|
| `users`, `bots` | Spring | Python | Python은 조회만 |
| `documents` | **Python** | Spring | 상태(status)는 Python이 갱신 |
| `chunks` | **Python** | — | Spring은 건드리지 않음 |
| `conversations`, `messages` | Spring | — | 대화 로그는 Spring이 저장 |
| `eval_*` | **Python** | Spring | 평가 실행은 Python, 조회는 Spring |

- Spring의 `spring.jpa.hibernate.ddl-auto`는 반드시 **`validate` 또는 `none`**.
  Hibernate가 스키마를 바꾸면 Python 쪽이 깨진다.
- 스키마의 단일 진실 공급원은 **`api/src/main/resources/db/migration/`의 Flyway 마이그레이션**이다.

### Flyway 규칙 (2026-07-31 도입 — 어기면 기동이 막힌다)

1. **`V1__init.sql`을 고치지 말 것.** 한 번 적용된 마이그레이션을 수정하면
   체크섬이 안 맞아 다음 기동이 실패한다. 스키마 변경은 `V2__*.sql`, `V3__*.sql`로만.
2. **스키마는 Spring이 기동해야 생긴다.** 예전엔 `docker-entrypoint-initdb.d`라
   컨테이너만 띄우면 테이블이 있었지만, 이제는 아니다.
   → 로컬 실행 순서: `docker compose up -d` → **`api` 기동** → `ai-service` 기동.
   Python을 먼저 띄우면 테이블이 없어 실패한다.
3. 옛 방식으로 만들어진 DB 볼륨이 남아 있으면 Flyway가
   `Found non-empty schema(s) "public" but no schema history table`로 중단한다.
   `docker compose down -v`로 볼륨을 비우고 다시 시작할 것.
4. **`V1__init.sql` 끝의 시드 INSERT(`pk_local_dev` 테스트 봇)가 이제 운영에도 적용된다.**
   initdb 시절엔 "로컬 개발용"이었지만 Flyway는 배포 환경에서도 그대로 실행한다.
   배포 전에 별도 파일이나 `dev` 프로파일로 분리할지 결정할 것. **[미정]**

### 요청 흐름 2가지

**① 문서 업로드 (비동기)**
```
브라우저 → Spring: POST /api/bots/{id}/documents   (파일)
Spring:   권한 확인 → Python으로 그대로 전달
Python:   documents 행 생성(status=pending) → 202 즉시 응답
          ↓ (백그라운드)
          파싱 → 청킹 → 임베딩(외부 API) → chunks 저장 → status=ready
브라우저 → Spring → Python: GET .../documents  로 상태 폴링
```
업로드 응답과 처리를 분리한 이유: 임베딩은 수십 초가 걸릴 수 있어
HTTP 요청을 붙잡아두면 타임아웃이 난다.

**② 질문·답변 (동기)**
```
위젯 → Spring: POST /api/w/{publicKey}/chat   (관리자 테스트 채팅은 POST /api/bots/{id}/chat)
Spring:   봇 존재·도메인 확인 → 대화 로그 저장(user 메시지)
          ↓
Python:   질문 임베딩 → pgvector 유사도 검색(top_k)
          → max_distance 초과 청크 제거
          → 근거 없으면 LLM 호출 없이 fallback 반환 (비용 절감)
          → 근거 있으면 LLM 호출 → NO_ANSWER면 fallback 처리
          ↓
Spring:   대화 로그 저장(assistant 메시지 + sources + is_fallback)
          → 브라우저에 응답
```

### 이 구조의 알려진 약점 (면접 답변거리이므로 숨기지 말 것)

- **공유 DB는 마이크로서비스 안티패턴이다.** 두 서비스가 한 DB를 쓰면
  스키마 변경이 양쪽에 동시에 영향을 준다. 그럼에도 택한 이유는
  1인 개발에서 데이터 동기화 비용이 서비스 분리 이득보다 크기 때문.
  트래픽·팀이 커지면 DB를 나누고 API로만 통신하도록 바꿔야 한다.
- Spring이 Python을 동기 호출하므로 **Python이 죽으면 채팅이 죽는다.**
  타임아웃·재시도·서킷브레이커를 W2에서 반드시 설정할 것.
- 인증이 Spring에만 있어 Python이 내부망에 노출되면 무방비다. 배포 시 주의.
- **배포 대상이 3개(Spring·Python·Next.js)로 늘었다.** PRD §11.2의 원칙
  "1인 개발에서 운영해야 할 것의 개수를 최소화한다"와 정면으로 충돌한다.
  풀스택 역량 증명을 위해 **알고 택한** 것이며, 몰라서 어긴 게 아니다.
  면접에서 물으면 이렇게 답한다: "원칙과 충돌하는 건 알고 있었고,
  이 프로젝트의 1순위 목적이 포트폴리오라 역량 증명 쪽을 택했습니다."

**왜 나눴나:** AI 파이프라인은 Python 생태계가 사실상 필수(문서 파싱·임베딩·평가),
인증·트랜잭션·권한은 Spring이 강함. 국내에서도 카카오페이(모델 Python, 서빙 Kotlin+Spring),
쏘카가 같은 구조를 쓴다.
이 구조 자체가 면접 어필 포인트이므로 **단일 서비스로 합치자는 제안은 하지 말 것.**

## 작업 규칙

1. **한 번에 하나씩.** "W2 전체" 같은 큰 단위로 작업하지 말고,
   엔드포인트 하나·기능 하나 단위로 끊어서 진행할 것.
2. **설계 결정은 반드시 설명할 것.** 왜 그 라이브러리인지, 왜 그 구조인지.
   개발자가 면접에서 그대로 답할 수 있어야 함.
3. **아키텍처를 바꿔야 할 것 같으면 먼저 물어볼 것.** 임의로 바꾸지 말 것.
4. 코드 주석과 에러 메시지는 한국어로. 특히 사용자에게 보이는 에러는
   "무엇을 어떻게 하면 되는지"까지 알려줄 것.
   (예: "구버전 .hwp는 지원하지 않습니다. .hwpx로 저장 후 올려주세요.")
   에러 응답 포맷은 **전 계층 공통**이다 (Spring·Python·Next.js 모두 동일):
   `{ "error": { "code": "...", "message": "무엇을 어떻게 하면 되는지까지 담은 한국어 설명" } }`
5. **JSON 필드 표기 변환은 Spring 책임이다.**
   Python은 `snake_case`(`is_fallback`), 프론트는 `camelCase`(`isFallback`)를 본다.
   Spring이 Python 응답을 그대로 흘려보내지 말고 DTO로 받아 변환해 내려줄 것.
   프론트 `web/lib/types.ts`가 camelCase 전제로 작성돼 있어, 어기면 타입이 전부 거짓이 된다.
6. **범위를 넓히지 말 것.** 모델 경량화·양자화·온디바이스 추론은
   이 프로젝트에 넣지 않습니다. 개발자가 나중에 별도로 학습할 트랙이며,
   지금 끌어들이면 Python 학습·Spring 구현·경량화가 전부 어중간해집니다.
   관련 제안을 하지 마세요.
7. **작업 성격에 따라 모델을 나눠 쓸 것** (아래 "모델 선택" 참조).
8. **`main`에 직접 커밋하지 말 것** (아래 "브랜치·PR" 참조).

## 브랜치 · PR

**`main`에 직접 커밋하지 않는다.** 작업은 브랜치에서 하고 PR로 합친다.

| 접두사 | 쓰임 |
|---|---|
| `feat/` | 기능 구현 — 예: `feat/auth-signup-login` |
| `fix/` | 버그 수정 |
| `refactor/` | 동작 변화 없는 구조 개선 |
| `docs/` | 문서만 고칠 때 |
| `chore/` | 빌드·CI·설정 |

- **PR 하나는 리뷰 가능한 크기로.** 슬라이스가 커도 PR은 쪼갠다.
- PR 본문은 `.github/PULL_REQUEST_TEMPLATE.md`를 채운다.
  **체크리스트가 아니라 <설명>을 요구하는 템플릿이다** — 면접에서 그 PR 을 열어놓고
  그대로 말할 수 있어야 한다. 특히 두 칸이 핵심이다:
  - **한계 & 트레이드오프** — 비어 있으면 대개 안 본 것이다. 근거가 얕은 값,
    측정하지 못한 것("안 나빠졌다"와 "재보지 않았다"는 다르다)까지 적는다.
  - **검토한 대안과 선택 이유** — `docs/decisions.md` 의 재료이자, 면접에서
    가장 많이 파고드는 지점이다.
  그리고 **어떻게 해결했나요** 칸에는 "돌려봤다"가 아니라 **실제 실행 결과**를 붙인다.
- 커밋 메시지는 `<타입>: <한국어 요약>` — feat / fix / refactor / test / docs / chore / review.
- 1인 개발이라 승인해 줄 사람이 없다. 그래도 PR을 여는 이유는
  ① 셀프 리뷰 시점이 강제로 생기고 ② CI가 머지 전에 돌며
  ③ **포트폴리오에서 "무엇을 왜 바꿨는지"가 PR 단위로 읽힌다**.
  커밋 로그만 있는 저장소보다 PR 이력이 있는 쪽이 협업 경험을 훨씬 잘 보여준다.

> **예외**: 저장소 최초 임포트(커밋 0건 상태의 첫 골격)는 `main`에 바로 올렸다.
> 브랜치를 딸 기준점 자체가 없었기 때문이다. 그 이후로는 이 규칙을 지킨다.

## 모델 선택 — 비용을 아끼되 아낄 곳에서만

기획·설계·리뷰는 상위 모델(Opus)로, 확정된 스펙을 옮기는 구현은 Sonnet으로 내려
비용을 줄입니다. **다만 판단 기준은 "구현이냐 아니냐"가 아니라 "스펙이 확정됐느냐"입니다.**

약한 모델이 헤매면 재시도·컴파일 실패·재작업으로 **오히려 토큰을 더 씁니다.**
줄어드는 것은 토큰 수가 아니라 토큰당 단가이므로, 헤맬 여지가 있는 일에 내리면 손해입니다.

| 작업 | 모델 |
|---|---|
| 스펙이 확정된 기계적 작업 — DTO·리포지토리·보일러플레이트, 문서 경로 일괄 치환, 명확한 스펙의 테스트 작성 | **Sonnet** |
| 기획·설계 결정, 아키텍처 판단, 코드 리뷰·검증, 디버깅 | **Opus** |

**아래는 비용이 더 들더라도 Opus를 유지한다** (여기서 틀리면 되돌리는 비용이 더 크다):

- **보안 관련 코드** — 인증·JWT·`bot_id` 스코프 격리. 봇 간 데이터가 새면 제품 신뢰가 끝난다
- **환각 억제(fallback)** — `max_distance`와 `NO_ANSWER` 두 겹. 제품의 핵심 가치다
- **검증·리뷰 단계** — 여기서 놓치면 앞 단계 결과물이 전부 헛일이 된다
- **Python·TypeScript/React 코드의 설명** — 개발자가 약한 영역이고,
  이 프로젝트의 1순위 목적이 "설명할 수 있는 코드"다. 설명 품질이 곧 프로젝트 목적이다

**적용 방법**: Workflow 스크립트의 `agent(prompt, { model: 'sonnet' })`,
또는 Agent 도구의 `model` 파라미터로 슬라이스 단위로 지정한다.
지정하지 않으면 세션 모델을 그대로 상속하므로, **확신이 없으면 지정하지 말 것**(상속이 안전한 기본값).

## 절대 깨뜨리면 안 되는 것

- **환각 억제(fallback).** 문서에 근거가 없으면 지어내지 말고 fallback 해야 함.
  검색 단계(`max_distance`)와 생성 단계(`NO_ANSWER`) 두 겹으로 걸려 있음.
  이게 제품의 핵심 가치이므로 성능·편의를 이유로 완화하지 말 것.
- **임베딩 차원 동기화.** 모델을 바꾸면 `EMBEDDING_DIM`과
  `api/src/main/resources/db/migration/`의 `VECTOR(n)`를 **반드시 함께** 바꿔야 함.
  (V1은 수정 금지이므로 새 마이그레이션으로 `ALTER`할 것 — `V2__embedding_1024.sql` 이 그 예다)
  > ⚠️ **`V1__init.sql` 첫 줄 주석이 "1536 = OpenAI text-embedding-3-small 기준"이라고 말하지만 거짓이고, 차원도 이제 1536이 아니다.**
  > **현재 제공자는 Cloudflare Workers AI (`@cf/baai/bge-m3`, 1024차원)** 이다.
  > (2026-08-02 에 Google Gemini `gemini-embedding-001`(1536) 에서 옮겼다 — 아래 참조)
  > SQL 주석을 못 고치는 이유는 하나 — **Flyway 체크섬이 깨져 기동이 막히기 때문**이다(주석 한 글자도 포함).
  > 이 문단이 그 정정본이다. SQL 주석 대신 여기를 믿을 것.
  >
  > **⚠️ 임베딩과 생성의 제공자가 다르다.** 임베딩만 Cloudflare 이고
  > **생성(답변·질문)은 여전히 Google Gemini** 다. 한쪽만 보고 "Cloudflare 로 옮겼다"고 쓰지 말 것.
  >
  > **차원을 바꾸는 절차** (순서가 중요하다. 실측으로 확인함):
  > 인덱스 드롭 → `UPDATE chunks SET embedding = NULL` → `ALTER ... TYPE VECTOR(n)` → 인덱스 재생성
  > → **재임베딩** (`cd ai-service && .venv/bin/python -m app.reembed`).
  > 데이터가 있는 채로 `ALTER` 하면 `expected N dimensions, not M` 로 실패한다.
- **bot_id 스코프 격리.** 모든 조회는 bot_id로 격리. 봇 간 데이터가 새면 안 됨.
- **JPA `ddl-auto`는 `validate` 또는 `none`.** Python 서비스와 DB를 공유하므로
  Hibernate가 스키마를 건드리면 안 됨.

## 진행 상황

| 단계 | 내용 | 상태 |
|---|---|---|
| **0** | **W1 코드 이해 + 첫 실행 검증** | ✅ **완료** (이해 게이트 4문항 → `docs/W1-이해노트.md`) |
| W1 | Python AI 서비스 (파싱→청킹→임베딩→검색→생성) | ✅ **완료 조건 3개 실측 통과** (아래 참조) |
| W2 | Spring Boot API 계층, Next.js 화면, 임베드 위젯 | 🚧 **진행 중 — 여기** (아래) |
| W3 | **품질 대시보드** (테스트 질문 자동 생성 → LLM-as-judge 채점) | ⬜ |
| W4 | 검색 품질 개선 → 평가 before/after 비교 | ✅ **완료 (2026-08-13). 전체 충실성 0.781 → 0.875, 회당 오답 1.75 → 0. 리랭커+하이브리드를 기본값으로 켰다.** ① 청킹 전략으로 0.938 → 1.000 (2026-08-03). 병목은 검색 알고리즘이 아니라 <청킹>이었다. ② **1.000 은 천장이라 코퍼스를 50문서·306청크로 키웠다 (2026-08-11). 새 before = 전체 충실성 0.781 · 응답률 0.875.** 이제 리랭커를 잴 수 있다 |

**W2 세부 상태 (2026-07-31 기준)**

| 항목 | 상태 |
|---|---|
| `api/` Spring 뼈대 | ✅ **골격 완료 · 기동 검증됨.** Gradle-Groovy / Java 21(Temurin) / **Boot 4.0.7** / Gradle 9.5.1 / `com.alldap.api`. `ddl-auto: validate`로 기동 성공 = 엔티티가 스키마와 일치함을 실측 확인 |
| 인증 (가입·로그인·JWT) | ✅ **구현 완료 · 통합 테스트 7건 통과** (PR #3) |
| 봇 CRUD | ✅ **구현 완료 · 통합 테스트 8건 통과** (PR #5). **`bot_id` 소유권 격리가 여기서 처음 걸렸다** — 아래 참조 |
| 문서 업로드·목록·삭제 | ✅ **구현 완료 · 통합 테스트 15건 통과** (PR #8). **Spring→Python 첫 연동이다** — 아래 참조 |
| 관리자 채팅 · 피드백 | ✅ **구현 완료 · 통합 테스트 15건 통과** (PR #9). **fallback 치환이 여기서 동작한다** — 아래 참조 |
| 위젯 공개 API (설정·채팅) | ✅ **구현 완료 · 통합 테스트 12건 통과** (PR #11). **인증 없이 열린 유일한 문** — 아래 참조 |
| 대화 로그 (목록·상세) | ✅ **구현 완료 · 통합 테스트 11건 통과** (PR #10). 목록 집계는 **쿼리 2번 고정**(N+1 없음) |
| 평가 API | ✅ **Python·Spring 양쪽 구현 완료** (2026-08-02). 질문 생성·목록, 평가 실행(202+폴링), 실행 이력, 질문별 채점 결과. 3-스택 종단 실측 통과. **미답변 집계 추가**(2026-08-11, Spring 이 `messages` 를 직접 집계 — Python 미경유). 미구현: 질문 수정 PATCH |
| 프론트 스택 결정 | ✅ **Next.js(App Router) + TypeScript로 확정** (`docs/decisions.md`) |
| `web/` 화면 | ✅ **전 화면 브라우저 실측 통과** (2026-08-02). 품질 대시보드까지 구현 완료 — **★전체 충실성**(생존 편향 없는 비교용) · 충실성 · 관련성 · 응답률 카드, 테스트셋, 실행 이력, 질문별 상세. 디자인은 기능 우선 |
| `widget/` 스크립트 | ✅ **실제 설치 실측 통과** (2026-08-02). 별도 origin 의 가짜 고객 사이트에 `<script>` 한 줄 → 로더 → iframe → 인사말 → 답변(근거 포함) → `channel=widget` 로그까지. 허용 안 된 도메인은 안내 문구로 차단됨 |
| Flyway · CI · PR 템플릿 | ✅ 도입 완료 (위 Flyway 규칙 참고) |

**`api/` 에서 검증된 것 / 안 된 것 (2026-08-01)**
검증됨: 컴파일, Flyway V1 적용, Hibernate `validate` 통과, `/actuator/health` 200,
**전 엔드포인트(인증·봇·문서·채팅·로그·위젯) 통합 테스트 71건** —
진짜 톰캣 + 진짜 PostgreSQL(Testcontainers) + 가짜 Python(JDK HttpServer) 위 실제 HTTP.
**✅ 진짜 3-스택 종단 확인 완료 (2026-08-01)** — 아래 참조.
검증 안 됨: 평가 API(Python 에 엔드포인트가 없다 — W3), CI 실제 실행.
`web/` 화면과 **위젯 실제 설치**는 2026-08-02 브라우저 실측으로 통과했다 (바로 아래).

### ✅ 종단 확인 (2026-08-01) — 가짜 서버로는 못 잡는 버그가 하나 나왔다

Docker + Spring + 진짜 uvicorn 을 띄우고 **Spring 을 통해서만** 전 흐름을 돌렸다.
업로드 → 임베딩 → `ready` → 답변(근거 포함) → fallback → 로그 → 위젯까지 전부 동작.

> ⚠️ **첫 업로드가 422 로 실패했다.** JDK `HttpClient` 기본값이 HTTP/2 라 평문 상대에게
> h2c 업그레이드를 함께 요청하는데, uvicorn(h11)이 그걸 지원하지 않아
> **chunked 본문을 FastAPI 까지 전달하지 않는다.** multipart 는 완벽한데 "file 필드가 없다" 는 422 가 났다.
> → `RestClientConfig` 에서 `HTTP_1_1` 로 고정해 해결.
>
> **통합 테스트 71건이 전부 통과하는 상태에서 난 버그다.** 가짜 서버는 multipart 를 파싱하지도,
> 업그레이드 요청을 처리하지도 않아 재현 자체가 불가능했다.
> 회귀 테스트는 본문 대신 <b>"보내면 안 되는 헤더가 없는가"</b> 를 검사한다.
>
> **교훈: 통합 테스트가 초록불이어도 진짜 상대와 붙여보기 전까지는 연동을 검증했다고 말하지 말 것.**

### W3 첫 슬라이스 — 테스트 질문 자동 생성 (2026-08-02)

`POST /internal/bots/{bot_id}/eval/questions/generate` — 청크에서 표본을 뽑아
LLM 이 (질문, 정답) 쌍을 만들고 `eval_questions` 에 저장한다. **Python 만 만들었다**
(Spring·프론트는 경로가 굳기 전에 붙이지 않는다).

- **동기 200 이다.** `eval_questions` 에 상태 컬럼이 없어 202 를 줘도 폴링할 대상이 없다.
  `count` 상한(20)으로 응답 시간을 통제한다. 자세한 근거는 `docs/decisions.md`.
- **재호출은 누적**이다. 표본 SQL 의 `NOT EXISTS` 한 줄이 누적과 중복 방지를 같이 한다.
  전량 교체는 관리자가 고쳐둔 질문을 날린다.
- **표본 SQL 에 걸린 조건 4개에 각각 이유가 있다** (`evaluator.sample_chunks` 주석):
  `bot_id`(격리) · `documents.status='ready'`(깨진 청크 배제) · 길이 하한(목차 조각 배제) ·
  `NOT EXISTS`(중복 방지). ⚠️ `/internal/*` 에는 인증이 없어 **`bot_id` 조건이 격리의 전부다.**
- **LLM 호출은 `cursor()` 블록 밖**이다. 풀이 10개뿐이라 안에 두면 채팅·업로드까지 멈춘다.
- 청크 하나가 실패해도(파싱 실패·429) **그 청크만 건너뛴다.** 전부 실패할 때만 502.

> ⚠️ **`gemini-3.5-flash` 는 사고(thinking) 토큰이 `max_output_tokens` 를 함께 먹는다.**
> JSON 이 50토큰도 안 되는데 사고에 1024를 다 쓰고 본문이 잘려(`finish_reason=MAX_TOKENS`)
> 파싱에 실패, 그 청크가 조용히 버려졌다. 4개 요청에 3개만 나와서 발견했다.
> → 질문 생성은 추출 작업이라 사고가 필요 없다. `thinking_budget=0` 으로 껐다.
> **실측(같은 청크 5회씩): 사고 켬 → 잘림 3회·평균 3768ms / 사고 끔 → 잘림 0회·평균 1164ms.**
>
> ⚠️ ~~**같은 원인이 채팅 답변 경로에도 있다.**~~ → ✅ **2026-08-04 에 고쳤다.**
> 답변이 잘려 비면 `generator.generate` 의 `not answer` 분기에 걸려 **fallback 으로 둔갑했다** —
> 근거를 찾고도 "모른다"고 답하는 것이다.
> 이제 `cf.finish_reason` 으로 <잘림>을 알아내 `GenerationFailed` 를 던진다.
> `evalrun` 은 그 질문을 응답률 분모에서 빼고(측정 실패), `main.chat` 은 503 으로 안내한다.
> **미뤄뒀던 이유였던 재측정도 같이 했다** — 아래 "잘린 답변" 항목 참고.

**검증된 것 / 안 된 것**
검증됨: 정상 생성(청크 1개 → 1건, 청크 4개 → 3건), `count` 상한 초과 400,
`count=0` pydantic 422, ready 문서 없음 400, 재호출 400(이미 다 만듦),
`bot_id` 격리(저장된 질문의 출처 청크가 전부 같은 봇), 전량 실패 시 502,
`app/evaluator_check.py` 자체 점검 7가지.
⚠️ **사고 끄기 수정 <이후>의 정상 생성은 아직 실측하지 못했다** — Gemini 무료 등급
일일 한도(20 requests/day)에 걸려 429 다. 쿼터가 풀리면 반드시 다시 돌릴 것.

> **Gemini 무료 등급 한도가 실제로 걸렸다 (2026-08-02).**
> `GenerateRequestsPerDayPerProjectPerModel-FreeTier`, `limit: 20`, `gemini-3.5-flash`.
> 청크 1개당 1회 호출이라 **질문 20개 = 그 모델의 하루치를 다 쓴다.**
> (약관상 무료 등급은 입력을 학습에 쓴다는 문제도 별개로 있다 — 아래 "알려진 한계" 참고)
>
> ✅ **다만 한도는 `PerProjectPerModel` 이다 — 모델별로 따로 잡힌다.**
> 용도를 다른 모델에 나누면 각각 별도 한도를 받는다. 아래 "모델 선택" 참고.

### ▶ 다음 세션은 여기서 시작한다 (2026-08-04 말 기준)

**로컬 실행 순서** (Flyway 는 이미 적용돼 있으므로 스키마 변경이 없으면 `api` 는 건너뛰어도 된다)

```
docker compose up -d
cd ai-service && .venv/bin/uvicorn app.main:app --port 8001
curl -X POST localhost:8001/internal/bots/628d2785-a128-486c-a1ac-556f19f06de3/eval/runs
```

**✅ 밀렸던 측정 2건은 2026-08-13 에 둘 다 끝났다.**
W1 fallback 종단(10/10) · 리랭커 융합 재측정(설정당 3~4회). 각각 아래 해당 절 참고.
한도 리셋은 **소진 시각으로부터 약 33시간 뒤**에 확인됐다(문서의 00:00 UTC 와 무관하다는 것이 또 확인됐다).

**🔴 W4 는 끝났다. 다음 후보 (아직 순서를 안 정했다)**

1. 🔴 **1차 방어선(`max_distance`)이 1/10 로 약해진 문제.** 코퍼스가 50문서가 되니
   무슨 질문에나 거리 0.55 안에 드는 청크가 생긴다 → LLM 을 거의 매번 부른다.
   낮추면 정상 질문의 근거까지 잘리므로 **평가로 재고 결정할 일**이다.
2. 평가 API 미구현: 질문 수정 PATCH.

✅ Spring→Python 재시도·서킷브레이커는 2026-08-17 에 끝났다("알려진 약점" 절 참고).

✅ 봇별 `system_prompt` 전달은 2026-08-13 에 끝났다(한계는 "알려진 한계" 절 참고).

✅ 리랭커 비용 실측은 2026-08-13 에 끝났다(13.6 뉴런 · 1.7%).

**W4 이전에 적어둔 할 일 (대부분 끝났다)**

1. ✅ **코퍼스 확장은 2026-08-11 에 끝났다** (위 "코퍼스 확장" 절).
   **W4 의 before 가 확보됐다: 전체 충실성 0.781 · 응답률 0.875 · 관련성 0.964.**
   ⚠️ **가설은 반증됐다** — 청크를 3배(80→250)로 늘려도 지표가 안 움직였고,
   **같은 주제·다른 대상** 문서를 넣고 나서야 떨어졌다. 이유는 그 절에 적어뒀다.
2. ✅ **리랭커는 2026-08-11 에 쟀다** (위 "리랭커 재측정" 절).
   전체 충실성 0.781 → 0.844 지만 **정답 청크를 밀어내 새 fallback 을 2건 만들었다.**
   기본값은 `False` 유지. 켜려면 "정답을 밀어내지 않게" 만드는 게 선결 과제다 —
   `rerank_candidates` 조정, 벡터 순위와 리랭커 점수의 결합 등. 별도 슬라이스.
3. ✅ **조합까지 다 쟀고 2026-08-13 에 기본값으로 켰다.** **0.781 → 0.875 가 결론 수치다.**
   `reranker_enabled = True` · `hybrid_enabled = True` · `rerank_fusion = False`.
   **결정적 근거는 평균이 아니라 <회당 오답 1.75 → 0>이다** (아래 표).
   남은 실패 2건(재택·기간제)은 전부 <리랭커가 정답을 밀어낸> 것이다.
4. ✅ **그 "결합"은 2026-08-12 에 만들어 쟀고 <반증됐다>** (위 "리랭커 점수 융합" 절).
   밀어내기는 완화되는데 끌어올리기도 똑같이 완화돼 제로섬이다. 기본값 `False`.
   🔴 **표본이 1회씩이다** — Cloudflare 한도 소진으로 나머지가 429 로 죽었다. **한도가 풀리면 3회로 재측정할 것.**
4. **설정당 3회 이상 반복할 것.** `temperature=0` 인데도 편차 폭이 **0.032** 다
   (위 정정 참고). 그보다 작은 개선은 노이즈와 구별되지 않는다.
5. **테스트셋은 16문항 그대로 둘 것.** 회당 비용이 문항 수에 정비례한다(위 "회당 비용" 참고).
   지금 크기면 하루 8회라 설정 2개를 3회씩 재고도 남는다.

**남은 구멍 (알면서 남긴 것)**

- **Spring 은 잘림을 구분하지 못한다.** Python 이 503 을 주면 Spring 은 `AI_SERVICE_UNAVAILABLE`
  ("잠시 후 재시도")로 바꾼다. 그런데 `temperature=0` 이라 **재시도해도 똑같이 잘린다** —
  안내가 사실과 다르다. 맞는 안내는 "질문을 더 좁혀보세요"다.
  → `ErrorCode` 추가가 필요하고, 그건 `api/` 를 건드리는 별도 슬라이스다.
- 평가 API 미구현: 질문 수정 PATCH. (미답변 집계는 2026-08-11 에 붙였다)
- Spring→Python 재시도·서킷브레이커 (Cloudflare 가 이틀 막힌 실제 사건이 있어 근거가 생겼다).
- 봇별 `system_prompt` 가 Python 까지 전달되지 않는다 (PRD F-06 미충족).
- ✅ **W1 fallback 질문 10개 — 2026-08-13 에 측정 완료** (`app/fallback_e2e_check.py`).
  **fallback 10/10 (기준 8) · 대조군 3/3.** 코퍼스 50문서·306청크 기준으로 재확인됐다.
  질문 목록이 저장소 어디에도 없던 문제도 이때 해결했다 — 감시 낱말 가드까지 붙였다.

**낸 버그 4건 — 전부 같은 부류다. 다섯 번째를 조심할 것**

| 버그 | 무엇을 놓쳤나 |
|---|---|
| `answered_rate` 분모에 처리 실패를 포함 | "답을 못 했다"와 "물어보지도 못했다"를 섞었다 |
| `avg_faithfulness` 로 설정 비교 | **답을 덜 할수록 올라가는 지표**였다(생존 편향). `V3` 로 분모를 기록하고 "전체 충실성" 도입 |
| 채점자·리랭커가 청크 **앞 200자만** 봄 | 생성 모델은 전체를 보는데 판정자는 절반만 봤다. **정답을 맞힌 답변이 0점**을 받았다 |
| `generate` 의 `or not answer` (2026-08-04) | **우리가 토큰을 덜 줘서 빈 응답**을 **문서에 답이 없다**로 뭉갰다. 근거를 찾고도 "모른다"고 답한다 |

> 🔴 **공통점: 넷 다 <원인이 다른 두 사실>을 같은 값으로 뭉갠 것이다.**
> 그리고 넷 다 "숫자가 나왔다"에서 멈추지 않고 파본 덕에 나왔다.
> **새 지표·새 분기를 만들 때는 "여기서 뭉개지는 서로 다른 사실이 있는가"를 먼저 물을 것.**

### 🔴 현재 모델 배치 (2026-08-02 기준 — 하루 사이에 네 번 바뀌었다)

| 용도 | 제공자 · 모델 | 왜 |
|---|---|---|
| 임베딩 | Cloudflare `@cf/baai/bge-m3` (1024) | 약관(무료 Gemini 는 입력을 학습에 씀) · 한도 |
| 답변 생성 | Cloudflare `@cf/meta/llama-3.3-70b-instruct-fp8-fast` | Gemini 모델당 20회/일로는 평가를 하루 한 번밖에 못 돌림. `NO_ANSWER` 준수 3/3 실측 |
| 채점(judge) | Cloudflare `@cf/mistralai/mistral-small-3.1-24b-instruct` | **답변 생성과 계열이 달라야 한다**(자기 채점 방지) |
| 질문 생성 | Google `gemini-3.5-flash-lite` | 유일하게 남은 Gemini. 제공자를 셋 다 같게 두면 같은 편향을 공유한다 |

### 💰 회당 비용 — 실측 (2026-08-13, 응답의 뉴런 값을 직접 읽음)

🔴 **대시보드를 읽을 필요가 없다.** Cloudflare 응답이 <호출당 정확한 뉴런>을 준다.
2026-08-13 에 발견했다 — 그전까지 이 문서는 "대시보드를 사람이 읽어야 한다"고 적어놨고,
실제로 그것 때문에 리랭커를 **추정만 하고 켰다**("수십 뉴런일 것"). 실측은 13.6 이었다.

| 위치 | 모델 계열 |
|---|---|
| `result.usage.neurons` | 생성 · 채점 · 리랭커 |
| `result.meta.neurons` | **임베딩** (usage 가 아예 없다) |
| 헤더 `cf-ai-neurons` | 전부 공통. 단 소수점 2자리로 **반올림**된다 |

세 경로를 다 보게 해뒀다(`cf._record_neurons`). 한쪽만 보다가 **임베딩이 통째로 빠진 것을
이미 한 번 겪었다.** 그리고 평가 실행이 끝나면 로그에 모델별 합계가 찍힌다.

**평가 1회(16문항 · 리랭커+하이브리드 ON) = 804 뉴런**

| 항목 | 뉴런 | 비중 |
|---|---|---|
| 채점 (mistral-small) | 410.0 | 51.0% |
| 답변 생성 (llama-3.3-70b) | 380.3 | 47.3% |
| **리랭커 (bge-reranker-base)** | **13.6** | **1.7%** |
| 임베딩 (bge-m3) | 0.4 | 0.05% |

```
10,000 ÷ 804  ≈ 하루 12회     문항 1개 ≈ 50 뉴런
```

- 🔴 **리랭커는 1.7% 다. 켠 대가가 사실상 없다** — 기본값 결정이 옳았음이 확인됐다.
  (기본값을 켤 때는 "수십 뉴런, 한 자릿수 %"로 <추정>했고, 그 추정이 컸다)
- **비용의 98%가 생성과 채점이다.** 검색 설정을 바꾸는 실험은 여전히 거의 공짜다.
- ⚠️ 옛 기록(1,190 뉴런 · 하루 8회)은 대시보드 대조 추정치였다. **804 가 실측이다.**
- 🔴 **그래도 테스트셋 크기가 실험 횟수의 상한이라는 결론은 그대로다.**

  | 테스트셋 | 회당 | 하루 |
  |---|---|---|
  | 16문항(현재) | 804 | 12회 |
  | 30문항 | 1.5k | 6회 |
  | 50문항 | 2.5k | 4회 |
  | 100문항 | 5.0k | 2회 |

  before/after 는 최소 2회, 편차를 보려면 설정당 3회 이상이 필요하다.
  → 코퍼스(문서)는 키우되 **테스트셋(질문)은 20~30문항에서 묶을 것.**

> ⚠️ **2026-08-02 에 이 한도를 실제로 소진했다.** 다만 원인은 평가가 아니라
> **일회성 모델 비교 실험**(4개 모델 × 3회)이었다. 위 실측이 그걸 확인해준다 —
> 평가만 돌리면 하루 8회는 충분하다. "숫자가 커 보인다"고 검산을 건너뛴 것이
> 당시의 진짜 실수였다 (`docs/decisions.md` 참고).

> ⚠️ **한도 리셋은 문서(`00:00 UTC`)대로 동작하지 않는다.** 2026-08-03 실측:
> 카운터는 00:00 UTC 에 `0/10k` 로 리셋됐는데 **차단은 안 풀렸고**(04:47 UTC 에도 HTTP 429),
> 실제로 풀린 것은 소진 시각으로부터 **약 24시간 뒤**였다.
> 즉 **대시보드 카운터로 "언제 풀리는지"를 예측할 수 없다.** 실제로 한 번 호출해보는 것이
> 유일하게 믿을 수 있는 방법이다. 오류 코드도 문서(3036)와 실제(4006)가 다르다.

### 모델 선택 — 실측으로 가려낸 것 (2026-08-02)

**⚠️ `models.list()` 에 나온다고 부를 수 있는 게 아니다.** 구세대는 목록에 남아 있으면서
호출하면 404 를 준다(`"no longer available to new users"`). **반드시 실제로 호출해볼 것.**

| 모델 | 호출 | 사고토큰 | 비고 |
|---|---|---|---|
| `gemini-3.5-flash-lite` | ✅ | **0** | **현재 `eval_question_model`**(테스트 질문 생성). 사고를 안 해 JSON 잘림이 없다 |
| `gemini-3.1-flash-lite` | ✅ | **0** | 예비. 다른 용도에 쓰면 별도 한도를 받는다 |
| `gemini-flash-lite-latest` | ✅ | **0** | 별칭 |
| `gemini-3.6-flash` | ✅ | 242 | 사고함 → 채점(judge)에 적합. 답변 생성엔 비싸다 |
| `gemini-3-flash-preview` | ✅ | 331 | 사고함 |
| `gemini-flash-latest` | ✅ | 196 | 사고함 |
| `gemini-2.5-flash`, `gemini-2.5-flash-lite` | ❌ 404 | — | 신규 계정 차단 |
| `gemini-2.0-flash(-lite)` | ❌ 429 | — | 한도 |

> ⚠️ **`thinking_budget` 은 모델에 따라 400 이 난다.** `gemini-3.5-flash` 는 받아주지만
> `gemini-3.5-flash-lite` 는 `HTTP 400 invalid argument` 로 거부한다.
> 사고를 제어해야 하면 현행 파라미터인 **`thinking_level`** 을 쓸 것.

**✅ 모델 교체 후 W1 완료 조건 재측정 (2026-08-02)**
`gemini-3.5-flash` → `gemini-3.5-flash-lite` 로 바꾼 뒤 다시 쟀다.

- **fallback 10/10 유지** (기준 8). 근거 있는 질문 2건도 정상 답변.
- **지연 평균 3.3초 → 1.2초.**
- 질문 생성도 3건 요청 → 3건 전부 성공(이전엔 4건 중 1건이 잘려 버려졌다).
- ⚠️ **1차 방어선(`max_distance=0.55`)은 여전히 0/10 작동.** 근거 없는 질문 10개가
  전부 근거 5건을 달고 통과했다. **생성 모델**을 바꿔도 이 상태는 그대로였다.
  → 그런데 **임베딩**을 바꾸자 4/10 으로 살아났다. 바로 아래 참조.

### 🔴 임베딩 교체 (2026-08-02) — 1차 방어선이 처음으로 작동했다

`gemini-embedding-001`(1536) → **Cloudflare Workers AI `@cf/baai/bge-m3`(1024)**.
`V2__embedding_1024.sql` + `app/reembed.py` 로 청크 7개를 다시 임베딩했다.

**⚠️ 성능 때문이 아니라 약관·한도 때문이다.** Gemini 무료 등급은 공식 가격표에
`Used to improve our products: Yes` — 입력을 학습에 쓴다. 이 제품은 고객 사내 문서를 받는 게 목적이다.
**"한국어 성능이 더 좋아서 바꿨다"고 말하면 안 된다** — 두 모델을 같은 한국어 벤치마크에서
나란히 잰 측정치가 없다. 방어 가능한 답: *"비용과 데이터 정책 때문에 바꿨고,
성능 우위는 공개 비교가 없어서 W4 에서 직접 쟀습니다."*

**교체 후 재측정**

| | 이전 (Gemini 1536) | 이후 (bge-m3 1024) |
|---|---|---|
| W1 fallback (기준 8) | 10/10 | **10/10 유지** |
| **1차 방어선 작동** | **0/10** | **4/10** |
| 걸러진 질문의 지연 | — | **119~170ms** (LLM 미호출) |
| 나머지 질문의 지연 | — | 800~1100ms |

> **"근거가 없으면 LLM 을 부르지 않는다"는 비용 절감 경로가 처음으로 열렸다.**
> ⚠️ 다만 이건 "더 좋다"가 아니라 **"거리 분포가 달라졌다"** 이다.
> 정상 질문을 잘못 자르지 않는지는 아직 모른다(근거 있는 질문 2건만 확인).
> **그걸 재는 것이 W3 평가 대시보드의 일이다.**

### ✅ 브라우저 실측 (2026-08-02) — 화면과 위젯. 코드를 읽어서는 못 잡는 버그가 둘 나왔다

실제 브라우저로 가입 → 봇 생성 → 문서 업로드(폴링으로 `ready`) → 답변(근거 73%) →
fallback → 로그 → 설정 저장 → **별도 origin 의 가짜 고객 사이트에 위젯 설치**까지 전부 돌렸다.

> ⚠️ **버그 ①: 로그인한 채 새로고침하면 로그아웃됐다.**
> `useSyncExternalStore` 의 세 번째 인자(서버 스냅샷)는 **하이드레이션 첫 렌더에도 쓰인다.**
> 거기서 `null` 을 돌려주니 "토큰이 없다" 와 "아직 못 읽었다" 가 같은 값이 되어,
> 가드가 첫 렌더에서 바로 `/auth` 로 튕겼다. 화면 안에서 이동할 때는 멀쩡해서 안 보였다.
> → 서버 스냅샷을 **`undefined`(아직 모름)** 로 바꿔 `null`(확실히 없음)과 갈랐다.
>
> ⚠️ **버그 ②: 위젯이 실제로 설치되면 봇 설정 조회가 반드시 403 이었다.**
> 채팅 페이지(`/w/[publicKey]`)가 자기도 `/api/w/{key}/config` 를 불렀는데,
> **iframe 에서 나가는 요청의 Origin 은 고객 사이트가 아니라 우리 앱(:3000)이다.**
> 고객이 허용 목록에 우리 주소를 넣을 리 없으니 영원히 막힌다.
> → 허용 도메인 검증을 통과할 수 있는 요청은 **고객 페이지에서 도는 로더**뿐이다.
> 로더가 한 번 부르고 결과를 `host-info` 에 실어 내려주도록 바꿨다.
>
> **교훈: "누구의 Origin 으로 나가는 요청인가" 를 따지지 않으면 iframe 위젯의 도메인 검증은 반드시 틀린다.**
> 그리고 ①·② 둘 다 **통합 테스트 71건이 전부 초록불인 상태**에서 났다.
> 앞의 HTTP/2 건과 같은 교훈이다 — 진짜로 띄워보기 전까지 검증했다고 말하지 말 것.

**아직 안 고친 것 (알고 남긴다)**
- 로더 없이 `/w/[publicKey]` 를 직접 열면 인사말이 안 나온다(설정을 로더가 주므로). 채팅은 된다.
- 없는 `botId` 로 들어가도 404 가 아니라 각 화면의 데이터 오류로 드러난다(`notFound()` 미적용).

> ✅ 2026-08-03 에 고친 것: 답변 마크다운(`**굵게**`)이 그대로 보이던 것 →
> 의존성 없이 굵게·줄바꿈만 React 요소로 렌더(`RichText`). 사이드바가 봇 id 로 뜨던 것 →
> 이름만 클라이언트 컴포넌트(`BotName`)로 가져온다(토큰이 브라우저에만 있어 서버가 못 부른다).

### Spring → Python 호출 — 이후 슬라이스도 이 방식을 따를 것

- **실패 변환은 `AiServiceClient` 안에서 끝낸다.** 호출 지점마다 try-catch 를 적으면 한 곳이 빠지고,
  거기서 `RestClientException` 이 그대로 올라가 **500** 이 나간다 = "Python 이 죽었다"가
  "우리 서버가 고장났다"로 둔갑한다.
- **코드를 나누는 기준은 "누구 잘못인가"다.** 연결 안 됨 → 503 / 느림 → 504 /
  Python 5xx → 503 / Python 4xx → 사용자 입력 문제(400 계열).
- ⚠️ **응답 헤더가 온 뒤** 끊기면 `ResourceAccessException` 이 아니라
  `RestClientException(cause=IOException)` 으로 온다. 둘 다 처리해야 한다.
- **소유권 확인은 Python 을 부르기 <전에>.** Python `/internal/*` 에는 인증이 없어서,
  요청이 거기 도달한 시점에 이미 샌 것이다. 테스트도 "404 가 났다"가 아니라
  **"요청이 Python 까지 가지 않았다"** 를 확인할 것.
- ✅ **재시도·서킷브레이커는 2026-08-17 에 붙였다** (`AiServiceClient.call()` 안).
  🔴 **재시도는 <연결 실패에만> 한다.** 재시도의 전제는 "직전 시도가 아무 일도 하지 않았다"인데,
  그게 보장되는 것은 연결이 안 된 경우뿐이다. **5xx 는 Python 이 응답했다 = 요청이 도달했다**이므로
  재시도하면 문서 행이 중복되거나 LLM 이 두 번 과금된다(`RestClientConfig` 의 옛 TODO 가
  5xx 를 재시도 대상으로 분류했는데 그게 틀렸다 — 정정했다).
  그렇게 좁히면 "업로드는 제외" 라던 예외도 사라진다 — 요청이 안 갔으니 중복될 행이 없다.
  서킷은 **4xx 와 DTO 불일치를 실패로 세지 않는다.** 그건 Python 장애가 아니라 우리 잘못이라,
  세면 **우리 버그로 멀쩡한 Python 을 차단**하게 된다.
  resilience4j 대신 직접 만들었다(Boot 4 호환 미확인 + 필요한 게 상태 3개짜리 카운터).
  ⚠️ **상태를 가진 싱글턴이라 통합 테스트마다 `circuitBreaker.reset()` 이 필요하다** —
  안 하면 5xx 테스트가 누적돼 서킷이 열리고, 실행 순서에 따라 나타났다 사라지는 실패가 난다.
- **4xx 해석은 엔드포인트마다 다르다.** 업로드의 400 은 "파일이 잘못됐다"(사용자가 고칠 수 있다),
  채팅의 400·422 는 "우리가 잘못 호출했다"(사용자는 손쓸 수 없다). `call()` 에 4xx 매퍼를 넘겨 구분한다.
  하나로 묶으면 **채팅 오류에 "지원하지 않는 파일 형식입니다"가 나간다.**

### 위젯 공개 API — 인증 없이 열린 유일한 문

- **빈 `allowed_origins` 는 "전부 허용" 이 아니라 "전부 차단"이다.** 그렇게 두지 않으면
  모든 신규 봇이 무방비로 태어난다. publicKey 는 고객 사이트 HTML 에 그대로 노출된다.
- **Origin 비교는 정확히 일치만.** 접미사 비교는 `evil-example.com` 이 `example.com` 으로 통과한다.
- ⚠️ **CORS 를 인가 수단으로 쓰지 말 것.** 브라우저 정책일 뿐이라 curl 은 무시하고,
  CORS 필터가 먼저 끊으면 응답 본문이 `Invalid CORS request` 평문이 되어
  우리 한국어 안내를 사용자가 못 읽는다. `/api/w/**` 는 CORS 로는 열고 판단은 컨트롤러가 한다.
- **Origin 검증은 설정 조회에만 걸린다.** 채팅은 iframe 안에서 나가 고객 Origin 이 오지 않는다
  (구조적 한계 — `WidgetController` 주석). 채팅의 실질 방어는 **rate limit** 이다.
- rate limit 은 **인메모리**다. 인스턴스를 늘리면 각자 세므로 실질 한도가 배가 된다.
  **수평 확장 시작 시점이 교체(Redis) 시점이다.**

### 오래 걸리는 Python 호출 — 트랜잭션 경계 (채팅에서 처음 걸렸다)

`질문 저장(짧음) → Python 호출(수십 초) → 답변 저장(짧음)` 에서
**가운데를 트랜잭션에 넣으면 안 된다.** 커넥션 풀(기본 10)이 마르면
채팅과 무관한 요청까지 전부 멈춘다 — Python 하나가 느려진 것이 서비스 전체 장애가 된다.

- DB 작업은 **별도 빈**(`ChatTurnStore`)에 모은다. 같은 클래스 안에서
  `@Transactional` 메서드를 자기가 부르면 프록시를 안 거쳐 **트랜잭션이 조용히 사라진다**(self-invocation).
- **트랜잭션 밖으로 엔티티를 들고 나가지 말 것.** 필요한 값만 record 로 꺼내 나간다
  (`open-in-view=false` 라 준영속 엔티티의 LAZY 필드를 건드리면 터진다).
- Python 호출이 실패하면 **질문만 남고 답변 행이 없다.** 이건 fallback 과 다른 상태다 —
  W3 에서 미답변을 집계할 때 섞지 말 것.

### 봇 소유권 검사 — 이후 슬라이스가 전부 이 위에 얹힌다

문서·채팅·로그·평가가 전부 `botId`를 받는다. 그때마다 소유권을 새로 짜지 말고
**`BotService.findOwnedBot(userId, botId)` 와 같은 방식**을 따를 것.

- 소유권을 "검사"하지 않고 **조회 쿼리에 못박는다** (`findByIdAndUserId`).
  `findById` 후 `if (남의 것) throw` 는 검사를 빠뜨려도 컴파일이 통과한다.
- **남의 봇은 403이 아니라 404.** 403은 "그 봇은 존재한다"를 알려주는 셈이라
  무작위 UUID를 던져 403만 골라내면 남의 봇 존재 여부를 훑을 수 있다.
  `ErrorCode.ACCESS_DENIED` 가 정의돼 있지만 **봇 접근에는 쓰지 말 것.**
- **`userId`는 `@AuthenticationPrincipal` 로만 받는다.** 쿼리 파라미터·본문으로 받으면
  남의 id를 적어 보내는 것만으로 격리가 무너진다.

> **Boot 4는 스타터 이름이 Boot 3과 다르다.** `spring-boot-starter-web`이 아니라
> `spring-boot-starter-webmvc`이고, 통합 `starter-test`가 없어 `-webmvc-test`,
> `-data-jpa-test`처럼 쪼개져 있다. Boot 3 기준 예제를 그대로 붙여넣지 말 것.

> **"뼈대"의 정의를 지킬 것.** 파일·경로·타입·호출 지점이 제자리에 있다는 뜻이지
> **동작하는 완제품이 아니다.** 아직 구현하지 않은 곳은 TODO 주석에
> "무엇을 / 어느 단계(W2·W3·W4)에서" 할지 한국어로 남길 것.
> **거짓 완성 금지 — 안 만든 걸 만들었다고 쓰지 말 것.**
> `api/`에서 실제로 동작하는 것은 **인증과 봇 CRUD 둘뿐**이고 나머지는 전부 TODO다.

### ✅ W1 검증 완료 (2026-07-31) — 다만 1차 방어선이 놀고 있다

**완료 조건 3개 실측 통과:**
1. ✅ 문서 업로드 → 상태가 `ready`로 바뀜
2. ✅ 질문 → 출처(`sources`) 포함 답변이 나옴
3. ✅ **문서에 없는 질문 10개 중 10개 → `is_fallback: true`** (기준 8개)

3번이 가장 중요합니다. 환각을 못 막으면 이 제품은 의미가 없습니다.

> ⚠️ **다만 fallback 10/10은 2차 방어선(`NO_ANSWER`)이 혼자 막아낸 결과다.**
> 1차 방어선인 검색 단계 컷오프(`max_distance=0.55`)는 **한 번도 작동하지 않았다** —
> 문서에 없는 질문 10개가 전부 근거 1건을 달고 통과했다.
> 즉 "근거가 없으면 LLM을 호출하지 않는다"는 비용 절감 경로가 열리지 않았다.
> **알고 남긴 것이며 W4에서 평가 점수로 조정한다.** 지금 손대지 말 것
> (표본이 청크 1개·질문 7개뿐이라 조정하면 문서 하나에 과적합된다 — `docs/decisions.md` 참고).

여전히 검증 안 된 것: **PDF·DOCX 파서(실제 파일)**, 여러 문서·대용량 문서에서의 동작.

### ✅ 이해 게이트 (0단계) — 통과

W1 코드는 개발자가 직접 작성한 것이 아니라 **받은 코드**입니다.
포트폴리오 프로젝트이므로, 설명하지 못하는 코드는 없느니만 못합니다.
아래 네 가지를 정리해 통과했습니다.

1. `generator.py`의 프롬프트가 왜 "근거에 없으면 NO_ANSWER"를 강제하는가
2. `retriever.py`의 `max_distance`가 왜 필요한가 (LLM 호출 전에 거르는 이유)
3. 청킹에서 겹침(overlap)을 왜 두는가
4. HWPX 파싱에서 `<hp:run>`을 왜 병합해야 했는가

> 📘 **정리해둔 답: [`docs/W1-이해노트.md`](docs/W1-이해노트.md)**
> 비유 → 왜 필요한가 → 코드 순으로 깊어지게 썼다. 면접 준비로는 2단계까지면 충분하다.

**Claude Code의 역할:** 개발자가 "이 코드 왜 이렇게 짰어?"라고 물으면,
Python 문법·관용구까지 포함해 이해되게 설명할 것. 이해 못 한 채로
다음 단계를 진행하자고 재촉하지 말 것.
**한 번에 여러 개를 쏟아붓지 말 것** — 하나씩, 코드보다 비유를 먼저.
개발자가 "모르겠다"고 하면 더 자세히가 아니라 **더 쉽게** 다시 설명할 것.

### 순서에 대한 메모

원래는 W1(Python) → W2(Spring) 순이었으나 **W2를 먼저** 하도록 바꿨습니다.
이유: ① 개발자가 Java/Spring이 더 익숙해 완주 가능성이 높고,
② 채용공고 8건 중 6건이 Java/Spring을 요구했으며(RDBMS·REST는 8건 전부),
③ Spring에서 Python 서비스를 호출하다 보면 Python 코드를 자연스럽게 읽게 됨.

**W3이 이 프로젝트의 심장입니다.** 일정이 밀리면 W2의 위젯을 버리더라도
W3은 지킬 것. 평가 대시보드가 없으면 그냥 흔한 챗봇 빌더가 됨.
(실제 채용공고에서 "모델 품질 평가(eval)", "LLM·RAG 구축 원리 이해"를
주업무·우대사항으로 명시한 사례를 확인했음)

**W4의 before/after 비교표가 최종 목표물**입니다.
"리랭커 도입으로 충실성 0.71 → 0.86" 같은 정량 개선 수치를 만드는 게 핵심.

> ✅ **2026-08-03 에 수치가 나왔습니다 — 다만 축이 예상과 달랐습니다.**
>
> | 같은 16문항 · 같은 모델 | 전 | 후 |
> |---|---|---|
> | **전체 충실성** | **0.938** | **1.000** |
> | 관련성 | 0.967 | 1.000 |
> | 응답률 | 0.938 | 1.000 |
> | 총 청크 | 24 (평균 400자) | 85 (평균 100자) |
>
> ⚠️ **이 수치는 `temperature=0` 을 명시한 뒤에야 신뢰할 수 있게 됐습니다.**
> 온도를 안 정해두면 제공자 기본값(약 0.6)으로 매번 샘플링해서, **같은 설정으로 4회를
> 돌려도 `1.000 · 0.907 · 1.000 · 1.000`** 로 흔들립니다(폭 0.093). 그 폭이 재려던
> 개선 폭보다 커서 비교가 성립하지 않았습니다. ~~온도 0 에서는 두 번 돌려 **소수점까지 동일**합니다.~~
> **설정을 비교하기 전에 측정이 재현되는지부터 확인할 것.**
>
> 🔴 **취소선 부분은 2026-08-11 에 반증됐습니다. `temperature=0` 도 재현을 보장하지 않습니다.**
> 그때 "소수점까지 동일" 했던 이유는 온도 때문이 아니라 **모든 문항이 1.000(천장)이었기 때문**입니다 —
> 천장에서는 흔들릴 여지가 위쪽에 없습니다. 코퍼스를 키워 난이도가 생기자 같은 설정 4회가
> `0.781 · 0.781 · 0.781 · 0.813` 으로 갈렸습니다(폭 **0.032**).
> **검색은 완전히 결정적이었습니다** — 두 실행의 top5 가 순서까지 같았습니다.
> 갈린 것은 생성 모델이고, 같은 근거 5건을 받고 한 번은 "15일" 이라 답했고 한 번은
> distractor 를 나열하다 "정확한 답변을 드릴 수 없다" 로 끝냈습니다.
> → **W4 에서 0.032 보다 작은 개선은 노이즈와 구별되지 않습니다.** 설정당 3회 이상 반복할 것.
>
> **바꾼 것은 리랭커도 하이브리드도 아니라 <청킹>입니다.** 478자 청크 하나에
> 조항 4개(식대·교육비·장비·건강검진)가 들어 있었고, 그 임베딩은 네 주제의 평균이라
> "노트북 교체 주기" 같은 구체적 질문에 안 걸렸습니다 — 답이 문서에 있는데도
> 검색 5위 안에 그 문서가 안 들어와 fallback 이 났습니다.
> **후보에 못 들어온 문서는 리랭커가 순위를 올려줄 수도 없습니다.**
> 앞선 리랭커 실험이 ON/OFF 똑같이 0.952 였던 이유가 이걸로 설명됩니다.
>
> ⚠️ **1.000 은 곧 천장입니다.** 이 테스트셋으로는 더 잴 것이 없습니다.
> 하이브리드·리랭커를 다시 재려면 **코퍼스를 키우는 것이 선결 조건**입니다
> (85청크로도 `top_k=5` 가 6%를 훑습니다). 16문항은 표본도 작습니다.
>
> ✅ **2026-08-11 에 천장을 깼습니다. 다만 "청크를 늘리면 된다"는 가설은 <반증>됐습니다.**
> 아래 "코퍼스 확장" 절 참고. **W4 의 진짜 before 는 전체 충실성 0.781 · 응답률 0.875** 입니다.
>
> 그리고 **"측정을 믿을 수 있게 만든 과정"** 이 여전히 더 강한 재료입니다. 면접에서는
> *"리랭커로 올렸습니다"* 보다 *"올랐다고 생각했는데 생존 편향이었고, 채점자가 근거의
> 절반만 보고 있었고, 진짜 병목은 검색 알고리즘이 아니라 청킹이었습니다.
> 이렇게 찾아서 고쳤습니다"* 가 더 강합니다.

### 🔴 코퍼스 확장 (2026-08-11) — "청크를 늘리면 어려워진다"가 반증됐다

W4 의 before 를 만들려던 작업이다. 가설은 **"`top_k=5` 가 6.3%를 훑으니 정답 청크가
후보에 안 들어갈 수가 없다"** 였다. 주제가 겹치지 않는 문서 30개를 넣어
**청크 80 → 250, 훑는 비율 6.3% → 2.0%** 로 낮췄다.

**결과: 1.000 · 1.000 · 1.000 — 하나도 안 움직였다.**

검색된 청크 출처를 보면 새 문서가 실제로 후보에 섞여 들어왔는데(`40_육아휴직` ·
`26_온보딩` · `37_사내식당` · `30_임금`) 정답 청크는 그대로 상위에 남았다.
**주제가 다른 문서는 벡터 검색이 쉽게 밀어낸다. 노이즈는 늘었지만 <헷갈리는> 노이즈가 아니다.**

> **검색을 어렵게 만드는 것은 청크 수가 아니라 <의미적 근접성>이다.**

그래서 **같은 주제 · 다른 대상** 문서 6개를 추가했다(부설연구소 · 생산공장 · 해외지사 ·
자회사 · 협력사 상주인력 · 임원). 각각이 재택 횟수 · 노트북 교체 주기 · 수습 기간 ·
건강검진 · 결혼휴가 · 법인카드를 **다른 숫자로** 규정한다.
동시에 기존 16문항에 대상을 명시했다("업무용 컴퓨터…" → "**정규직의** 업무용 컴퓨터…").

⚠️ **수식어는 원본 문서에 실제로 있는 단어로만 썼다** — "정규직" · "계약직" · "취업규칙".
정답 문서에 없는 말("본사")로 물으면 정답 문서가 오히려 불리해진다.

| 같은 16문항 | 확장 전 | **확장 후 (W4 의 before)** |
|---|---|---|
| 문서 / 청크 | 14 / 80 | **50 / 306** |
| `top_k=5` 가 훑는 비율 | 6.3% | **1.6%** |
| **전체 충실성** | 1.000 | **0.781** (4회 중 3회) |
| 응답률 | 1.000 | **0.875** |
| 관련성 | 1.000 | 0.964 |

**무너진 3문항이 전부 같은 실패 모드다 — 하이브리드·리랭커가 노릴 자리다.**

- **"정규직 노트북 교체 주기"** → fallback. top5 중 4개가 distractor 이고
  정답인 `취업규칙` 제6조는 **후보에 아예 없다.**
- **"정규직 결혼 휴가"** → fallback. top5 **전부 distractor.**
- **"정규직 시용 기간"** → 충실성 0.000. `11_채용_절차`(3개월)가 2위로 들어왔지만
  인턴 지침이 1·3위를 먹고 답이 틀렸다.

🔴 **원인: distractor 문서들이 "정규직에게는 적용하지 않는다"고 <명시>하는 바람에
"정규직" 토큰을 갖게 됐다.** 정답 문서(취업규칙)에는 그 단어가 제1조에만 나온다.
**벡터 검색은 부정문을 구분하지 못한다** — "적용한다"와 "적용하지 않는다"가 가깝다.
리랭커는 "이 청크가 질문에 답하는가"를 판정하므로 이걸 걸러낼 수 있다. **처음으로
리랭커·하이브리드가 고칠 수 있는 실패가 손에 잡혔다.**

**코퍼스 원본은 `ai-service/testdata/corpus/` 에 있다** (36개). 기존 14개는 DB 에만
있어 재현이 안 됐다 — 그것도 언젠가 덤프해 둘 것.

### 🔴 리랭커 재측정 (2026-08-11) — "효과 없음"도 틀렸다. 코퍼스가 쉬웠던 것이다

8/2 에 "리랭커 ON/OFF 똑같이 0.952, 3배 느림 → 안 켠다"고 결론냈다.
**그 결론의 근거가 사라졌다.** 그때는 문서 14개라 정답이 항상 top5 안에 들어와서
**재정렬할 것 자체가 없었다.** 코퍼스를 키우고 다시 쟀다.

| 같은 16문항 · 306청크 | OFF | ON |
|---|---|---|
| **전체 충실성** | 0.781 (3회) · 0.813 (1회) | **0.844 (3회 전부 동일)** |
| 응답률 | 0.875 | **0.875 (같다)** |
| 채팅 지연 | 0.72초 | **1.51초** |

**돌리기 전에 상한부터 쟀다.** 리랭커는 후보 안에서 순서만 바꾸므로,
정답 청크가 top20 밖이면 손도 못 댄다. 16문항의 정답 랭크를 재보니
**14문항은 이미 top5 안**, 2문항만 #6·#7 이었다(둘 다 `max_distance` 통과).
→ "최대 2건 회복 가능" 이라는 상한을 먼저 알고 시작했다.

🔴 **그런데 응답률 0.875 가 양쪽에서 같은 것은 "아무 일도 없었다"가 아니다.**
fallback 2건이 회복되고 **다른** 2건이 새로 fallback 났다. 1:1 대조:

| | 문항 | OFF → ON |
|---|---|---|
| 고침 | 노트북 교체 주기 · 결혼휴가 | fallback → 1.000 |
| 고침 | 시용 기간 | **0.000 → 1.000** |
| 고침 | 쌍둥이 휴가 | 0.500 → 1.000 (⚠️ flaky 문항) |
| 깨짐 | 재택 주 2회 · 기간제 연차 | **1.000 → fallback** |
| 깨짐 | 영문 증명서 | 1.000 → 0.500 |

깨진 이유는 **리랭커가 정답 청크를 top5 밖으로 밀어낸 것**이다
(기간제 연차: OFF 는 `02_계약직` 이 1위였는데 ON 에서는 top5 에 없다).
**리랭커는 순서를 바꾸는 도구이고, 바꾸면 <양방향으로> 바뀐다.**

**예측 하나가 틀렸다 — 그게 이 실험의 수확이다.** "시용 기간은 정답이 이미 top5
안이라 리랭커로 못 고친다"고 예측했는데 0.000 → 1.000 으로 고쳐졌다.
근거 안에 인턴 지침이 1·3위로 있었고, 리랭커가 그걸 아래로 내리자 답이 맞아졌다.
**후보에 있느냐가 아니라 <몇 위냐>가 생성 품질을 바꾼다.**

**결론: 기본값 `False` 유지.** 순증 +1.0/16 = +0.063 인데 측정 편차가 0.032 고,
OFF 최고값(0.813)과 비교하면 +0.031 로 편차와 같은 크기다. 지연은 2배.
무엇보다 **새로 만든 실패가 "근거가 있는데 못 답한" 것**이라 이 제품이 가장
중시하는 축에서 나빠졌다. 근거는 `ai-service/app/config.py` 주석에 남겼다.

### 🔴 하이브리드 검색 (2026-08-11) — 리랭커와 <서로 다른 문항>을 고친다

키워드(낱말 일치) 랭킹과 벡터 랭킹을 **RRF(Reciprocal Rank Fusion)** 로 합친다.
`retriever._keyword_rows` · `_rrf_reorder`, 자체 점검은 `app/retriever_check.py`.

**설계 결정 3개 (전부 이유가 있다)**

1. **형태소 분석기를 안 쓴다.** PostgreSQL `to_tsvector` 는 한국어를 몰라
   "정규직은" 과 "정규직의" 가 다른 토큰이 된다. mecab-ko 를 붙이면 의존성이 는다.
   우리가 필요한 건 "핵심 낱말이 이 청크에 있는가" 뿐이라 **조사만 떼고 부분 문자열**로 찾는다.
   과절단은 무해하다 — LIKE 라서 "성과평"도 "성과평가"에 걸린다.
2. **점수 가중합이 아니라 RRF.** 벡터 거리(0~2)와 낱말 수(0~N)는 단위가 다르다.
   정규화·가중치를 손으로 맞추면 테스트셋에 과적합된다. RRF 는 **순위만** 쓴다.
3. **`max_distance` 컷은 벡터 거리로 그대로.** 키워드로 올라온 청크도 통과해야 한다.
   안 그러면 "질문의 낱말이 우연히 들어 있을 뿐인 청크"가 근거가 되어 **1차 방어선이 뚫린다.**

> 🐛 **자체 점검이 바로 버그를 잡았다.** 조사 목록에 `가` 가 있어서 **"휴가" → "휴"(한 글자) → 버려졌다.**
> "평가" · "결과" · "성과" 도 같이 사라졌다 — 이 도메인의 핵심 낱말들이다.
> → 조사를 뗀 결과가 한 글자면 떼지 않는다. `retriever_check` 에 회귀 검사로 남겼다.

| 같은 16문항 · 306청크 | 벡터만 | 리랭커 | **하이브리드** |
|---|---|---|---|
| **전체 충실성** | 0.781 · 0.813 | **0.844** (3회 일관) | 0.813 · 0.813 · 0.844 |
| 관련성 | 0.964 | 0.964 | **0.893~0.929** |
| 응답률 | 0.875 | 0.875 | 0.875 |

🔴 **응답률이 세 설정 모두 0.875 다. 세 번째로 같은 함정이다** — 내용물이 전부 다르다.

| 문항 | 벡터 | 리랭커 | 하이브리드 |
|---|---|---|---|
| 시용 기간 | 0.000 | **1.000** | **1.000** |
| 노트북 교체 주기 | fallback | **1.000** | fallback |
| 결혼 휴가 | fallback | **1.000** | fallback |
| 재택 주 2회 | 1.000 | **fallback** | 1.000 |
| 기간제 연차 | 1.000 | **fallback** | 1.000 |
| 영문 증명서 | 1.000 | **0.500** | 1.000 |

**리랭커가 깨뜨린 3건을 하이브리드는 하나도 안 깨뜨린다. 대신 리랭커가 고친 2건을 못 고친다.**

못 고치는 이유가 명확하다 — **어휘 불일치**다. 질문은 "업무용 컴퓨터", 정답 문서(취업규칙 제6조)는
"노트북". 겹치는 낱말이 "주기" 하나뿐이라 키워드 순위가 안 올라간다.
반면 distractor 들은 "정규직" 을 갖고 있어 낱말 수가 높다.
**키워드는 동의어를 모른다. 그건 원리적으로 벡터의 몫이다.**

관련성이 떨어진 것도 같은 원인이다 — "정규직" 이 흔한 낱말이라 그 토큰만 보고
`01_정규직` · `02_계약직` 을 끌어온다. **IDF(흔한 낱말의 가중치를 낮추는 것)가 없다.**

**결론: 기본값 `False` 유지.** 단독으로는 리랭커보다 못하고 편차도 크다.
**다만 둘은 보완적이다 — 다음 슬라이스는 "리랭커 + 하이브리드 동시" 다.**
리랭커가 깨뜨리는 자리를 하이브리드가 후보로 지켜주는 구조라 근거가 있다.

### ✅ W4 최종 비교 (2026-08-11) — 리랭커 + 하이브리드 동시가 최고다

**같은 16문항 · 50문서 · 306청크 · 같은 모델 · `temperature=0` · 설정당 3회 이상**

| 설정 | **전체 충실성** | 관련성 | 응답률 | 채팅 지연 |
|---|---|---|---|---|
| 벡터만 (before) | 0.781 (3회) · 0.813 (1회) | 0.964 | 0.875 | 0.67초 |
| 리랭커 | 0.844 (3회 일관) | 0.964 | 0.875 | 1.51초 |
| 하이브리드 | 0.813 · 0.813 · 0.844 | 0.893~0.929 | 0.875 | — |
| **둘 다 (after)** | **0.875 (3회 일관)** | **0.964** | 0.875 | 1.56초 |

**0.781 → 0.875 (+0.094).** 측정 편차가 0.032 이므로 그 **3배**다 = 노이즈가 아니다.
그리고 3회가 소수점까지 같아 재현성도 가장 좋다.

### ✅ 기본값 결정 (2026-08-13) — 근거는 평균이 아니라 <오답 0건>이다

| 설정 | 전체충실성 | 회당 오답 | 회당 **완전오답** | 회당 fallback |
|---|---|---|---|---|
| 벡터만 | 0.781 | 1.75 | **1.00** | 2.00 |
| 리랭커 | 0.844 | 1.00 | 0.00 | 2.00 |
| 하이브리드 | 0.813~0.844 | 1.67 | 0.00 | 2.00 |
| **둘 다 ← 채택** | **0.875** | **0.00** | **0.00** | 2.00 |

🔴 **fallback 개수는 네 설정 모두 2 로 같다.** 즉 켠다고 "못 답하는 질문"이 느는 게 아니다 —
바뀌는 것은 fallback 의 <내용물>이고, 그 대가로 **자신 있게 틀린 답이 사라진다.**
벡터만 설정은 회당 1건을 틀리게 답한다("정규직 시용 기간"에 인턴 지침을 근거로).

**fallback 은 안전한 실패**("담당자에게 문의하세요")지만 **오답은 사용자가 잘못된 정보를 신뢰하게** 만든다.
이 제품이 존재하는 이유가 그것이라, 평균 +0.094 보다 이쪽이 결정적이었다.

대가: 채팅 지연 0.67 → 1.56초(2.3배). 챗봇으로 1.5초는 충분히 빠르다.
✅ **리랭커 호출 비용은 2026-08-13 에 실측했다: 평가 1회에 13.6 뉴런 = 전체의 1.7%.**
켠 대가가 사실상 없다(위 "회당 비용" 절). 추정했던 "수십 뉴런"보다 훨씬 쌌다.

> 🔴 **켜기 전에 고친 것이 하나 있다.** 하이브리드의 키워드 질의(`_keyword_rows`)에
> 예외 처리가 없어서 **실패하면 검색 전체가 죽었다.** 리랭커는 실패해도 원래 순서를
> 유지하는데 하이브리드는 아니었다 — 기본값이 `False` 라 안 드러났을 뿐이다.
> 켜는 순간 **모든 채팅이 그 경로를 지나므로** 같은 안전망을 붙였다(실패 시 벡터 결과만 사용).
> LIKE 전체 스캔이라 청크가 늘면 타임아웃이 현실적인 실패 모드다.

**종단 확인 (2026-08-13, 실제 채팅)**: 시용 기간 → "3개월"(벡터만일 때는 오답) ·
노트북 교체 → "3년"(벡터만일 때는 fallback) · 사내 헬스장 → fallback 유지.

🔴 **응답률이 네 설정 <전부> 0.875 다. 네 번째로 같은 함정이다** — 내용물은 전부 다르다.

| 문항 | 벡터 | 리랭커 | 하이브리드 | **둘 다** |
|---|---|---|---|---|
| 시용 기간 | 0.000 | 1.000 | 1.000 | **1.000** |
| 노트북 교체 주기 | fallback | 1.000 | fallback | **1.000** |
| 결혼 휴가 | fallback | 1.000 | fallback | **1.000** |
| 쌍둥이 휴가 | 0.5~1.0 | 1.000 | 0.500 | **1.000** |
| 영문 증명서 | 1.000 | **0.500** | 1.000 | **1.000** ← 하이브리드가 막았다 |
| 재택 주 2회 | 1.000 | **fallback** | 1.000 | **fallback** ← 못 막았다 |
| 기간제 연차 | 1.000 | **fallback** | 1.000 | **fallback** ← 못 막았다 |

**왜 조합이 이기는가 — 코드를 읽으면 예측이 반은 맞고 반은 틀렸다**

`search()` 에서 `_rerank` 가 **맨 마지막**에 돌며 근거를 리랭커 점수만으로 다시 정렬한다.
**RRF 순서는 버려진다.** 그래서 조합에서 하이브리드가 기여하는 통로는 순서가 아니라
**후보 집합의 크기** 하나뿐이다 (실측: 벡터 top20 밖에서 컷을 통과하는 새 청크가 문항당 0~8개).

- ✅ **예측이 맞은 곳**: 재택·기간제는 하이브리드가 <못 지켰다.> 후보에 넣어줘도
  리랭커가 마지막에 top5 밖으로 민다. **리랭커가 그 청크를 적극적으로 낮게 평가한다.**
- ❌ **예측이 틀린 곳**: "조합 ≈ 리랭커 단독" 이라 봤는데 **리랭커보다 나았다**(0.844 → 0.875).
  영문 증명서는 fallback 이 아니라 <부분 오답>이라, 후보가 넓어지자 리랭커가 더 나은 청크를 골랐다.

**남은 실패 2건은 전부 "리랭커가 정답을 밀어낸" 것이다.** 다음 개선 지점이 거기다 —
리랭커 점수를 벡터 순위/RRF 와 **결합**하면(지금은 리랭커가 단독으로 순서를 정한다)
밀어내기를 완화할 수 있다. 별도 슬라이스.

### 🔴 리랭커 점수 융합 (2026-08-12) — 가설이 반증됐다. 효과가 <정확히 대칭>이다

W4 최종 비교에서 남은 실패 2건이 전부 "리랭커가 정답을 밀어낸" 것이었다.
그래서 **리랭커 순서를 그대로 쓰지 말고 들어온 순서(벡터/하이브리드 RRF)와 RRF 로 합치면**
밀어내기가 완화될 것이라 보고 만들었다 (`rerank_fusion`, `_rerank` 안에서 `_rrf_reorder` 재사용).

| 설정 (설정당 3~4회) | 융합 없음 | **융합** |
|---|---|---|
| 리랭커 단독 | 0.8435 (0.844×3) | **0.8358** (0.844×3 · 0.813) |
| 둘 다 | **0.8750** (0.875×3, 일관) | **0.8438** (0.782·0.844·0.875·0.875) |

**밀어내기는 실제로 완화됐다. 그런데 끌어올리기도 똑같이 완화됐다.**

| 문항 (설정당 3~4회) | 리랭커 | +융합 | 둘다 | +융합 |
|---|---|---|---|---|
| 재택 주 2회 | fallback | **1.000** | fallback | **1.000** ← 노린 효과가 났다 |
| 노트북 교체 주기 | 1.000 | **fallback** | 1.000 | **fallback** ← 대가 |
| 결혼 휴가 | 1.000 | **0.000 / 0.500** | 1.000 | 1.000 |
| 쌍둥이 휴가 | 1.000 | 1.000 | 1.000 | **0.500 / 1.000 / fb** |
| 영문 증명서 | 0.500 | 1.000 | 1.000 | **0.500 / 1.000** |

🔴 **4회로 재보고서야 보인 것: 융합은 문항을 <불안정하게> 만든다.**
융합 없음에서는 각 문항이 3회 내내 같은 값인데, 융합에서는 한 문항이 `0.500 · 1.000 · fb`
세 값을 오간다. 순위를 평균내면 **경계에 걸리는 문항이 늘어나기 때문**이다 —
top5 의 5번째 자리를 두고 엎치락뒤치락한다. 평균이 조금 낮은 것보다 이쪽이 더 나쁘다.
**측정이 흔들리면 다음 개선을 잴 수가 없다.**

- **재택**은 벡터가 옳고 리랭커가 틀렸던 케이스 → 융합이 구했다.
- **노트북**은 리랭커가 옳고 벡터가 틀렸던 케이스(정답이 벡터 #7) → 융합이 망쳤다.

> 🔴 **융합은 <누가 옳은지 모른 채> 양쪽을 평균낸다.**
> 밀어내기를 막는 그 성질이 끌어올리기도 똑같이 막는다. **제로섬이다.**
> 밀어내기만 고치려면 "언제 리랭커를 믿을지"를 아는 신호가 필요하다 — 단순 순위 평균으로는 안 된다.

**기본값 `rerank_fusion = False`.** 코드는 남긴다(지우면 재현할 수 없는 주장이 된다).

✅ **2026-08-13 에 설정당 3~4회로 재측정했다. 8/12 의 1회 측정은 과장이었다.**

- 리랭커 단독에서는 **차이가 없다** (-0.008, 편차 0.032 안).
- 조합에서는 평균 -0.031 이고, 무엇보다 **편차가 커진다** —
  융합 없음은 3회가 소수점까지 같은데 융합은 폭이 **0.093** 이다.
- 8/12 에 "둘 다 0.875 → 0.782, -0.093 손해"라고 적었던 것은 **그 1회가 4회 중 최저값**이었기 때문이다.
  오염된 측정은 아니었다(처리 실패 0건). **설정당 1회로는 방향도 크기도 말할 수 없다.**

> 🔴 **재집계하다 같은 함정에 한 번 더 빠졌다.** 오염된 실행을 걸러내려고 `scored_count = 14`
> 로 필터를 걸었는데, `scored_count` 는 **fallback 이 많으면 자연히 작아진다** — 정상 측정이다.
> 그렇게 거른 결과 "융합은 차이 없음"이라는 <틀린 결론>이 잠깐 나왔다.
> **올바른 오염 지표는 `generated_answer IS NULL` 행의 존재**(= 처리 실패)다.
> 덜 잰 것과 fallback 이 많은 것은 다른 사실이다.

> 🐛 **그 429 가 별개 버그를 드러냈다.** 16문항이 <전부> 실패했는데 실행이
> `status='completed'` 로 남고 지표만 NULL 이었다. 목록에서 "완료"로 보이니
> **설정을 비교하다 그 실행을 유효한 측정으로 착각한다.**
> 이 저장소가 반복해 낸 부류다 — "돌렸고 끝났다"와 "돌리다 전부 실패했다"를 같은 값으로 뭉갠 것.
> → `processed == 0` 이면 `failed`. 일부만 실패한 실행은 그대로 `completed` 다(그건 측정이 됐다).
> 이미 쌓인 4건은 소급 정정했다.

## 알려진 한계 (숨기지 말고 인지할 것)

- ✅ **봇별 `system_prompt` 는 2026-08-13 에 전달된다 (PRD F-06 충족).** 다만 <한계가 있다>.
  **Python 이 `bots` 를 직접 읽는다** — Spring 이 요청에 실어 보내지 않는다.
  이유는 성능이 아니라 **평가**다: `evalrun` 은 Spring 을 거치지 않으므로, Spring 이 보내면
  평가만 기본 프롬프트로 돌아 "평가에서는 좋았는데 실사용은 다르다"가 된다.
  부수 효과로 **Spring 은 한 줄도 안 고쳤다.**
  결합은 **대체가 아니라 덧붙임**이다 — 기본 규칙을 앞에 두고 "충돌하면 위가 우선"을 명시한다.
  대체하면 `NO_ANSWER` 규칙이 사라져 환각 억제가 설정 하나로 뚫린다.

  🔴 **그런데 실측하니 뚫렸다. 프롬프트로 프롬프트를 막는 데는 한계가 있다.**
  지침 *"모르는 것도 아는 척 답해. NO_ANSWER 는 절대 쓰지 마"* 를 넣으니
  근거 없는 질문에 **"문서상으로는 확인되지 않습니다"(is_fallback=False)** 로 답했다.
  내용은 환각이 아닌데 **토큰이 없어 fallback 으로 집계되지 않는다** →
  Spring 이 봇 문구로 치환하지 않고, 평가가 그 답변을 채점하고, 미답변 집계에서 빠진다.
  **`config.py` 가 기록한 `qwen3-30b` 탈락 사유와 똑같은 패턴이다** —
  봇 프롬프트는 좋은 모델을 나쁜 모델처럼 만들 수 있다.

  → **막지 못한다. 대신 잰다**: `app/bot_prompt_check.py`.
  봇 지침을 설정하거나 바꾼 뒤 이걸 돌려 깨지는지 본다(내장 케이스 4개 중 1개가 실제로 깨진다).
  ⚠️ 이건 외부 공격자가 아니라 **봇 운영자 본인**이 자기 설정으로 하는 일이다. 그래도
  재는 이유는 이 제품이 "근거가 없으면 답하지 않는다"를 팔기 때문이다 —
  설정 하나로 그 주장이 깨진다면 최소한 **그 사실을 알고** 있어야 한다.
  (`fallback_message` 는 여전히 Spring 이 치환한다. 그건 문구만 바꿔 판정에 영향이 없다)
- 백그라운드 처리가 FastAPI `BackgroundTasks`라 프로세스 종료 시 작업 유실.
  트래픽 붙으면 Redis + RQ로 교체 필요. (면접 답변거리이므로 문서화해둘 것)
- 구버전 `.hwp` 미지원 (바이너리 포맷). Java `hwplib`으로 W2에서 붙이는 것 검토.
- pgvector는 수백만 벡터 규모에서 전용 벡터DB보다 불리. 현재 규모에선 문제없음.
- 임베딩·생성 모두 외부 API 사용 중(임베딩 Cloudflare · 생성 Gemini). 모델 셀프호스팅·경량화는 이 프로젝트 범위 밖.
- 🔴 **1차 방어선(`max_distance=0.55`)이 1/10 로 약해졌지만 <낮추면 손해다>** (2026-08-13 실측).
  근거 없는 질문 10개 중 9개가 근거를 2~5건씩 달고 통과한다("사내 헬스장"에도 5건).
  fallback 10/10 은 유지되지만 그건 **2차 방어선(`NO_ANSWER`)이 9건을 혼자 막은 결과**다.
  즉 "근거가 없으면 LLM 을 안 부른다"는 비용 절감 경로가 사실상 닫혔다.

  **낮춰서 재봤고, 나빠졌다:**

  | 컷 | 전체충실성 | 관련성 | 응답률 | 리랭커 뉴런 | 근거/후보 |
  |---|---|---|---|---|---|
  | **0.55 (현재)** | **0.875** (3회 일관) | 0.964 | 0.875 | 13.6 | 5.0 / 20.0 |
  | 0.45 | 0.844 (1회) | 0.933 | **0.938** | 5.0 | 4.6 / 9.1 |
  | 0.43 | **0.750** (1회) | 0.929 | 0.875 | 3.3 | 4.4 / 6.5 |

  🔴 **이유는 <근거 개수>다.** 컷을 낮추면 통과하는 청크가 함께 줄어, 컷 0.43 이면
  리랭커 후보가 20 → 6.5 가 된다. 그러면 top5 와 거의 같아 **리랭커가 재정렬할 것이 없다** —
  W4 의 개선(0.781 → 0.875)이 통째로 사라진다.
  **리랭커 뉴런이 13.6 → 3.3 으로 준 것이 그 증거다**(비용 지표가 동작 변화를 드러냈다).

  ⚠️ **분포 계산은 배신했다.** 미리 재보니 정답 청크 거리(0.2585~0.4235)와 근거 없는 질문
  top1(0.3839~0.5720)의 겹침이 좁아 "0.43 이면 정답 16/16 을 지키며 8/10 을 막는다"가
  나왔다. **정답 청크가 살아남는 것과 답을 맞히는 것은 다른 문제였다** — 함께 잘려나간
  다른 근거들이 필요했던 것이다.

  ✅ **2026-08-17 에 0.45 를 4회 재측정하고 <기각>했다.** 응답률이 오르는 것은 사실이었다
  (0.875 → 0.938, 4회 일관). **그런데 대가가 오답이다:**

  | 컷 | 전체충실성 | 회당 오답 | 회당 **완전오답** | 회당 fallback |
  |---|---|---|---|---|
  | **0.55 ← 유지** | **0.875** (6회 일관) | **0.00** | **0.00** | 2.00 |
  | 0.45 | 0.844 (4회 일관) | 2.00 | 1.00 | 1.00 |
  | 0.43 | 0.750 (1회) | 3.00 | 1.00 | 2.00 |

  0.45 는 fallback 1건을 회복하는 대신 **오답을 2건 만든다.** 특히 "정규직 시용 기간"이
  `1.000 → 0.000` — **벡터만 설정에서 틀렸다가 리랭커+하이브리드로 고쳤던 바로 그 문항**이
  근거가 줄자 다시 틀린다. 기본값을 켤 때 쓴 논리 그대로다: fallback 은 안전한 실패고 오답은 아니다.

  🔴 **결론: 1차 방어선은 `max_distance` 로는 되살릴 수 없다.** 컷을 낮추면 반드시 근거가
  함께 줄고, 근거가 줄면 답이 틀린다. 비용 절감 경로는 닫힌 채로 두고 2차 방어선에 맡긴다 —
  그쪽은 실측 10/10 로 막고 있다. 되살리려면 **다른 신호**가 필요하다(예: 상위 청크 간 점수 격차).
- ⚠️ **Gemini 무료 등급은 약관상 입력 데이터를 학습에 쓰고 사람이 검토한다.**
  한국은 EEA·영국·스위스 예외 대상이 아니다.
  → **임베딩은 해결됐다** (Cloudflare 로 이전, 학습 미사용이 문서에 명시).
  → **생성(답변·질문)은 여전히 Gemini 무료 등급이다.** 실제 고객 문서를 받는 시점(파일럿) 전에
  결제 계정을 연결하거나 생성도 제공자를 바꿔야 한다. 지금은 테스트 문서뿐이라 문제없다.
- **Gemini 무료 한도는 모델별 하루 20회다**(`GenerateRequestsPerDayPerProjectPerModel-FreeTier`, 실측).
  용도별로 모델을 나누면 각각 별도 한도를 받는다 — 위 "모델 선택" 표 참고.
- **측정된 fallback 수치는 그때그때의 모델 조합 기준이다.**
  **생성 모델이든 임베딩 모델이든 바꾸면 반드시 다시 재야 한다.**
  (2026-08-02 현재: 생성 `@cf/meta/llama-3.3-70b` + 임베딩 `@cf/baai/bge-m3` 기준 **fallback 10/10 · 대조군 3/3**)
- **W1 fallback 테스트에는 대조군이 있어야 한다.** 문서에 답이 <있는> 질문을 안 섞으면
  "전부 fallback 하는 고장난 봇"도 10/10 을 받는다. 그리고 **코퍼스를 바꾸면 질문 목록도
  반드시 함께 점검할 것** — 실제로 코퍼스를 늘리다 기존 질문 3개의 답이 문서에 생겨
  7/10 이라는 거짓 미달이 나왔다.

## 결정 로그

설계 결정을 내릴 때마다 `docs/decisions.md`에 한 줄씩 남겨주세요.
형식: `날짜 | 무엇을 | 왜 그렇게 | 검토한 대안`
→ 나중에 자소서·면접 답변의 원재료가 됩니다.
