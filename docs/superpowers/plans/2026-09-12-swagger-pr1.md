# Swagger(OpenAPI) PR 1 구현 계획 - 기반 + 노출 정책 + summary 33개

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `api/` 에 springdoc-openapi 를 붙여 로컬에서만 열리는 Swagger UI 를 띄우고, 전 엔드포인트 33개에 `summary` 를 채운 뒤 "빠진 것"과 "운영 노출"을 CI 테스트 둘로 잠근다.

**Architecture:** 의존성 한 줄(`springdoc-openapi-starter-webmvc-ui:2.8.6`) + 공통 설정 한 파일(`OpenApiConfig`: Info · JWT 스킴 · 공통 에러 응답) + 노출을 막는 두 겹(운영 프로파일의 `enabled: false`, `SecurityConfig` 의 프로파일 분기). 검증은 통합 테스트 3개로 한다: ① 로컬에서 열린다 ② 운영에서 두 겹이 실제로 막는다 ③ `summary` 가 빠진 엔드포인트가 없다.

**Tech Stack:** Spring Boot 4.0.7 / Java 21 / Gradle-Groovy / springdoc-openapi 2.8.6 / swagger-annotations 2.2.29 / JUnit 5 + AssertJ + `RestTestClient` + Testcontainers(pgvector)

## Global Constraints

이 절의 값은 **모든 태스크의 요구사항에 암묵적으로 포함된다.**

- 원본 스펙: `docs/superpowers/specs/2026-09-12-swagger-design.md`. 이 계획과 어긋나면 아래 "스펙과 의도적으로 다르게 가는 곳" 절을 볼 것.
- **주석 · 문구 · 테스트 이름은 전부 한국어.** 사용자에게 보이는 에러는 "무엇을 어떻게 하면 되는지"까지 적는다.
- **em dash(`-`) 를 쓰지 않는다.** 쉼표 · 콜론 · 괄호 · `·` 로 대체한다. 대화 · 문서 · 주석 · 커밋 전부 해당.
- **에러 응답 포맷은 전 계층 공통 `{ "error": { "code": "...", "message": "..." } }`.** 문서에 적는 예시도 이 모양이어야 한다.
- **`V1__init.sql` 을 비롯한 적용된 Flyway 마이그레이션을 고치지 않는다.** 이 PR 은 마이그레이션을 추가하지도 않는다.
- **`spring.jpa.hibernate.ddl-auto` 는 `validate` 그대로.** 건드리지 않는다.
- **springdoc 버전은 `2.8.6` 으로 고정해 적는다.** Boot 의 dependency-management 가 관리하지 않는 서드파티라 버전을 적어야 한다.
- **운영 프로파일 스위치는 `SPRING_PROFILES_ACTIVE=prod` 하나다.** 새 축(예: `app.swagger.enabled`)을 만들지 않는다.
- **springdoc 이 쓰는 기본 경로:** `/v3/api-docs`, `/v3/api-docs/**`(`swagger-config` 포함), `/v3/api-docs.yaml`, `/swagger-ui.html`, `/swagger-ui/**`. 경로를 커스터마이즈하지 않는다.
- 테스트 실행: `cd api && ./gradlew test`. 전체 빌드: `cd api && ./gradlew build`.
- **브랜치:** `feat/swagger-openapi-base`. `main` 에 직접 밀지 않는다(인증 경로의 보안 설정을 건드리고, 왜 그렇게 했는지를 설명해야 하는 변경이다).
- 커밋 메시지: `<타입>: <한국어 요약>` (feat / fix / refactor / test / docs / chore).

---

## 전제 확인 (실측으로 확인된 사실만 쓴다)

| 사실 | 확인 방법 |
|---|---|
| 엔드포인트 33개 · 컨트롤러 11개 | 2026-09-12 에 `@(Get\|Post\|Put\|Patch\|Delete)Mapping` 을 세어 확인. 스펙의 수와 일치 |
| `ErrorCode` 36개 · `INTERNAL_ERROR` · `AUTHENTICATION_REQUIRED` 존재 | `ErrorCode.java` grep |
| springdoc 최신은 2.8.6 이고 3.x 는 없다 | Maven Central 조회 |
| 2.8.6 의 부모 POM 은 `spring-boot-starter-parent 3.4.4` | `springdoc-openapi-2.8.6.pom` 조회 |
| Boot 4.0.7 에서 기동 · `/v3/api-docs` 200 · 테스트 206건 통과 | 스펙 §1 의 스파이크(2026-09-12) |
| 통합 테스트는 전부 `@IntegrationTest`(진짜 톰캣 `RANDOM_PORT` + Testcontainers pgvector) | `support/IntegrationTest.java` |
| 열린 PR 0건 · 작업트리 깨끗 · `main` 최신(`e41fa3a`) | 2026-09-12 확인 |

---

## 스펙과 의도적으로 다르게 가는 곳 (2곳. 리뷰에서 이 절을 먼저 볼 것)

### ① 공통 401 을 **전 엔드포인트에 일괄로 달지 않는다**

스펙 §4 는 "모든 엔드포인트에 공통인 것만(401 · 500)" 을 `OperationCustomizer` 로 붙이라고 적었다.
그런데 **401 은 모든 엔드포인트에 공통이 아니다.** `/api/auth/signup` 은 401 을 낼 수 없고,
위젯 API(`/api/w/**`)도 인증이 없어 401 이 나갈 일이 없다.
일괄로 달면 **문서가 없는 응답을 있다고 말한다.** 이 저장소가 제일 경계하는 부류다.

→ **500 은 전부에 달고, 401 은 "인증이 필요한 엔드포인트에만" 단다.**
판정 근거는 그 메서드에 `@SecurityRequirements`(전역 JWT 요구를 개별 해제하는 애노테이션)가 붙었는지 여부다.
해제한 엔드포인트 = 토큰을 안 받는 엔드포인트 = 401 이 없는 엔드포인트. 한 신호로 두 곳이 자동으로 맞는다.

⚠️ 로그인의 401(`INVALID_CREDENTIALS`)은 "토큰이 없어서" 가 아니라 "비밀번호가 틀려서" 다. **별개 사실이므로 PR 2 에서 개별로 단다.** 이 PR 에서 로그인에 401 이 안 보이는 것은 정상이다.

### ② 공통 에러 스키마를 **`ErrorResponse` 클래스 참조가 아니라 손으로 정의한다**

`@Schema(implementation = ErrorResponse.class)` 로 두면 springdoc 이 타입을 뒤져 스키마를 만들어야 한다.
Boot 4 는 Jackson 3(`tools.jackson`)을 쓰는데 swagger-core 2.2.29 는 Jackson 2 를 쓴다.
**두 Jackson 이 한 클래스패스에 있는 상태에서 모델 introspection 에 의존하지 않는 쪽이 안전하다.**
스키마가 `code` · `message` 두 필드뿐이라 손으로 적는 비용이 거의 없고, 결과가 결정적이다.

---

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `api/build.gradle` | springdoc 의존성 한 줄 (버전 명시 + 왜 명시하는지 주석) | 1 |
| `api/src/main/java/com/alldap/api/global/config/SecurityConfig.java` | 문서 경로를 **prod 가 아닐 때만** `permitAll`. 노출 방어 두 겹 중 ② | 1 |
| `api/src/test/java/com/alldap/api/global/config/OpenApiExposureIntegrationTest.java` | 로컬에서 문서가 **토큰 없이 열린다** | 1 |
| `api/src/main/resources/application-prod.yaml` | `springdoc.*.enabled: false`. 노출 방어 두 겹 중 ① | 2 |
| `api/src/test/java/com/alldap/api/global/config/OpenApiProdExposureIntegrationTest.java` | prod 프로파일로 실제 기동해 **두 겹이 각각 동작하는지** 확인 | 2 |
| `api/src/main/java/com/alldap/api/global/config/OpenApiConfig.java` | Info · JWT 보안 스킴(전역) · 공통 에러 스키마 · 공통 응답 customizer | 3 |
| `api/src/main/java/.../auth/controller/AuthController.java` 외 3개 | `@SecurityRequirements` 로 전역 JWT 요구 개별 해제(공개 엔드포인트 4개) | 3 |
| `api/src/test/java/com/alldap/api/global/config/OpenApiDocumentIntegrationTest.java` | 생성된 `/v3/api-docs` JSON 의 내용 검증 | 3 |
| `api/src/test/java/com/alldap/api/global/config/OpenApiCoverageTest.java` | `@Tag` · `@Operation(summary)` 빠진 것을 **전부 나열**하고 실패 | 4 |
| 컨트롤러 11개 전부 | `@Tag` 11개 + `@Operation(summary)` 33개 | 4 |
| `AGENTS.md` · `docs/decisions.md` | 진행 상황 한 행 + 결정 로그 | 5 |

**새 파일을 `global/config` 에 두는 이유:** `SecurityConfig` · `RestClientConfig` · `CorsProperties` 가 이미 거기 있다. 기존 패턴을 따른다. 테스트도 같은 패키지 경로(`global/config`)에 둔다(`ManagementPortIntegrationTest` 와 같은 자리).

---

## Task 1: 의존성 + 로컬 노출 (`SecurityConfig` 프로파일 분기)

**Files:**
- Modify: `api/build.gradle` (dependencies 블록 안)
- Modify: `api/src/main/java/com/alldap/api/global/config/SecurityConfig.java`
- Test: `api/src/test/java/com/alldap/api/global/config/OpenApiExposureIntegrationTest.java` (신규)

**Interfaces:**
- Consumes: 없음 (첫 태스크)
- Produces:
  - `SecurityConfig.securityFilterChain(HttpSecurity, JwtService, ApiAuthenticationEntryPoint, ApiAccessDeniedHandler, CorsConfigurationSource, Environment)` : 파라미터 맨 끝에 `org.springframework.core.env.Environment` 가 **추가된다**. 시그니처가 바뀌는 유일한 기존 메서드다.
  - `SecurityConfig.OPEN_API_PATHS` : `static final String[]`. Task 2 의 테스트가 "이 경로들" 을 가리킬 때 문자열을 다시 적지 않게 공개한다.

- [ ] **Step 1: 브랜치를 딴다**

```bash
cd /Users/cheonjamin/projects/AllDap
git switch -c feat/swagger-openapi-base
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`api/src/test/java/com/alldap/api/global/config/OpenApiExposureIntegrationTest.java` 를 새로 만든다.

```java
package com.alldap.api.global.config;

import com.alldap.api.support.IntegrationTest;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.web.server.LocalServerPort;

import java.io.IOException;
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * <b>로컬</b>에서 API 문서가 토큰 없이 열리는지 재는 통합 테스트.
 *
 * <h2>왜 "토큰 없이" 가 검증 대상인가</h2>
 * 문서의 목적이 전시다. 열 때마다 먼저 가입하고 토큰을 붙여야 한다면 목적을 못 이룬다.
 * (Swagger UI 의 Authorize 는 <b>Try it out 으로 실제 API 를 부를 때</b> 쓰는 것이고,
 *  문서를 <b>읽는</b> 데에는 필요하지 않다)
 *
 * <p>운영에서 열리지 않는 것은 짝 테스트인 {@code OpenApiProdExposureIntegrationTest} 가 잰다.
 * 두 테스트는 프로파일이 달라 <b>같은 클래스에 둘 수 없다</b>(활성 프로파일은 컨텍스트 단위 설정이다).
 *
 * <h2>JDK {@code HttpClient} 를 쓰는 이유</h2>
 * Boot 4 에서는 {@code TestRestTemplate} 빈이 자동 등록되지 않아
 * {@code @AutoConfigureTestRestTemplate} 을 붙여야 하는데, 그러면 이 클래스만 테스트 설정이 달라져
 * 애플리케이션 컨텍스트가 하나 더 생기고 Testcontainers 도 하나 더 뜬다
 * ({@code IntegrationTest} 주석이 경계하는 그 상황). 필요한 것이 상태 코드와 본문뿐이라
 * {@code ManagementPortIntegrationTest} 와 같은 방식으로 JDK 표준 클라이언트를 쓴다.
 */
@IntegrationTest
@DisplayName("OpenAPI 문서 노출 (로컬)")
class OpenApiExposureIntegrationTest {

    @LocalServerPort
    int port;

    private final HttpClient http = HttpClient.newHttpClient();

    @Test
    @DisplayName("/v3/api-docs 는 토큰 없이 200 이고 OpenAPI 문서를 준다")
    void api_docs_는_토큰_없이_열린다() {
        HttpResponse<String> response = get("/v3/api-docs");

        assertThat(response.statusCode()).isEqualTo(200);
        // 200 만 보면 빈 본문이어도 통과한다. 실제로 OpenAPI 문서인지까지 확인한다.
        assertThat(response.body()).contains("\"openapi\"");
        // 우리 엔드포인트가 실제로 수집됐는가. springdoc 이 떠도 스캔이 비면 문서는 껍데기다.
        assertThat(response.body()).contains("/api/bots");
    }

    @Test
    @DisplayName("Swagger UI 화면도 토큰 없이 200 이다")
    void swagger_ui_는_토큰_없이_열린다() {
        // springdoc 은 /swagger-ui.html 로 오면 /swagger-ui/index.html 로 보낸다.
        // JDK HttpClient 는 기본적으로 리다이렉트를 따라가지 않으므로 최종 주소를 직접 부른다.
        HttpResponse<String> response = get("/swagger-ui/index.html");

        assertThat(response.statusCode()).isEqualTo(200);
    }

    @Test
    @DisplayName("[실측 기록] 문서를 열어도 보호 경로는 그대로 401 이다")
    void 보호_경로는_영향을_받지_않는다() {
        // permitAll 을 잘못 적어 /api/** 까지 열리는 사고를 이 한 줄이 잡는다.
        assertThat(get("/api/bots").statusCode()).isEqualTo(401);
    }

    private HttpResponse<String> get(String path) {
        try {
            HttpRequest request = HttpRequest.newBuilder()
                    .uri(URI.create("http://localhost:" + port + path))
                    .GET()
                    .build();
            return http.send(request, HttpResponse.BodyHandlers.ofString());
        } catch (IOException e) {
            throw new AssertionError("요청이 실패했다: " + path, e);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new AssertionError("요청이 중단됐다: " + path, e);
        }
    }
}
```

- [ ] **Step 3: 실패를 확인한다**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test --tests '*OpenApiExposureIntegrationTest*'
```

기대: **FAIL.** `api_docs_는_토큰_없이_열린다` 가 `expected: 200 but was: 401` 로 깨진다
(springdoc 이 없으니 매핑도 없고, `anyRequest().authenticated()` 가 먼저 401 을 낸다).
`보호_경로는_영향을_받지_않는다` 는 지금도 통과한다(그게 정상이다).

- [ ] **Step 4: 의존성을 추가한다**

`api/build.gradle` 의 `dependencies { ... }` 블록에서 `spring-boot-starter-webmvc` 줄 **바로 아래**에 넣는다.

```groovy
	// ── API 문서 (Swagger UI / OpenAPI) ──────────────────────────────────
	// 🔴 버전을 직접 적는다. Boot 의 dependency-management 가 관리하지 않는 서드파티다.
	//
	// 🔴 이 버전은 <Boot 3 계열(3.4.4)을 부모로 삼아> 빌드됐다. springdoc 3.x 는 존재하지 않는다
	//    (2026-09-12 Maven Central 확인 결과 2.8.6 이 최신). 즉 Boot 4 호환이 <보장된 것이 아니라
	//    우연히 맞는 것>이고, Boot 를 올릴 때 깨질 수 있으며 그때 후퇴할 곳이 없다. 알고 택했다.
	//    근거와 실측 결과: docs/superpowers/specs/2026-09-12-swagger-design.md
	//
	// ⚠️ 운영에서는 문서를 내보내지 않는다. 막는 곳이 두 군데다:
	//      ① application-prod.yaml 의 springdoc.*.enabled: false   (문서 생성 자체를 끈다)
	//      ② SecurityConfig 의 프로파일 분기                        (경로를 인증 뒤에 둔다)
	//    한쪽만 두면 우회 경로가 생겨 규칙이 없는 것과 같아진다.
	implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.8.6'
```

- [ ] **Step 5: `SecurityConfig` 에 프로파일 분기를 넣는다**

먼저 import 를 추가한다.

```java
import org.springframework.core.env.Environment;
```

클래스 본문 맨 위(`public class SecurityConfig {` 바로 다음)에 경로 상수를 둔다.

```java
    /**
     * springdoc 이 쓰는 기본 경로들. <b>로컬에서만</b> 인증 없이 열린다(아래 분기 참고).
     *
     * <p>상수로 빼둔 이유: 짝 테스트({@code OpenApiProdExposureIntegrationTest})가 같은 경로를
     * 검사해야 하는데, 문자열을 양쪽에 적으면 한쪽만 고쳐져 <b>테스트가 엉뚱한 주소를 재게 된다</b>.
     *
     * <p>네 갈래를 모두 적는 이유:
     * <ul>
     *   <li>{@code /v3/api-docs} 자체는 {@code /v3/api-docs/**} 에 걸리지 않는다(하위 경로만 매칭)</li>
     *   <li>{@code /v3/api-docs/**} 는 Swagger UI 가 부르는 {@code /swagger-config} 를 포함한다</li>
     *   <li>{@code /v3/api-docs.yaml} 은 같은 문서의 YAML 표현이다. 빼면 JSON 만 열려 어긋난다</li>
     *   <li>{@code /swagger-ui.html} 은 {@code /swagger-ui/index.html} 로 가는 입구다</li>
     * </ul>
     */
    static final String[] OPEN_API_PATHS = {
            "/v3/api-docs",
            "/v3/api-docs/**",
            "/v3/api-docs.yaml",
            "/swagger-ui.html",
            "/swagger-ui/**",
    };
```

`securityFilterChain` 의 시그니처에 `Environment environment` 를 **맨 끝 파라미터로** 추가하고, javadoc `@param` 도 한 줄 더한다.

```java
     * @param environment              활성 프로파일을 보고 API 문서 경로를 열지 말지 정한다(아래 참고)
     */
    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http,
                                                   JwtService jwtService,
                                                   ApiAuthenticationEntryPoint authenticationEntryPoint,
                                                   ApiAccessDeniedHandler accessDeniedHandler,
                                                   CorsConfigurationSource corsConfigurationSource,
                                                   Environment environment) throws Exception {
```

`.authorizeHttpRequests(auth -> auth ... )` 를 **표현식 람다에서 블록 람다로** 바꾼다.
`/widget/**` permitAll 줄과 `.anyRequest().authenticated()` 사이에 분기를 넣는다.

```java
                .authorizeHttpRequests(auth -> {
                    auth
                        // ── 공개 경로 ───────────────────────────────────────────────
                        // (기존 permitAll 줄 5개는 주석까지 그대로 둔다. 체인만 세미콜론으로 끊는다)
                        .requestMatchers("/api/auth/signup", "/api/auth/login").permitAll()
                        .requestMatchers("/api/w/**").permitAll()
                        .requestMatchers("/actuator/health", "/actuator/prometheus").permitAll()
                        .requestMatchers("/widget/**").permitAll();

                    // ── API 문서: 운영이 아닐 때만 연다 ─────────────────────────
                    // 🔴 노출 방어 두 겹 중 <②> 다. ① 은 application-prod.yaml 이 문서 생성 자체를 끄는 것.
                    //    두 겹으로 두는 이유: ① 만 두면 나중에 누가 그 설정을 되돌리는 순간 전부 열리고,
                    //    ② 만 두면 문서는 계속 생성되면서 401 뒤에 숨어 있을 뿐이라 계정 하나만 있으면 읽힌다.
                    //    (요금제 불변식을 PlanService 와 BillingService.delete 양쪽에서 막은 것과 같은 논리다.
                    //     한쪽만 두면 우회 경로가 생겨 규칙이 없는 것과 같아진다)
                    //
                    // 🔴 판정을 <허용 목록>이 아니라 "prod 가 아니면 연다" 로 적은 것은 의도다.
                    //    JwtService·BillingCrypto 의 가드는 반대로(개발 프로파일 허용 목록) 판정하는데,
                    //    그건 "모르는 이름은 운영일 수 있다" 로 보수적으로 굴어야 하는 <비밀값> 이라서다.
                    //    여기서 같은 방식을 쓰면 staging 같은 이름에서 문서가 <닫혀> 전시가 안 되는데,
                    //    그건 안전 쪽 실패가 아니라 목적 상실이다. 반면 prod 는 이 저장소의 유일한 운영 스위치이고
                    //    docker-compose.prod.yml 이 그것을 준다(docs/DEPLOY.md).
                    //    ⚠️ 운영 프로파일 이름을 늘릴 때는 이 줄을 함께 볼 것.
                    if (!environment.matchesProfiles("prod")) {
                        auth.requestMatchers(OPEN_API_PATHS).permitAll();
                    }
                    // ⚠️ matchesProfiles 가 이 버전에 없어 컴파일이 깨지면 아래로 바꿀 것(같은 뜻이다).
                    //    import org.springframework.core.env.Profiles;
                    //    if (!environment.acceptsProfiles(Profiles.of("prod"))) { ... }

                    // ── 그 외 전부 인증 필요 ────────────────────────────────────
                    auth.anyRequest().authenticated();
                })
```

⚠️ 기존 주석 블록(공개 경로 5줄에 달린 설명, `OPTIONS /**` 를 지운 이유)을 **지우지 말고 그대로 옮긴다.** 위 코드에서 생략한 것은 지면 때문이고, 실제 편집에서는 남겨야 한다.

- [ ] **Step 6: 테스트가 통과하는지 확인한다**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test --tests '*OpenApiExposureIntegrationTest*'
```

기대: **PASS** (3건).

- [ ] **Step 7: 전체 테스트를 돌려 회귀가 없는지 본다**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test
```

기대: **PASS.** 기존 206건 + 신규 3건.

⚠️ `NotFoundIntegrationTest` 가 깨지면 `OPEN_API_PATHS` 가 너무 넓게 매칭된 것이다
(예: `/v3/**` 처럼 적으면 없는 경로가 401 대신 404 가 되어 "존재 여부가 새지 않는다" 가 깨진다).
그때는 범위를 좁힐 것이고, **테스트를 고치지 않는다.**

- [ ] **Step 8: 커밋**

```bash
cd /Users/cheonjamin/projects/AllDap
git add api/build.gradle api/src/main/java/com/alldap/api/global/config/SecurityConfig.java \
        api/src/test/java/com/alldap/api/global/config/OpenApiExposureIntegrationTest.java
git commit -m "feat: springdoc 을 붙이고 API 문서를 운영이 아닐 때만 연다"
```

---

## Task 2: 운영 차단 두 겹을 실제 기동으로 확인한다

**Files:**
- Modify: `api/src/main/resources/application-prod.yaml`
- Test: `api/src/test/java/com/alldap/api/global/config/OpenApiProdExposureIntegrationTest.java` (신규)

**Interfaces:**
- Consumes: `SecurityConfig.OPEN_API_PATHS` (Task 1), `com.alldap.api.support.TestcontainersConfiguration` (기존)
- Produces: 없음 (다른 태스크가 참조하지 않는다)

**🔴 이 태스크는 Testcontainers 컨테이너를 하나 더 띄운다. 알고 하는 것이다.**
활성 프로파일이 다르면 스프링 테스트의 컨텍스트 캐시 키가 달라져 컨텍스트가 하나 더 생기고,
컨테이너는 그 컨텍스트의 빈이므로 PostgreSQL 도 하나 더 뜬다
(`TestcontainersConfiguration` 주석이 경계하는 바로 그 상황이다).
**그럼에도 하는 이유:** prod 프로파일의 동작은 prod 프로파일로 띄워보지 않으면 알 수 없다.
이 저장소는 "짜둔 검사가 아무 데서도 안 돌아" 오픈 리다이렉트가 뚫린 적이 있고,
배포 rate limit 점검 명령이 원리적으로 아무것도 못 잡는 상태로 방치된 적이 있다.
**대가는 테스트 1회당 컨테이너 하나(수 초)이고, 얻는 것은 운영 노출 여부의 실측이다.**

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`api/src/test/java/com/alldap/api/global/config/OpenApiProdExposureIntegrationTest.java` 를 새로 만든다.

```java
package com.alldap.api.global.config;

import com.alldap.api.support.TestcontainersConfiguration;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.context.TestConfiguration;
import org.springframework.boot.test.web.server.LocalServerPort;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Import;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.context.DynamicPropertyRegistrar;
import org.testcontainers.postgresql.PostgreSQLContainer;

import java.io.IOException;
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * <b>운영 프로파일(prod)로 실제 기동해</b> API 문서가 열리지 않는지 재는 통합 테스트.
 *
 * <h2>왜 두 번 부르는가: 두 겹을 <b>각각</b> 확인하기 위해서다</h2>
 * <ul>
 *   <li><b>토큰 없이</b> 부르면 401 이다 → 겹 ②({@code SecurityConfig} 가 permitAll 을 걸지 않았다)가 동작한다.</li>
 *   <li><b>토큰을 붙여</b> 부르면 404 다 → 겹 ①({@code springdoc.api-docs.enabled: false} 로 문서가
 *       아예 생성되지 않았다)이 동작한다. 여기서 200 이 나오면 <b>계정 하나만 있으면 읽힌다</b>는 뜻이다.</li>
 * </ul>
 * 한 번만 부르면 401 에 가려 겹 ① 이 살아 있는지 <b>알 수 없다</b>. 그래서 둘 다 부른다.
 *
 * <h2>⚠️ 이 테스트는 애플리케이션 컨텍스트를 하나 더 만든다 (그래서 컨테이너도 하나 더 뜬다)</h2>
 * 활성 프로파일은 컨텍스트 캐시 키의 일부라 {@code @IntegrationTest}(프로파일 없음)와 같은
 * 컨텍스트를 쓸 수 없다. 대가를 알고 감수한다. 근거는 이 테스트가 담당하는 계획 문서의 Task 2 참고.
 *
 * <h2>prod 프로파일이 요구하는 환경변수를 어떻게 채우는가</h2>
 * {@code application-prod.yaml} 은 fail-closed 라 기본값이 없다({@code ${DB_URL}} 처럼).
 * 플레이스홀더는 <b>환경 프로퍼티 이름으로</b> 해석되므로, 환경변수와 같은 이름의 프로퍼티를 주면 채워진다.
 * <ul>
 *   <li>DB 셋은 컨테이너가 떠야 값이 정해지므로 {@link ProdEnvRegistrar} 가 동적으로 넣는다.</li>
 *   <li>나머지는 {@code @SpringBootTest(properties = ...)} 로 고정값을 준다.</li>
 * </ul>
 *
 * <p>⚠️ {@code JWT_SECRET} 과 {@code BILLING_CRYPTO_KEY} 는 <b>저장소에 공개된 로컬 기본값과 달라야 한다.</b>
 * {@code JwtService} · {@code BillingCrypto} 가 "공개된 기본값으로 운영에 뜨는 것" 을 막는 가드를 갖고 있어,
 * 기본값 그대로면 prod 프로파일에서 <b>기동 자체가 실패한다</b>(그게 그 가드의 목적이다).
 * {@code BILLING_CRYPTO_KEY} 는 디코딩하면 정확히 32바이트인 Base64 여야 한다.
 */
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT, properties = {
        // 32자 이상이고 JwtService.KNOWN_LOCAL_DEFAULT_SECRET 과 다르다.
        "JWT_SECRET=prod-profile-test-secret-0123456789-abcdefghijklmn",
        // Base64("alldap-prod-profile-test-key-32b") = 디코딩 32바이트. BillingCrypto 의 기본값과 다르다.
        "BILLING_CRYPTO_KEY=YWxsZGFwLXByb2QtcHJvZmlsZS10ZXN0LWtleS0zMmI=",
        // 이 테스트는 Python 을 부르지 않는다. 값이 필요한 것은 <기동>이지 호출이 아니다.
        "AI_SERVICE_BASE_URL=http://localhost:1",
        "CORS_ALLOWED_ORIGINS=http://localhost:3000",
        "TOSS_SECRET_KEY=test_sk_prod_profile_only",
})
@ActiveProfiles("prod")
@Import({TestcontainersConfiguration.class, OpenApiProdExposureIntegrationTest.ProdEnvRegistrar.class})
@DisplayName("OpenAPI 문서 노출 (운영 프로파일)")
class OpenApiProdExposureIntegrationTest {

    @LocalServerPort
    int port;

    private final HttpClient http = HttpClient.newHttpClient();

    /**
     * prod 프로파일의 {@code ${DB_URL}} 등을 컨테이너 값으로 채운다.
     *
     * <p>{@code @ServiceConnection} 이 DataSource 배선을 이미 해주지만, {@code application-prod.yaml} 의
     * {@code spring.datasource.url: ${DB_URL}} 은 <b>바인딩 시점에 플레이스홀더가 해석돼야</b> 한다.
     * 해석되지 않으면 "Could not resolve placeholder 'DB_URL'" 로 컨텍스트가 뜨지 않는다.
     * 같은 컨테이너를 가리키므로 두 경로가 어긋날 일은 없다.
     */
    @TestConfiguration(proxyBeanMethods = false)
    static class ProdEnvRegistrar {

        @Bean
        DynamicPropertyRegistrar prodDbEnvRegistrar(PostgreSQLContainer container) {
            return registry -> {
                registry.add("DB_URL", container::getJdbcUrl);
                registry.add("DB_USERNAME", container::getUsername);
                registry.add("DB_PASSWORD", container::getPassword);
            };
        }
    }

    @Test
    @DisplayName("겹 ②: 토큰 없이 부르면 문서 경로가 401 이다 (permitAll 이 걸리지 않았다)")
    void 운영에서는_문서_경로가_인증_뒤에_있다() {
        assertThat(get("/v3/api-docs", null).statusCode()).isEqualTo(401);
        assertThat(get("/swagger-ui/index.html", null).statusCode()).isEqualTo(401);
    }

    @Test
    @DisplayName("겹 ①: 로그인한 사용자가 불러도 문서가 없다 (생성 자체가 꺼져 있다)")
    void 운영에서는_문서가_생성되지_않는다() {
        String token = 가입해서_토큰을_받는다();

        // 여기서 200 이 나오면 겹 ① 이 죽은 것이다 = 계정 하나만 있으면 전 API 명세가 읽힌다.
        assertThat(get("/v3/api-docs", token).statusCode()).isEqualTo(404);
        assertThat(get("/swagger-ui/index.html", token).statusCode()).isEqualTo(404);
    }

    /**
     * 가입은 prod 에서도 permitAll 이다(가입을 막으면 서비스가 성립하지 않는다).
     * JSON 파싱을 붙이지 않고 문자열에서 토큰만 꺼낸다 - 이 테스트가 검증하려는 것이 토큰 구조가 아니다.
     */
    private String 가입해서_토큰을_받는다() {
        String 본문 = """
                {"email":"prod-doc-tester@example.com","password":"correct-password-1234","name":"테스터"}""";
        HttpResponse<String> response = post("/api/auth/signup", 본문);
        assertThat(response.statusCode())
                .withFailMessage("가입이 실패하면 겹 ① 을 잴 수 없다. 응답: %s", response.body())
                .isEqualTo(201);

        String 시작표시 = "\"token\":\"";
        int 시작 = response.body().indexOf(시작표시) + 시작표시.length();
        return response.body().substring(시작, response.body().indexOf('"', 시작));
    }

    /** @param bearerToken null 이면 Authorization 헤더를 붙이지 않는다(= 비로그인 요청) */
    private HttpResponse<String> get(String path, String bearerToken) {
        HttpRequest.Builder builder = HttpRequest.newBuilder()
                .uri(URI.create("http://localhost:" + port + path))
                .GET();
        if (bearerToken != null) {
            builder.header("Authorization", "Bearer " + bearerToken);
        }
        return send(builder.build(), path);
    }

    private HttpResponse<String> post(String path, String jsonBody) {
        HttpRequest request = HttpRequest.newBuilder()
                .uri(URI.create("http://localhost:" + port + path))
                .header("Content-Type", "application/json")
                .POST(HttpRequest.BodyPublishers.ofString(jsonBody))
                .build();
        return send(request, path);
    }

    private HttpResponse<String> send(HttpRequest request, String path) {
        try {
            return http.send(request, HttpResponse.BodyHandlers.ofString());
        } catch (IOException e) {
            throw new AssertionError("요청이 실패했다: " + path, e);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new AssertionError("요청이 중단됐다: " + path, e);
        }
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test --tests '*OpenApiProdExposureIntegrationTest*'
```

기대: **`운영에서는_문서가_생성되지_않는다` 가 FAIL** (`expected: 404 but was: 200`).
아직 `application-prod.yaml` 에 스위치가 없어 문서가 생성되기 때문이다.
`운영에서는_문서_경로가_인증_뒤에_있다` 는 **이미 통과한다** (Task 1 의 분기가 prod 에서 permitAll 을 걸지 않으므로).
즉 이 단계에서 겹 ② 가 먼저 확인되고, 겹 ① 을 이제 만든다.

⚠️ 여기서 **기동 자체가 실패하면** 원인은 문서가 아니라 prod 프로파일의 필수값이다.
로그에서 `Could not resolve placeholder` 또는 `IllegalStateException` 을 찾을 것.
플레이스홀더면 `properties` 에 그 이름을 추가하고, `IllegalStateException` 이면
공개된 로컬 기본값을 그대로 쓴 것이다(위 javadoc 참고).

- [ ] **Step 3: `application-prod.yaml` 에 스위치를 넣는다**

파일 맨 아래 `logging:` 블록 **바로 위**에 넣는다 (`app:` 블록 다음).

```yaml
# 🔴 운영에서는 API 문서를 내보내지 않는다. 이것이 노출 방어 두 겹 중 <①> 이다.
#    ② 는 SecurityConfig 가 문서 경로에 permitAll 을 걸지 않는 것(프로파일 분기).
#    한 겹만 두면 우회 경로가 생겨 규칙이 없는 것과 같아진다:
#      · ① 만 두면 나중에 누가 이 설정을 되돌리는 순간 전부 열린다.
#      · ② 만 두면 문서는 계속 생성되면서 401 뒤에 숨어 있을 뿐이라 계정 하나만 있으면 읽힌다.
#    두 겹이 각각 동작하는 것은 OpenApiProdExposureIntegrationTest 가 CI 에서 확인한다
#    (토큰 없이 401 = ②, 토큰 붙여 404 = ①).
#
# ⚠️ 문서는 로컬에서만 본다. 전시가 목적인데 운영에서 못 보는 것은 목적과 일부 상충하지만,
#    노출 판단을 나중에 되돌리는 쪽이 그 반대보다 안전하다.
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
```

- [ ] **Step 4: 통과를 확인한다**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test --tests '*OpenApiProdExposureIntegrationTest*'
```

기대: **PASS** (2건).

⚠️ `/swagger-ui/index.html` 이 404 가 아니라 200 으로 나오면 **진짜 문제다**
(webjar 가 springdoc 과 무관하게 서빙되고 있다는 뜻). 그때는 테스트를 고치지 말고
`SecurityConfig` 에 prod 전용 거부 규칙(`denyAll`)을 추가하고, 이 계획 문서에 그 사실을 기록할 것.

- [ ] **Step 5: 전체 테스트**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test
```

기대: **PASS.** 로그에 PostgreSQL 컨테이너가 **2개** 뜨는 것이 정상이다(위 경고 참고).

- [ ] **Step 6: 커밋**

```bash
cd /Users/cheonjamin/projects/AllDap
git add api/src/main/resources/application-prod.yaml \
        api/src/test/java/com/alldap/api/global/config/OpenApiProdExposureIntegrationTest.java
git commit -m "feat: 운영에서 API 문서 생성을 끄고, 두 겹이 각각 막는지 실측한다"
```

---

## Task 3: 공통 설정 (`OpenApiConfig`) + 공개 엔드포인트 4개의 JWT 요구 해제

**Files:**
- Create: `api/src/main/java/com/alldap/api/global/config/OpenApiConfig.java`
- Modify: `api/src/main/java/com/alldap/api/domain/auth/controller/AuthController.java` (메서드 2개)
- Modify: `api/src/main/java/com/alldap/api/domain/widget/controller/WidgetController.java` (메서드 2개)
- Test: `api/src/test/java/com/alldap/api/global/config/OpenApiDocumentIntegrationTest.java` (신규)

**Interfaces:**
- Consumes: `SecurityConfig.OPEN_API_PATHS` 는 쓰지 않는다. `ErrorCode.INTERNAL_ERROR` · `ErrorCode.AUTHENTICATION_REQUIRED`(기존 enum).
- Produces:
  - `OpenApiConfig.BEARER_SCHEME` : `public static final String` = `"bearerAuth"`. PR 2~4 가 개별 보안 요구를 적을 때 이 상수를 쓴다.
  - `OpenApiConfig.ERROR_SCHEMA_NAME` : `public static final String` = `"ErrorResponse"`. PR 2~4 가 엔드포인트별 `@ApiResponse` 의 스키마를 참조할 때 쓴다.
  - **규약:** 공개(토큰 불필요) 엔드포인트에는 `@io.swagger.v3.oas.annotations.security.SecurityRequirements` 를 붙인다. 이 애노테이션의 **유무가 공통 401 을 붙일지 판정하는 유일한 신호다.** PR 2~4 에서 새 공개 엔드포인트를 만들면 반드시 붙일 것.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`api/src/test/java/com/alldap/api/global/config/OpenApiDocumentIntegrationTest.java` 를 새로 만든다.

```java
package com.alldap.api.global.config;

import com.alldap.api.global.exception.ErrorCode;
import com.alldap.api.support.IntegrationTest;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.web.server.LocalServerPort;
import tools.jackson.databind.JsonNode;
import tools.jackson.databind.ObjectMapper;

import java.io.IOException;
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * 생성된 OpenAPI 문서의 <b>내용</b>을 재는 통합 테스트.
 * (열리는지 여부는 {@code OpenApiExposureIntegrationTest} 가 잰다)
 *
 * <h2>왜 JSON 을 직접 파싱하는가</h2>
 * {@code OpenApiConfig} 의 빈을 단위 테스트로 검사하면 "우리가 만든 모델" 만 보게 된다.
 * 정작 알고 싶은 것은 springdoc 이 그 모델과 컨트롤러 애노테이션을 합쳐 <b>실제로 내보낸 결과</b>다.
 * 두 Jackson(Boot 4 의 Jackson 3 / swagger-core 의 Jackson 2)이 한 클래스패스에 있는 상황이라
 * 더욱 결과물을 봐야 한다.
 *
 * <h2>401 을 전부에 달지 않는 것이 검증 대상이다</h2>
 * 가입과 위젯은 토큰을 받지 않으므로 401 이 나갈 수 없다. 일괄로 달면 문서가 <b>없는 응답을
 * 있다고 말한다.</b> 그래서 "보호 경로에는 있고, 공개 경로에는 없다" 를 양쪽에서 확인한다.
 */
@IntegrationTest
@DisplayName("OpenAPI 문서 내용")
class OpenApiDocumentIntegrationTest {

    private static final ObjectMapper JSON = new ObjectMapper();

    @LocalServerPort
    int port;

    private JsonNode 문서;

    @BeforeEach
    void 문서를_한_번_읽는다() {
        문서 = JSON.readTree(get("/v3/api-docs"));
    }

    @Test
    @DisplayName("JWT 보안 스킴이 정의돼 있고 전역 기본으로 걸려 있다")
    void jwt_스킴이_전역으로_걸린다() {
        JsonNode 스킴 = 문서.path("components").path("securitySchemes").path("bearerAuth");
        assertThat(스킴.path("type").asString()).isEqualTo("http");
        assertThat(스킴.path("scheme").asString()).isEqualTo("bearer");
        assertThat(스킴.path("bearerFormat").asString()).isEqualTo("JWT");

        // 전역으로 걸려 있어야 Swagger UI 의 Authorize 에 토큰을 한 번 넣고 전부 시험할 수 있다.
        assertThat(문서.path("security").toString()).contains("bearerAuth");
    }

    @Test
    @DisplayName("공통 에러 스키마가 중첩 구조로 정의돼 있다")
    void 에러_스키마가_중첩_구조다() {
        JsonNode 스키마 = 문서.path("components").path("schemas").path("ErrorResponse");
        // 평평하게 만들면 프론트(ApiErrorBody)·Python·위젯이 모두 어긋난다.
        JsonNode 내부 = 스키마.path("properties").path("error").path("properties");
        assertThat(내부.has("code")).isTrue();
        assertThat(내부.has("message")).isTrue();
    }

    @Test
    @DisplayName("보호 엔드포인트에는 공통 401 과 500 이 붙는다")
    void 보호_엔드포인트에_공통_응답이_붙는다() {
        JsonNode 응답 = 문서.path("paths").path("/api/bots").path("get").path("responses");

        assertThat(응답.has("401")).isTrue();
        assertThat(응답.has("500")).isTrue();
        assertThat(응답.path("401").path("description").asString())
                .contains(ErrorCode.AUTHENTICATION_REQUIRED.getCode());
        assertThat(응답.path("500").path("description").asString())
                .contains(ErrorCode.INTERNAL_ERROR.getCode());
    }

    @Test
    @DisplayName("공개 엔드포인트에는 401 이 붙지 않는다 (500 만 붙는다)")
    void 공개_엔드포인트에는_401이_없다() {
        // 가입은 토큰을 받지 않으므로 401 이 나갈 수 없다. 붙으면 문서가 거짓말을 한다.
        JsonNode 가입 = 문서.path("paths").path("/api/auth/signup").path("post");
        assertThat(가입.path("responses").has("401")).isFalse();
        assertThat(가입.path("responses").has("500")).isTrue();
        // 전역 JWT 요구를 개별 해제했으므로 빈 배열이어야 한다.
        assertThat(가입.path("security").isArray()).isTrue();
        assertThat(가입.path("security").isEmpty()).isTrue();

        JsonNode 위젯채팅 = 문서.path("paths").path("/api/w/{publicKey}/chat").path("post");
        assertThat(위젯채팅.path("responses").has("401")).isFalse();
        assertThat(위젯채팅.path("security").isEmpty()).isTrue();
    }

    @Test
    @DisplayName("문서 제목과 설명에 아키텍처 한 줄이 들어 있다")
    void 설명에_아키텍처가_들어_있다() {
        assertThat(문서.path("info").path("title").asString()).isEqualTo("AllDap API");
        // 문서를 여는 사람이 가장 먼저 알아야 할 사실이다: 외부에 열린 API 는 Spring 뿐이다.
        assertThat(문서.path("info").path("description").asString()).contains("내부망");
    }

    private String get(String path) {
        try {
            HttpRequest request = HttpRequest.newBuilder()
                    .uri(URI.create("http://localhost:" + port + path))
                    .GET()
                    .build();
            return HttpClient.newHttpClient()
                    .send(request, HttpResponse.BodyHandlers.ofString())
                    .body();
        } catch (IOException e) {
            throw new AssertionError("요청이 실패했다: " + path, e);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new AssertionError("요청이 중단됐다: " + path, e);
        }
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test --tests '*OpenApiDocumentIntegrationTest*'
```

기대: **FAIL** (5건 중 4건 이상). `bearerAuth` 도 `ErrorResponse` 스키마도 공통 응답도 아직 없다.

- [ ] **Step 3: `OpenApiConfig` 를 만든다**

`api/src/main/java/com/alldap/api/global/config/OpenApiConfig.java`

```java
package com.alldap.api.global.config;

import com.alldap.api.global.exception.ErrorCode;
import io.swagger.v3.oas.annotations.security.SecurityRequirements;
import io.swagger.v3.oas.models.Components;
import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.media.Content;
import io.swagger.v3.oas.models.media.MediaType;
import io.swagger.v3.oas.models.media.ObjectSchema;
import io.swagger.v3.oas.models.media.Schema;
import io.swagger.v3.oas.models.media.StringSchema;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.responses.ApiResponse;
import io.swagger.v3.oas.models.responses.ApiResponses;
import io.swagger.v3.oas.models.security.SecurityRequirement;
import io.swagger.v3.oas.models.security.SecurityScheme;
import org.springdoc.core.customizers.OperationCustomizer;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * OpenAPI 공통 설정. 컨트롤러 33곳에 반복될 것을 여기 한 곳에 모은다.
 *
 * <h2>여기서 정하는 것 세 가지</h2>
 * <ol>
 *   <li><b>Info</b> - 문서를 여는 사람이 가장 먼저 알아야 할 사실(외부에 열린 API 는 Spring 뿐이다)을 설명에 넣는다.</li>
 *   <li><b>JWT 보안 스킴을 전역 기본으로</b> - Swagger UI 의 Authorize 에 토큰을 한 번 넣으면 전부 시험할 수 있다.
 *       인증이 없는 엔드포인트는 {@code @SecurityRequirements} 로 개별 해제한다.</li>
 *   <li><b>공통 에러 응답</b> - {@code ErrorCode} 가 36개인데 엔드포인트마다 나열하면 문서를 못 읽는다.
 *       모든 엔드포인트에 참인 것만 일괄로 붙이고, 나머지는 엔드포인트별로 단다.</li>
 * </ol>
 *
 * <h2>🔴 401 을 <b>전부에</b> 달지 않는다</h2>
 * 401 은 모든 엔드포인트에 공통이 아니다. {@code /api/auth/signup} 은 401 을 낼 수 없고,
 * 위젯 API 는 인증이 없어 401 이 나갈 일이 없다. 일괄로 달면 <b>문서가 없는 응답을 있다고 말한다.</b>
 * 그래서 500 만 전부에 달고, 401 은 "전역 JWT 요구를 해제하지 않은 엔드포인트" 에만 단다.
 * 판정 신호는 {@code @SecurityRequirements} 의 유무 하나다 - 보안 요구와 401 표기가 <b>자동으로 맞는다.</b>
 *
 * <p>⚠️ 로그인의 401({@code INVALID_CREDENTIALS})은 "토큰이 없어서" 가 아니라 "비밀번호가 틀려서" 다.
 * 별개 사실이므로 여기서 다루지 않고 엔드포인트별로 단다.
 *
 * <h2>🔴 에러 스키마를 클래스 참조가 아니라 손으로 정의하는 이유</h2>
 * {@code @Schema(implementation = ErrorResponse.class)} 는 springdoc 이 타입을 뒤져 스키마를 만들게 한다.
 * Boot 4 는 Jackson 3({@code tools.jackson})을 쓰는데 swagger-core 2.2.29 는 Jackson 2 를 쓴다.
 * 두 Jackson 이 한 클래스패스에 있는 상태에서 모델 introspection 에 의존하지 않는 쪽이 안전하고,
 * 필드가 {@code code}·{@code message} 둘뿐이라 손으로 적는 비용이 거의 없다. 결과가 결정적이다.
 */
@Configuration
public class OpenApiConfig {

    /** 보안 스킴 이름. 엔드포인트별 보안 표기를 적을 때 이 상수를 쓴다(문자열을 다시 적지 않는다). */
    public static final String BEARER_SCHEME = "bearerAuth";

    /** 공통 에러 응답 스키마 이름. 엔드포인트별 {@code @ApiResponse} 가 이 이름을 참조한다. */
    public static final String ERROR_SCHEMA_NAME = "ErrorResponse";

    private static final String ERROR_SCHEMA_REF = "#/components/schemas/" + ERROR_SCHEMA_NAME;

    @Bean
    public OpenAPI alldapOpenApi() {
        return new OpenAPI()
                .info(new Info()
                        .title("AllDap API")
                        .version("v0.4")
                        .description("""
                                문서를 올리면 출처가 표시되는 한국어 RAG 챗봇 서비스의 API.

                                외부에 노출되는 API 는 이 Spring 서비스뿐입니다. 파싱·청킹·임베딩·검색·생성·평가를 \
                                담당하는 Python AI 서비스(:8001)는 내부망 전용이고 인증이 없습니다. \
                                브라우저에서 직접 호출하지 마세요.

                                에러 응답은 전 계층 공통으로 {"error":{"code","message"}} 모양이고, \
                                message 는 "무엇을 어떻게 하면 되는지" 까지 담은 한국어 문장입니다.

                                이 문서는 로컬에서만 열립니다(운영에서는 생성 자체를 끕니다).
                                """))
                .components(new Components()
                        .addSchemas(ERROR_SCHEMA_NAME, 공통_에러_스키마())
                        .addSecuritySchemes(BEARER_SCHEME, new SecurityScheme()
                                .type(SecurityScheme.Type.HTTP)
                                .scheme("bearer")
                                .bearerFormat("JWT")
                                .description("""
                                        로그인(POST /api/auth/login) 응답의 token 값을 그대로 넣으세요. \
                                        가입·로그인과 위젯 API(/api/w/**)는 토큰이 필요 없습니다.""")))
                // 전역 기본. 인증이 없는 엔드포인트는 @SecurityRequirements 로 개별 해제한다.
                .addSecurityItem(new SecurityRequirement().addList(BEARER_SCHEME));
    }

    /**
     * 모든 엔드포인트에 <b>참인</b> 응답만 붙인다.
     *
     * <p>{@code OperationCustomizer} 는 springdoc 이 엔드포인트 하나를 모델로 만든 <b>뒤</b> 불러주는 훅이다.
     * 이미 적힌 응답이 있으면 덮지 않는다({@code containsKey} 검사) - 엔드포인트별 설명이 공통 문구보다 정확하다.
     */
    @Bean
    public OperationCustomizer 공통_에러_응답() {
        return (operation, handlerMethod) -> {
            ApiResponses responses = operation.getResponses();

            // 500 은 어디서든 날 수 있다(GlobalExceptionHandler 의 마지막 그물).
            응답을_없을_때만_붙인다(responses, "500", ErrorCode.INTERNAL_ERROR);

            // 401 은 토큰을 받는 엔드포인트에만 있다. 판정 신호는 개별 해제 애노테이션의 유무다.
            boolean 토큰을_받는다 = handlerMethod.getMethodAnnotation(SecurityRequirements.class) == null;
            if (토큰을_받는다) {
                응답을_없을_때만_붙인다(responses, "401", ErrorCode.AUTHENTICATION_REQUIRED);
            }

            return operation;
        };
    }

    private static void 응답을_없을_때만_붙인다(ApiResponses responses, String status, ErrorCode errorCode) {
        if (responses.containsKey(status)) {
            return;
        }
        responses.addApiResponse(status, new ApiResponse()
                .description(errorCode.getCode() + " : " + errorCode.getMessage())
                .content(new Content().addMediaType("application/json", new MediaType()
                        .schema(new Schema<>().$ref(ERROR_SCHEMA_REF))
                        .example("""
                                {"error":{"code":"%s","message":"%s"}}"""
                                .formatted(errorCode.getCode(), errorCode.getMessage())))));
    }

    /**
     * 전 계층 공통 에러 응답. <b>중첩 구조가 핵심이다</b> -
     * 평평하게 만들면 프론트({@code web/lib/types.ts} 의 {@code ApiErrorBody})·Python·위젯이 모두 어긋난다.
     */
    private static Schema<?> 공통_에러_스키마() {
        Schema<?> 내부 = new ObjectSchema()
                .addProperty("code", new StringSchema()
                        .description("기계가 분기할 식별자(영문 대문자 스네이크). 문구가 바뀌어도 이 값은 유지된다.")
                        .example(ErrorCode.BOT_NOT_FOUND.getCode()))
                .addProperty("message", new StringSchema()
                        .description("사용자에게 그대로 보여줄 한국어 문장. 무엇을 어떻게 하면 되는지까지 담는다.")
                        .example(ErrorCode.BOT_NOT_FOUND.getMessage()));

        return new ObjectSchema()
                .description("전 계층(Spring·Python·Next.js) 공통 에러 응답")
                .addProperty("error", 내부);
    }
}
```

- [ ] **Step 4: 공개 엔드포인트 4개에서 전역 JWT 요구를 해제한다**

`AuthController` 에 import 를 추가하고 두 메서드에 애노테이션을 붙인다.

```java
import io.swagger.v3.oas.annotations.security.SecurityRequirements;
```

```java
    // 전역 JWT 요구를 해제한다. 이 애노테이션의 <유무>가 공통 401 을 붙일지 판정하는 신호이기도 하다
    // (OpenApiConfig 참고) - 보안 표기와 401 표기가 자동으로 맞는다.
    @SecurityRequirements
    @PostMapping("/signup")
    public ResponseEntity<AuthResponse> signup(@Valid @RequestBody SignupRequest request) {
```

```java
    // 전역 JWT 요구를 해제한다. 로그인 자체가 토큰을 <발급받는> 요청이다.
    // ⚠️ 이 경로의 401(INVALID_CREDENTIALS)은 "토큰이 없어서" 가 아니라 "비밀번호가 틀려서" 다.
    //    별개 사실이므로 공통 401 이 아니라 엔드포인트별 @ApiResponse 로 단다(PR 2).
    @SecurityRequirements
    @PostMapping("/login")
```

`WidgetController` 에 같은 import 를 추가하고 두 메서드에 붙인다.

```java
    // 위젯은 고객 사이트에 심겨 <방문자>가 쓴다. JWT 가 없다.
    // 보호는 publicKey + Origin 검증 + rate limit 이다(이 클래스 주석 참고).
    @SecurityRequirements
    @GetMapping("/config")
```

```java
    // 위젯 채팅. 인증 없이 열린 유일한 문이고 실질 방어선은 rate limit 이다.
    @SecurityRequirements
    @PostMapping("/chat")
```

- [ ] **Step 5: 통과를 확인한다**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test --tests '*OpenApiDocumentIntegrationTest*'
```

기대: **PASS** (5건).

- [ ] **Step 6: 전체 테스트**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test
```

기대: **PASS.**

- [ ] **Step 7: 커밋**

```bash
cd /Users/cheonjamin/projects/AllDap
git add api/src/main/java/com/alldap/api/global/config/OpenApiConfig.java \
        api/src/main/java/com/alldap/api/domain/auth/controller/AuthController.java \
        api/src/main/java/com/alldap/api/domain/widget/controller/WidgetController.java \
        api/src/test/java/com/alldap/api/global/config/OpenApiDocumentIntegrationTest.java
git commit -m "feat: OpenAPI 공통 설정을 넣고 401 을 토큰 받는 경로에만 붙인다"
```

---

## Task 4: 커버리지 테스트 + `@Tag` 11개 + `@Operation(summary)` 33개

**Files:**
- Test: `api/src/test/java/com/alldap/api/global/config/OpenApiCoverageTest.java` (신규)
- Modify: 컨트롤러 11개 전부

**Interfaces:**
- Consumes: 없음 (애노테이션만 다룬다)
- Produces: 없음

**왜 `summary` 33개가 이 PR 에 전부 들어가는가:** 커버리지 테스트가 **첫 PR 부터 초록이어야** 한다.
나중에 넣으면 그때까지 그 테스트가 없거나 빨간 채로 방치되고, 그것이 정확히 이 저장소가 두 번 겪은
"짜둔 검사가 아무 데서도 안 도는" 경로다.

- [ ] **Step 1: 커버리지 테스트를 쓴다**

`api/src/test/java/com/alldap/api/global/config/OpenApiCoverageTest.java`

```java
package com.alldap.api.global.config;

import com.alldap.api.support.IntegrationTest;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.web.method.HandlerMethod;
import org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping;

import java.util.ArrayList;
import java.util.LinkedHashSet;
import java.util.List;
import java.util.Set;
import java.util.stream.Collectors;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * <b>설명이 빠진 엔드포인트가 없는지</b> 재는 테스트.
 *
 * <h2>효과는 지금이 아니라 나중에 난다</h2>
 * 34번째 엔드포인트를 추가하면서 설명을 빠뜨려도 컴파일은 되고, 기존 테스트도 초록이고,
 * Swagger 화면도 뜬다. <b>그 하나만 조용히 껍데기로 남는다.</b> 이 테스트가 그 순간 CI 에서 막는다.
 *
 * <h2>🔴 한계를 분명히 한다: 이 테스트는 <b>빈칸만</b> 본다</h2>
 * {@code summary} 가 맞는 말인지는 검사하지 못한다.
 * {@code summary = "봇 목록 조회"} 라고 써놓고 실제로는 봇을 삭제하는 코드여도 통과한다.
 * 이 저장소가 반복해서 낸 "기능은 고쳤는데 그것을 설명하는 자리를 안 고쳤다" 를
 * <b>완전히 막지는 못한다.</b> 빠뜨린 것만 잡는다.
 *
 * <h2>왜 빠진 것을 <b>전부</b> 나열하는가</h2>
 * 하나만 찍으면 고칠 때마다 테스트를 다시 돌려 다음 하나를 찾아야 한다.
 * 33개를 한 번에 붙이는 작업에서는 그 왕복이 그대로 낭비다.
 *
 * <h2>{@code @Qualifier} 가 필요한 이유</h2>
 * management 포트(8081) 분리 때문에 자식 컨텍스트가 생기는데, 우리가 재려는 것은
 * <b>서비스 포트의</b> 핸들러 매핑이다. 이름을 못박아 엉뚱한 매핑을 재지 않게 한다.
 */
@IntegrationTest
@DisplayName("OpenAPI 문서화 커버리지")
class OpenApiCoverageTest {

    /** 우리가 쓴 컨트롤러만 본다. actuator·에러 핸들러·springdoc 자신의 매핑은 대상이 아니다. */
    private static final String 우리_컨트롤러_패키지 = "com.alldap.api.domain";

    @Autowired
    @Qualifier("requestMappingHandlerMapping")
    private RequestMappingHandlerMapping mapping;

    @Test
    @DisplayName("모든 엔드포인트에 @Operation(summary) 가 있다")
    void 모든_엔드포인트에_summary_가_있다() {
        List<String> 빠진것 = new ArrayList<>();

        mapping.getHandlerMethods().forEach((info, handler) -> {
            if (!우리_컨트롤러인가(handler)) {
                return;
            }
            Operation operation = handler.getMethodAnnotation(Operation.class);
            if (operation == null || operation.summary().isBlank()) {
                빠진것.add(설명(info.toString(), handler));
            }
        });

        assertThat(빠진것)
                .withFailMessage("""

                        @Operation(summary = "...") 가 없는 엔드포인트가 %d 개 있다.
                        Swagger 화면에 경로만 뜨고 설명이 비어 껍데기로 남는다. 아래 전부에 붙일 것:
                        %s
                        """, 빠진것.size(), 줄로_묶는다(빠진것))
                .isEmpty();
    }

    @Test
    @DisplayName("모든 컨트롤러에 @Tag 가 있다")
    void 모든_컨트롤러에_tag_가_있다() {
        // @Tag 가 없으면 Swagger 화면에서 33개가 "bot-controller" 같은 기계 이름으로 묶인다.
        // 문서를 여는 사람이 무엇부터 봐야 하는지 알 수 없게 된다.
        Set<String> 빠진것 = new LinkedHashSet<>();

        mapping.getHandlerMethods().forEach((info, handler) -> {
            if (!우리_컨트롤러인가(handler)) {
                return;
            }
            Tag tag = handler.getBeanType().getAnnotation(Tag.class);
            if (tag == null || tag.name().isBlank()) {
                빠진것.add(handler.getBeanType().getName());
            }
        });

        assertThat(빠진것)
                .withFailMessage("""

                        @Tag(name = "...") 가 없는 컨트롤러가 %d 개 있다:
                        %s
                        """, 빠진것.size(), 줄로_묶는다(List.copyOf(빠진것)))
                .isEmpty();
    }

    private static boolean 우리_컨트롤러인가(HandlerMethod handler) {
        return handler.getBeanType().getPackageName().startsWith(우리_컨트롤러_패키지);
    }

    /**
     * {@code RequestMappingInfo.toString()} 을 그대로 쓴다. HTTP 메서드와 경로 패턴이 이미 들어 있어
     * 조건을 하나씩 꺼내 조립하는 것보다 짧고, Spring 버전에 따라 바뀌는 API 에 덜 의존한다.
     */
    private static String 설명(String mappingInfo, HandlerMethod handler) {
        return "  · %s  →  %s#%s".formatted(
                mappingInfo, handler.getBeanType().getSimpleName(), handler.getMethod().getName());
    }

    private static String 줄로_묶는다(List<String> 줄들) {
        return 줄들.stream().sorted().collect(Collectors.joining("\n"));
    }
}
```

- [ ] **Step 2: 실패를 확인한다 (빠진 33개가 전부 나열되는지)**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test --tests '*OpenApiCoverageTest*'
```

기대: **FAIL 2건.** 실패 메시지에 **33개 행**과 **컨트롤러 11개**가 나열된다.
⚠️ 33 · 11 이 아니면 필터가 잘못된 것이다. 목록에 actuator 나 springdoc 경로가 섞였는지 볼 것.
**여기서 나온 목록이 다음 단계의 체크리스트다.**

- [ ] **Step 3: `AuthController` (2개)**

import 를 추가한다 (이후 모든 컨트롤러에 같은 두 줄이 필요하다).

```java
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
```

```java
@Tag(name = "인증", description = "가입·로그인. 토큰 없이 호출되는 공개 경로다.")
@RestController
@RequestMapping("/api/auth")
```

```java
    @Operation(summary = "가입")
    @SecurityRequirements
    @PostMapping("/signup")
```

```java
    @Operation(summary = "로그인")
    @SecurityRequirements
    @PostMapping("/login")
```

- [ ] **Step 4: `BotController` (5개)**

```java
@Tag(name = "봇", description = "챗봇 생성과 설정. 모든 조회는 소유자 본인의 봇으로 격리된다.")
```

| 메서드 | `@Operation(summary = ...)` |
|---|---|
| `getMyBots` | `"내 봇 목록 조회"` |
| `createBot` | `"봇 생성"` |
| `getBot` | `"봇 단건 조회"` |
| `updateBot` | `"봇 설정 수정"` |
| `deleteBot` | `"봇 삭제"` |

- [ ] **Step 5: `DocumentController` (3개)**

```java
@Tag(name = "문서", description = "봇의 지식 원본. 업로드는 비동기로 처리된다.")
```

| 메서드 | summary |
|---|---|
| `uploadDocument` | `"문서 업로드 (비동기 처리)"` |
| `getDocuments` | `"문서 목록 조회"` |
| `deleteDocument` | `"문서 삭제"` |

- [ ] **Step 6: `ChatController` (2개)**

```java
@Tag(name = "채팅", description = "관리자 테스트 채팅과 답변 피드백.")
```

| 메서드 | summary |
|---|---|
| `chat` | `"관리자 테스트 채팅"` |
| `feedback` | `"답변 피드백 등록"` |

- [ ] **Step 7: `ConversationLogController` (2개)**

```java
@Tag(name = "대화 로그", description = "위젯·관리자 채팅에서 오간 대화 기록.")
```

| 메서드 | summary |
|---|---|
| `getLogs` | `"대화 목록 조회"` |
| `getMessages` | `"대화 상세(메시지 목록) 조회"` |

- [ ] **Step 8: `ConflictController` (3개)**

```java
@Tag(name = "문서 충돌", description = "서로 다른 값을 말하는 문서 쌍을 찾아 알려준다.")
```

| 메서드 | summary |
|---|---|
| `getConflicts` | `"충돌 목록 조회"` |
| `scan` | `"충돌 진단 실행"` |
| `updateStatus` | `"충돌 처리 상태 변경"` |

- [ ] **Step 9: `EvalController` (7개)**

```java
@Tag(name = "품질 평가", description = "테스트 질문을 자동 생성해 답변 품질을 채점한다. 이 제품의 핵심 기능이다.")
```

| 메서드 | summary |
|---|---|
| `getQuestions` | `"테스트 질문 목록 조회"` |
| `generateQuestions` | `"테스트 질문 자동 생성"` |
| `startRun` | `"평가 실행 시작 (비동기)"` |
| `getRuns` | `"평가 실행 이력 조회"` |
| `getResults` | `"평가 실행의 질문별 채점 결과 조회"` |
| `updateQuestion` | `"테스트 질문 수정"` |
| `getUnanswered` | `"미답변 질문 집계 조회"` |

- [ ] **Step 10: `UsageController` (1개)**

```java
@Tag(name = "사용량", description = "과금 원장(usage_events) 집계. 아직 실제로 청구하지는 않는다.")
```

| 메서드 | summary |
|---|---|
| `getUsage` | `"사용량 집계 조회"` |

- [ ] **Step 11: `BillingController` (4개)**

```java
@Tag(name = "결제 수단", description = "토스 빌링키 기반 카드 등록. 계정당 최대 5장이고 기본 카드는 1장이다.")
```

| 메서드 | summary |
|---|---|
| `list` | `"등록된 카드 목록 조회"` |
| `register` | `"카드 등록 (토스 빌링키 발급)"` |
| `delete` | `"카드 삭제"` |
| `setDefault` | `"기본 카드 지정"` |

- [ ] **Step 12: `PlanController` (2개)**

```java
@Tag(name = "요금제", description = "무료·Pro 선택. 무엇을 골랐는지만 기억하고 청구는 하지 않는다.")
```

| 메서드 | summary |
|---|---|
| `get` | `"현재 요금제 조회"` |
| `change` | `"요금제 변경"` |

- [ ] **Step 13: `WidgetController` (2개)**

```java
@Tag(name = "위젯 공개 API", description = "고객 사이트에 심긴 위젯이 부른다. 인증 없이 열린 유일한 문이다.")
```

| 메서드 | summary |
|---|---|
| `getConfig` | `"위젯 설정 조회 (Origin 검증)"` |
| `chat` | `"위젯 채팅 (인증 없음, 분당 요청 제한)"` |

- [ ] **Step 14: 커버리지 테스트가 통과하는지 확인한다**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test --tests '*OpenApiCoverageTest*'
```

기대: **PASS** (2건). 아직 실패하면 메시지에 남은 것이 그대로 나열된다.

- [ ] **Step 15: 전체 테스트**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew test
```

기대: **PASS.**

- [ ] **Step 16: 커밋**

```bash
cd /Users/cheonjamin/projects/AllDap
git add api/src/main/java/com/alldap/api/domain api/src/test/java/com/alldap/api/global/config/OpenApiCoverageTest.java
git commit -m "feat: 전 엔드포인트 33개에 summary 를 달고 빠진 것을 CI 가 막게 한다"
```

---

## Task 5: 브라우저로 눈으로 확인하고, 문서를 갱신하고, PR 을 올린다

**Files:**
- Modify: `AGENTS.md` (W2 세부 상태 표에 한 행)
- Modify: `docs/decisions.md` (항목 2개)

**Interfaces:**
- Consumes: Task 1~4 의 결과물
- Produces: 없음

- [ ] **Step 1: 로컬에서 실제로 띄워 눈으로 본다**

```bash
cd /Users/cheonjamin/projects/AllDap
docker compose up -d
cd api && ./gradlew bootRun
```

브라우저에서 `http://localhost:8080/swagger-ui/index.html` 을 연다.
확인할 것 네 가지:

1. 태그 11개가 한국어로 묶여 있다(기계 이름 `bot-controller` 가 아니다).
2. 엔드포인트 33개 전부에 한 줄 설명이 붙어 있다.
3. 우상단 **Authorize** 에 자물쇠가 있고, `/api/auth/signup` · `/api/w/**` 에는 자물쇠가 없다.
4. 아무 보호 엔드포인트를 펼치면 **401 · 500** 응답이 공통 포맷 예시와 함께 보인다.
   `/api/auth/signup` 에는 **401 이 없다.**

**🔴 이 단계를 건너뛰지 말 것.** 이 저장소는 통합 테스트 71건이 전부 초록인 상태에서
브라우저로만 보이는 버그를 두 번 냈다(하이드레이션 로그아웃, iframe Origin 403).
테스트는 JSON 을 검사하고, 이 단계는 **사람이 읽을 수 있는 화면인지**를 검사한다.

확인이 끝나면 `bootRun` 을 `Ctrl+C` 로 멈춘다.

- [ ] **Step 2: `AGENTS.md` 의 W2 세부 상태 표에 한 행을 추가한다**

"요금제 선택 (2026-09-08)" 행 **다음**에 넣는다.

```markdown
| API 문서 Swagger (2026-09-12) | ✅ **기반 완료.** springdoc 2.8.6 + `OpenApiConfig`(Info · JWT 전역 스킴 · 공통 에러 스키마) + 전 엔드포인트 33개 `summary`. 🔴 **로컬에서만 열린다 - 운영은 두 겹으로 막는다**(`application-prod.yaml` 의 `springdoc.*.enabled: false`, `SecurityConfig` 의 prod 분기). 두 겹이 <각각> 동작하는 것을 `OpenApiProdExposureIntegrationTest` 가 CI 에서 확인한다(토큰 없이 401 = 경로 차단, 토큰 붙여 404 = 생성 차단). 한 번만 부르면 401 에 가려 생성 차단이 살아 있는지 알 수 없어서 두 번 부른다. 🔴 **springdoc 3.x 는 존재하지 않는다** - 2.8.6 은 Boot 3.4.4 를 부모로 빌드된 것이고 Boot 4.0.7 에서 도는 것은 <실측했을 뿐 보장이 아니다>. Boot 를 올릴 때 깨질 수 있고 후퇴할 곳이 없다. 🔴 **공통 401 을 전 엔드포인트에 달지 않는다** - 가입·위젯은 토큰을 받지 않아 401 이 나갈 수 없다. 판정 신호는 `@SecurityRequirements` 의 유무 하나로, 보안 표기와 401 표기가 자동으로 맞는다. 🔴 **커버리지 테스트는 빈칸만 잡는다** - `summary` 가 맞는 말인지는 검사하지 못한다("봇 목록 조회" 라고 써놓고 삭제하는 코드여도 통과한다). ⚠️ **prod 프로파일 테스트는 컨텍스트와 Testcontainers 를 하나 더 띄운다**(활성 프로파일이 캐시 키의 일부라 불가피하다). 알고 감수했다. 🚧 `description`("왜")·엔드포인트별 `@ApiResponse`·요청 DTO `example` 은 PR 2~4 |
```

- [ ] **Step 3: `docs/decisions.md` 에 두 줄을 추가한다**

파일 맨 아래(최신 항목 자리)에 넣는다. 형식은 `날짜 | 무엇을 | 왜 그렇게 | 검토한 대안`.

```markdown
## 2026-09-12 | API 문서를 Spring REST Docs 가 아니라 Swagger(springdoc)로 만든다

REST Docs 는 테스트가 통과해야 문서가 생성돼 문서가 거짓말을 할 수 없다. 문서 정확성만 보면 더 나은
도구이고 국내에서 자주 거론된다. **그런데 기각 사유는 도구의 우열이 아니라 이 저장소의 테스트 구조다.**
REST Docs 가 붙는 것은 MockMvc · WebTestClient · RestAssured 뿐인데, 이 저장소의 통합 테스트는
전부 `TestRestTemplate`/`RestTestClient` + 진짜 톰캣(`RANDOM_PORT`)이라 붙지 않는다.

두 갈래가 다 나빴다. ① 206건을 MockMvc 로 재작성하는 것은 비용보다 방향이 문제다. 진짜 톰캣을
고집하는 이유가 기록돼 있다 - 통합 테스트 71건이 전부 초록인 상태에서 HTTP/2 업그레이드 버그가 났고
가짜 서버로는 재현 자체가 불가능했다. MockMvc 는 서블릿 컨테이너를 아예 안 띄우므로 그때 못 잡은
부류를 더 못 잡는다. **문서를 얻으려고 테스트 신뢰도를 내리는 거래다.** ② 문서 전용 테스트 33개를
병행하면 테스트가 두 벌이 되고, "문서가 거짓말 못 한다" 는 장점이 문서용 테스트에 한해서만 성립한다.

검토한 대안: Spring REST Docs / `restdocs-api-spec`(REST Docs → OpenAPI → Swagger UI. 위 문제가
그대로 남고 Boot 4 호환이 또 미확인이라 미확인 변수를 하나 더 들이지 않았다) / 문서를 안 만드는 것.

**한계:** springdoc 2.8.6 은 Boot 3.4.4 를 부모로 빌드됐고 springdoc 3.x 는 없다(Maven Central 확인).
Boot 4.0.7 에서 기동·`/v3/api-docs` 200·테스트 통과를 실측했지만 **호환이 보장된 것이 아니라 우연히
맞는 것**이고, Boot 를 올릴 때 깨지면 후퇴할 곳이 없다. 대신 깨지면 기동 실패나 테스트 실패로
시끄럽게 드러나고, 문서는 로컬 전시용이라 운영 장애가 아니다. 그 조건에서 감수했다.

## 2026-09-12 | 문서 노출을 한 겹이 아니라 두 겹으로 막고, 공통 401 은 전 엔드포인트에 달지 않는다

**두 겹:** ① `application-prod.yaml` 의 `springdoc.api-docs.enabled: false`(생성 자체를 끈다)
② `SecurityConfig` 가 문서 경로의 `permitAll` 을 prod 가 아닐 때만 거는 것(경로를 인증 뒤에 둔다).
한 겹만 두면 우회 경로가 생겨 규칙이 없는 것과 같아진다 - ① 만 두면 누가 설정을 되돌리는 순간 전부
열리고, ② 만 두면 문서는 계속 생성되면서 401 뒤에 숨어 있을 뿐이라 계정 하나만 있으면 읽힌다.
요금제 불변식을 `PlanService` 와 `BillingService.delete` 양쪽에서 막은 것과 같은 논리다.
새 축(`app.swagger.enabled` 같은 것)을 만들지 않고 이미 서 있는 `prod` 프로파일 축에 얹었다.

검증도 두 겹을 **각각** 잰다. 운영 프로파일로 실제 기동해 토큰 없이 부르면 401(②가 동작),
토큰을 붙여 부르면 404(①이 동작)를 확인한다. **한 번만 부르면 401 에 가려 ① 이 살아 있는지 알 수
없다.** 대가는 활성 프로파일이 컨텍스트 캐시 키의 일부라 컨텍스트와 Testcontainers 가 하나 더
뜨는 것이고, 알고 감수했다 - prod 프로파일의 동작은 prod 로 띄워보지 않으면 알 수 없다.

**공통 401:** 스펙은 "모든 엔드포인트에 공통인 401·500" 을 일괄로 붙이라고 적었는데, 짜다 보니
401 은 공통이 아니었다. `/api/auth/signup` 은 401 을 낼 수 없고 위젯 API 는 인증이 없다. 일괄로
달면 **문서가 없는 응답을 있다고 말한다.** 그래서 500 만 전부에 달고, 401 은 전역 JWT 요구를
`@SecurityRequirements` 로 해제하지 않은 엔드포인트에만 단다. 신호 하나로 보안 표기와 401 표기가
자동으로 맞는다. 검토한 대안: 일괄로 달기(문서가 거짓이 된다) / 엔드포인트마다 손으로 달기
(33곳에 반복되고 빠뜨리면 조용히 어긋난다).

**공통 에러 스키마는 `@Schema(implementation = ErrorResponse.class)` 가 아니라 손으로 정의했다.**
Boot 4 는 Jackson 3(`tools.jackson`)이고 swagger-core 2.2.29 는 Jackson 2 다. 두 Jackson 이 한
클래스패스에 있는 상태에서 모델 introspection 에 의존하지 않는 쪽이 안전하고, 필드가 `code`·`message`
둘뿐이라 손으로 적는 비용이 거의 없으며 결과가 결정적이다.
```

- [ ] **Step 4: 프론트에 영향이 없는 것을 확인한다**

이 PR 은 `web/` 을 건드리지 않으므로 API 컨트랙트가 바뀌지 않는다.
**확인만 한다** (변경 파일 목록에 `web/` 이 섞였는지):

```bash
cd /Users/cheonjamin/projects/AllDap
git diff --name-only main...HEAD
```

기대: `api/` 와 `AGENTS.md` · `docs/` 만 나온다. `web/` 이 나오면 잘못 담은 것이다.

- [ ] **Step 5: 전체 검사를 마지막으로 한 번 더 돌린다**

```bash
cd /Users/cheonjamin/projects/AllDap/api
./gradlew build
```

기대: **BUILD SUCCESSFUL.** 테스트 건수를 출력에서 확인해 적어둘 것(PR 본문에 쓴다).

- [ ] **Step 6: 커밋하고 푸시한다**

```bash
cd /Users/cheonjamin/projects/AllDap
git add AGENTS.md docs/decisions.md
git commit -m "docs: Swagger 기반 슬라이스의 진행 상황과 결정을 남긴다"
git push -u origin feat/swagger-openapi-base
```

- [ ] **Step 7: PR 을 만든다**

`.github/PULL_REQUEST_TEMPLATE.md` 를 채운다. **체크리스트가 아니라 설명을 요구하는 템플릿이다.**
두 칸이 핵심이다:

- **한계 & 트레이드오프** : springdoc 3.x 가 없어 Boot 3 대상 최신을 쓴 것(보장이 아니라 실측),
  커버리지 테스트가 빈칸만 잡는 것, prod 테스트가 컨테이너를 하나 더 띄우는 것,
  운영에서 문서를 못 보는 것(전시 목적과 일부 상충), Python `/docs` 는 여전히 네트워크 한 겹인 것.
- **검토한 대안과 선택 이유** : REST Docs · `restdocs-api-spec` · 문서를 안 만드는 것.
  `docs/decisions.md` 2026-09-12 항목 두 개를 요약해 옮긴다.

**어떻게 해결했나요** 칸에는 "돌려봤다" 가 아니라 **Step 5 의 실제 실행 결과**(테스트 건수)와
**Step 1 의 브라우저 확인 결과**를 붙인다.

```bash
cd /Users/cheonjamin/projects/AllDap
gh pr create --base main --head feat/swagger-openapi-base \
  --title "feat: API 문서(Swagger)를 붙이고 운영 노출을 두 겹으로 막는다"
```

⚠️ `gh pr create` 까지가 이 태스크의 끝이다. **CI 결과를 기다리며 폴링하지 않는다.**

---

## Self-Review

**1. 스펙 커버리지**

| 스펙 절 | 어디서 | 비고 |
|---|---|---|
| §1 Spring 만 / Boot 4 실측 / 규모 | Task 1 Step 4 주석, 전제 확인 표 | Python 은 범위 밖(§3 마지막) |
| §2 REST Docs 기각 | Task 5 Step 3 `decisions.md` | 코드에 남는 것이 없어 결정 로그가 유일한 자리 |
| §3 노출 정책 두 겹 | Task 1(②) · Task 2(①) | 검증까지 포함 |
| §3 Python 손대지 않음 | Task 5 PR 본문 한계 칸 | 알고 남긴다 |
| §4 Info · JWT 스킴 · 공통 에러 | Task 3 | 401 범위는 의도적으로 다름(위 절 ①) |
| §5 `@Tag` 11 · `@Operation` 33 | Task 4 | `description`·DTO `example` 은 PR 2~4 |
| §5 한국어 | Global Constraints | |
| §6 ① 커버리지 테스트 | Task 4 Step 1 | 한계(빈칸만)를 javadoc 과 PR 에 명시 |
| §6 ② 운영 차단 테스트 | Task 2 | 필수 환경변수 채우는 방법까지 |
| §7 PR 1 범위 | 전부 | PR 2~4 는 별도 계획 |

**2. 플레이스홀더 스캔** : "TBD"·"적절히 처리"·"Task N 과 비슷하게" 없음. 코드가 필요한 모든 단계에 실제 코드나 값 표가 있다. 컨트롤러 11개의 `summary` 33개는 전부 문자열로 적혀 있다.

**3. 타입 일관성**
- `SecurityConfig.OPEN_API_PATHS` : Task 1 에서 `static final String[]` 로 선언하고 Task 1 의 `requestMatchers(OPEN_API_PATHS)` 에서만 쓴다. Task 2 의 테스트는 경로 문자열을 직접 적는다(HTTP 로 부르는 쪽이라 상수를 참조할 이유가 없다). 상수의 javadoc 이 "짝 테스트가 같은 경로를 본다" 고 말하는 것은 **사람이 함께 고치라는 뜻**이고, 코드 의존은 없다.
- `OpenApiConfig.BEARER_SCHEME` · `ERROR_SCHEMA_NAME` : Task 3 에서 `public` 으로 선언. 이 PR 에서는 `OpenApiConfig` 내부에서만 쓰이고, PR 2~4 가 참조한다. **이 PR 범위에서는 미사용 `public` 상수 2개가 생긴다** (의도된 것이고 javadoc 이 그 이유를 말한다).
- `@SecurityRequirements` : Task 3 에서 4곳에 붙이고 Task 3 의 `OperationCustomizer` 가 그 유무를 읽는다. Task 4 의 `@Operation` 과 같은 메서드에 공존한다(Step 3 의 코드가 둘을 함께 보여준다).
- `ErrorCode.INTERNAL_ERROR` · `AUTHENTICATION_REQUIRED` · `BOT_NOT_FOUND` : 셋 다 기존 enum 에 실재함을 grep 으로 확인했다.

---

## 이 계획이 다루지 않는 것

- **PR 2~4** (`description` 의 "왜", 엔드포인트별 `@ApiResponse`, 요청 DTO `@Schema(example)`, 모호한 응답 필드 설명). 각각 별도 계획으로 쓴다. 이 저장소 규칙("한 번에 하나씩")대로다.
- **Python `/docs` 의 노출 차단.** 조건 없이 항상 열려 있고 방어는 네트워크 한 겹뿐이다. 스펙 §3 이 알고 뺐다. 필요해지면 `config` 의 prod 플래그로 `docs_url=None` 을 거는 한 줄짜리 슬라이스.
- **management 포트(8081)의 문서 경로.** 로컬에서는 그 포트에도 필터 체인이 붙어 permitAll 이 적용될 수 있다. 운영에서는 `docker-compose.prod.yml` 이 그 포트를 열지 않고 겹 ① 이 문서를 아예 생성하지 않으므로 실질 위험이 없다. 재지 않는다.
- **엔드포인트 개수 고정 검사**(예: "정확히 33개여야 한다"). 엔드포인트를 추가할 때마다 무관한 테스트가 깨진다. 커버리지 테스트가 이미 "설명 없는 것" 을 막으므로 중복이다.
