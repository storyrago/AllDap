# 설계 결정 로그

형식: `날짜 | 무엇을 | 왜 그렇게 | 검토한 대안`

이 파일은 자소서·면접 답변의 원재료입니다. 결정할 때마다 한 줄씩 남기세요.

---

2026-07-30 | 프로젝트 주제를 문서 기반 FAQ 챗봇 빌더로 확정 | 실제 채용공고가 RAG·품질 평가·모니터링을 주업무/우대사항으로 명시해 시장 요구와 일치 | 전세 안심 체크, 온디바이스 식단앱

2026-07-30 | 차별점을 "품질 평가 대시보드"로 설정 | 챗봇을 만드는 문제는 이미 풀렸고, 믿는 문제가 안 풀림. 기존 빌더에 없는 지점 | 단순 챗봇 빌더(=클론), 한국어 특화만으로 승부
  ↳ ⚠️ **2026-07-31 경쟁 조사로 "기존 빌더에 없는 지점"이 사실이 아님이 확인됨.** Voiceflow Evaluations가 노코드 빌더 안에 LLM-as-judge 자동 채점을 이미 제공하고, 국내에도 DATUMO Eval이 있다. **결론(품질 평가를 차별점으로 둔다)은 유지하되 근거가 바뀌었다** — "아무도 안 해서"가 아니라 "표준 지표를 직접 구현해 개선을 증명하는 자리라서"이다. 자세한 것은 아래 2026-07-31 "차별점을 …침묵…으로 이동" 항목.

2026-07-31 | 아키텍처를 Spring+Python 하이브리드로 변경 | 채용공고 8건 중 6건이 Java/Spring 요구, 8건 전부 RDBMS·REST 요구. 동시에 AI 파이프라인은 Python 생태계 필수 | 전체 Spring 이식(강점 상실), Python 유지(시장 요구 미충족)

2026-07-31 | 공유 DB를 감수하고 서비스만 분리 | 1인 개발에서 데이터 동기화 비용 > 서비스 분리 이득 | DB 분리 + API 통신(운영 부담 과다)

2026-07-31 | 개발 순서를 W2(Spring) 우선으로 변경 | 익숙한 영역에서 시작해 완주 가능성을 높이고, Spring에서 Python을 호출하며 자연스럽게 Python 코드를 읽게 됨 | W1 Python 완성 후 진행

2026-07-31 | 모델 경량화·양자화를 프로젝트 범위에서 제외 | 지금 끌어들이면 Python 학습·Spring 구현·경량화가 모두 어중간해짐 | 프로젝트에 온디바이스 임베딩 포함

2026-07-31 | 제품명을 다답(DaDap)에서 **AllDap**으로 변경 | 이름이 코드 곳곳(패키지 `com.alldap.api`, DB명·계정 `alldap`, 컨테이너명, 나중엔 도메인)에 박히므로 늦게 바꿀수록 비용이 커져 지금 확정함. "다답"은 한글로 읽을 때만 뜻이 통하고 영문 표기 DaDap은 발음도 의미도 전달되지 않는데, AllDap은 All(전부) + Dap(답)으로 영문 표기에서도 "다 답한다"는 제품 한 줄 정의가 그대로 읽힘 | 다답/DaDap 유지(영문 표기 문제 잔존), 한글 어원을 버린 완전 신규 영문 작명(제품 슬로건 "문서만 올리면 다 답합니다"와의 연결이 끊김)

2026-07-31 | 프론트엔드를 **Next.js(App Router) + TypeScript**로 확정 | ① 지원 방향을 백엔드 한정이 아니라 **풀스택 지향**으로 잡았고, 프론트를 직접 만든 흔적이 없으면 그 주장을 증명할 근거가 없음 ② PRD §7 사이트맵이 이미 `/bot/[botId]/quality` 같은 동적 경로 중심이라 App Router의 파일 기반 라우팅과 그대로 대응됨 ③ 랜딩 페이지는 SEO가 필요하고 대시보드는 상태가 많은데, SSR과 CSR을 한 프레임워크 안에서 골라 쓸 수 있음 | Spring + Thymeleaf(배포 대상이 1개 늘지 않는 게 최대 장점. 그러나 프론트 역량 증명이 사실상 불가능하고 위젯·대시보드의 상호작용을 서버 렌더링으로 감당하기 어려움), React SPA를 빌드해 Spring 정적 리소스로 서빙(배포 대상은 1개지만 SEO를 포기하고 빌드 산출물을 Java 저장소에 섞게 됨)
  ↳ **정직한 트레이드오프**: 이 선택은 PRD §11.2의 원칙 "1인 개발에서 운영해야 할 것의 개수를 최소화한다"와 정면으로 충돌한다. 배포 대상이 Spring·Python 2개에서 Next.js를 더한 3개가 된다. 그럼에도 택한 이유는 이 프로젝트의 1순위 목적이 "운영 효율"이 아니라 "취업 포트폴리오"이고, 풀스택 역량 증명의 가치가 배포 대상 +1의 운영 비용보다 크다고 판단했기 때문. 원칙을 몰라서 어긴 게 아니라 알고 어긴 것이며, 이 사실을 PRD §11.3 "알려진 약점"에 명시해 둔다.

2026-07-31 | 저장소 디렉터리 중첩(`AllDap/dadap/*`)을 저장소 루트로 평탄화 | 실제 서비스 경계는 api·ai-service·web 3개인데 그 위에 의미 없는 디렉터리가 한 겹 더 있으면 `docker-compose.yml`의 상대 경로, IDE 모듈 인식, README의 `cd` 안내가 전부 한 칸씩 어긋남. 게다가 그 중간 디렉터리 이름이 옛 제품명 `dadap`이라 이름 변경 결정과도 충돌 | 중첩 유지(경로만 계속 길어짐), 서비스별로 저장소를 분리(1인 개발에서 3개 저장소를 동시에 커밋·배포하는 비용이 과다)

~~2026-07-31 | Spring Boot 버전을 **3.5.3**으로 고정 (Initializr가 4.0.7로 생성한 것을 내림) | start.spring.io에서 3.4.x 선택지가 내려가 있어 4.0.7로 생성됐으나, 국내 채용공고와 레퍼런스·블로그·StackOverflow 답변이 아직 3.x 중심이라 막혔을 때 찾을 수 있는 자료의 양이 다름. 신입 포트폴리오에서 중요한 건 최신 버전을 쓰는 것이 아니라 **막혔을 때 스스로 빠져나오는 것**이고, 지금은 Spring 구현이 크리티컬 패스라 여기서 버전 문제로 시간을 쓰면 W3(품질 대시보드)까지 못 간다 | 4.0.7 그대로 사용(최신 버전 경험은 얻지만 레퍼런스 부족 리스크를 크리티컬 패스에 얹음), Initializr를 다시 돌려 3.x 계열을 직접 지정~~
  ↳ ⚠️ **같은 날 철회됨. 아래 "Boot 4.0.7 유지" 항목으로 대체.** 이 줄은 판단 과정의 기록으로만 남긴다.

~~2026-07-31 | **Flyway를 도입하지 않음** — `api/src/main/resources/db/migration/V1__init.sql`을 docker-entrypoint-initdb.d로 적용하는 현재 방식 유지 | 스키마의 주인은 하나여야 한다. 지금은 Python·Spring 두 서비스가 같은 DB를 공유하는 구조라 이미 스키마 사고 위험이 있고(그래서 Spring `ddl-auto`를 `validate`로 못박음), 여기에 Flyway까지 얹으면 "SQL 파일 / Flyway 이력 테이블 / Hibernate" 셋이 스키마를 주장하게 된다. MVP 단계에서는 테이블이 아직 굳지 않아 마이그레이션을 쌓기보다 `docker compose down -v`로 초기화하는 편이 빠르고 안전함 | 지금 Flyway 도입(버전 관리는 되지만 스키마 주인이 둘이 됨 + 초기화 시 이력 테이블까지 정리해야 함), Hibernate `ddl-auto: update`(Python 쪽이 즉시 깨지므로 논외)
  ↳ **언제 바꿔야 하나**: 배포된 DB에 데이터가 쌓여 `down -v`로 초기화할 수 없게 되는 시점(파일럿 도입 시). 그때 현재 `V1__init.sql`을 Flyway의 baseline으로 삼아 도입한다. 파일명을 `V1__` 형식으로 지어둔 것이 그 대비다.~~
  ↳ ⚠️ **같은 날 철회됨. 아래 "Flyway 도입" 항목으로 대체.** 이 줄은 판단 과정의 기록으로만 남긴다.

2026-07-31 | Spring Boot **4.0.7 + Java 21을 그대로 유지** (같은 날 내린 "3.5.3으로 내림" 결정을 철회) | 내리는 비용이 예상보다 컸다. Boot 4는 스타터 이름 자체가 바뀌어서 — `spring-boot-starter-web`이 `-webmvc`가 되고, 통합 `starter-test`가 `-webmvc-test`·`-data-jpa-test` 등으로 쪼개졌다 — "한 줄만 바꾸면 된다"고 봤던 게 실제로는 7줄이었고, 버전을 내린 뒤 무엇이 더 깨질지 확인하는 시간까지 계산하면 "레퍼런스가 많아 빨리 빠져나온다"는 이득을 상쇄한다. 결정적으로 참고 레포(hwanh2/kcalog)가 이미 Boot 4.0.7 + Java 21로 실제 굴러가는 것을 확인했고, `./gradlew compileJava`와 실기동(Flyway 적용 + Hibernate `validate` 통과 + `/actuator/health` 200)까지 직접 검증했다 | 3.5.3으로 내림(레퍼런스는 많아지나 7줄 수정 + 미지의 추가 수정 리스크), Initializr 재생성(같은 문제 반복)

2026-07-31 | **Flyway 도입** — 스키마를 `api/src/main/resources/db/migration/`으로 옮기고 Spring 기동 시 적용 (같은 날 내린 "도입하지 않음" 결정을 철회) | 철회 사유가 핵심이다. 원래 걱정은 "SQL 파일 / Flyway 이력 테이블 / Hibernate 셋이 스키마를 주장한다"였는데, 이는 **데이터 쓰기 소유권과 스키마 소유권을 섞어서 본 오류**였다. Flyway로 스키마는 Spring이 단독 소유하고 Python은 데이터만 읽고 쓰면 주인은 여전히 하나다(테이블 소유권 표는 *데이터* 소유권이지 *스키마* 소유권이 아니다). 반대로 유지하려던 옛 방식은 스키마를 고칠 때마다 `docker compose down -v`로 데이터를 통째로 날려야 해서 파일럿이 시작되면 못 쓴다 — 즉 "나중에 바꾼다"가 아니라 **지금 안 바꾸면 데이터가 쌓인 뒤엔 더 비싸지는** 종류의 결정이었다 | 옛 방식 유지(파일럿 시점에 이관 비용을 다시 지불), Hibernate `ddl-auto: update`(Python 쪽이 즉시 깨지므로 논외)
  ↳ **생긴 제약**: ① 적용된 마이그레이션은 수정 금지(체크섬 불일치로 기동 실패) → 변경은 `V2__*.sql`로만 ② 로컬 실행 순서가 `db → ai-service → api`에서 **`db → api → ai-service`**로 바뀜(Spring이 떠야 테이블이 생김) ③ `V1__init.sql`의 로컬 시드 INSERT가 배포 환경에도 적용됨 → `dev` 프로파일 분리 여부 **[미정]**

2026-07-31 | `AGENTS.md`를 원본으로 두고 `CLAUDE.md`는 `@AGENTS.md` 한 줄 포인터로 전환 | 내용이 한 곳에만 있어야 어긋나지 않는다. 도구별 파일에 규칙을 복사하기 시작하면 반드시 둘이 따로 논다. 또 AI 도구를 바꿔도(Cursor 등) 가이드가 그대로 살아남는다. `create-next-app`이 `web/`에 이미 같은 구조를 만들어 놓은 것을 확인했고(Next.js 공식 스캐폴더의 기본형), 루트를 거기에 맞춰 일관성을 얻었다 | `CLAUDE.md` 단독 유지(다른 도구를 붙이는 순간 복사본이 생김), 양쪽에 전문 복사(즉시 불일치)

2026-07-31 | `docker-compose.yml`의 Postgres 포트를 `"5432:5432"`에서 **`"127.0.0.1:5432:5432"`**로 변경 | 앞의 표기는 `0.0.0.0`에 바인딩돼 같은 네트워크(카페 와이파이, 공유 오피스)의 누구나 DB에 붙을 수 있다. 계정·비번이 `alldap/alldap`이라 사실상 무방비다. PRD가 "Python 서비스는 외부 노출 금지"를 [확정]으로 못박아 놓고 정작 DB를 열어두는 건 앞뒤가 맞지 않는다 | 그대로 두기(로컬이니 괜찮다는 판단 — 실제로는 노트북을 들고 나가는 순간 깨짐)

2026-07-31 | CI(GitHub Actions)·PR 템플릿을 뼈대 단계에서 미리 도입 | 나중에 붙이면 "이미 깨진 상태"에서 시작하게 되고, 그러면 빨간불에 익숙해져 CI가 무의미해진다. 서드파티 액션은 커밋 SHA로 핀 고정하고 `permissions: contents: read`를 기본값으로 둬서 공급망·권한 리스크를 낮췄다. **커버리지 게이트는 일부러 넣지 않았다** — 지금 테스트가 0개라 게이트를 걸면 첫 PR부터 실패하고, 숫자를 채우려는 의미 없는 테스트를 유도한다 | 코드가 쌓인 뒤 도입(초기 커밋부터의 이력이 검증 안 됨), 커버리지 게이트 포함(숫자 채우기용 테스트 유발)

2026-07-31 | **과금축을 "문서 수·용량"이 아니라 "답변 수 + 평가 실행"으로 설계** (설계만 확정, 구현은 PRD §13 진입 기준 충족 이후) | 문서 수 과금은 두 가지가 어긋난다. ① **원가와 어긋남** — 문서는 업로드 시 한 번 임베딩하면 끝이고 저장 비용은 사실상 0인 반면, 실제 비용은 질문마다 발생하는 LLM 호출과 평가 실행에서 나온다. 원가가 안 드는 곳에 값을 매기는 셈이다. ② **제품 가치와 역방향** — 이 제품의 가치는 "봇이 정확히 답하는 것"이고 그러려면 근거 문서가 많을수록 좋다. 문서 수를 제한하면 고객이 봇을 잘 만드는 것 자체를 막게 되고, 결국 못 답하는 봇이 되어 이탈한다. 즉 제품 가치를 깎는 방향으로 과금하는 구조다. 따라서 문서 수는 공정 사용 상한(fair use)으로만 두고, 과금은 비용이 실제로 발생하고 고객이 가치를 느끼는 지점에 건다 | 문서 수·용량 과금(이해는 쉽고 구현도 쉬우나 위 두 이유로 기각), 봇 개수 과금(봇을 나누는 건 고객의 조직 구조 문제지 가치와 무관), 정액제 단일 요금(원가 변동이 큰 LLM 호출을 흡수하지 못해 헤비 유저에서 역마진)
  ↳ **① 답변 수 기반 — 단, `is_fallback == true`는 과금하지 않는다.** 이 항목이 핵심이다. "봇이 답하지 못한 질문에는 요금을 받지 않습니다"가 그대로 판매 문구가 된다. 원가(실제 LLM 호출분만 청구)와 제품 철학(환각 억제)이 동시에 정렬되는 드문 지점이다 — 억지로 답하게 만들 유인이 회사 쪽에 생기지 않는다. `messages.is_fallback` 컬럼이 이미 스키마에 있어 과금 로직을 새로 설계할 필요가 없고, fallback을 구분해 기록하지 않는 경쟁 제품은 이 과금을 따라할 수 없다.
  ↳ **② 품질 평가 실행 횟수.** 평가는 질문 N개를 실제 파이프라인에 태우고 LLM으로 채점하므로 단건 비용이 가장 크다. 무료는 1회(맛보기가 없으면 제품 가치 자체를 못 느낌), 유료는 반복 실행과 **설정별 before/after 비교**. 비교를 유료로 두는 게 핵심 — 점수를 한 번 보면 개선 여부가 궁금해지고 그러려면 재실행이 필요하다. 평가를 돌릴수록 이력 데이터가 쌓여 이탈이 어려워진다(PRD Flow C의 락인).
  ↳ **③ 미답변 로그 기반 인사이트 리포트.** 미답변 질문은 "문서에 없는 것 = 조직이 몰랐던 수요"다. 챗봇은 비용 절감이지만 이건 인사이트라 값어치의 결이 다르다(PRD Phase 2 "대화 인사이트"와 동일 선상).
  ↳ **진입 시장**: 1인 개발 기준으로는 "한글 문서(HWPX) × 오답이 분쟁이 되는 도메인"의 교집합이 현실적이다. ~~전자는 해외 툴이 진입하지 못하는 실질적 해자이고~~(공공·대학·병원은 한글 문서가 기본), 후자는 근거 표시가 옵션이 아니라 필수인 영역이다(PRD 페르소나 P3 쇼핑몰 환불 규정이 정확히 여기). 규제 산업(금융·의료)은 단가가 높지만 보안 심사·계약 부담으로 1인 개발 MVP 단계에서는 보류.
    ↳ ⚠️ **2026-07-31 정정: "해자"가 아니다.** ChatGPT가 HWP를 직접 지원하고 한컴이 파싱 SDK를 판다. **진입 요건**으로 격하한다. 다만 "노코드 빌더 계층에서는 아직 드물다"(Chatbase 공식 미지원)까지는 여전히 사실이라 시장 선택의 근거로는 유효하다.
  ↳ **지금 구현하지 않는 이유**: 위 ①②③이 전부 평가·fallback 데이터에 기대고 있어 **W3(품질 대시보드)가 없으면 어느 것도 성립하지 않는다.** 지금 결제를 붙이면 W3이 밀리고, 그러면 과금 근거 자체가 사라진다. PRD §13의 진입 기준(주 20건 실사용 + 미답변률 15% 미만 4주 연속)을 그대로 유지한다.

2026-07-31 | **차별점을 "한글 문서를 읽는다"에서 "답하지 않아야 할 때 침묵하는지를 측정한다"로 이동** (경쟁 제품 33건 조사 후 PRD §2 전면 개정, v0.3 → v0.4) | 원래 차별점 3개 중 2개가 조사로 무너졌다. ① **한글 문서(HWPX) 지원** — ChatGPT가 2026-04부터 HWP·HWPX 직접 업로드를 지원하고(Gemini는 4개월 앞섬), 한컴이 HWP·HWPX 직파싱 SDK를 SaaS API로 판매한다. 즉 "만들기 어려워서 생기는 해자"가 아니라 **돈 주고 사는 부품**이며, 심지어 AllDap은 구버전 바이너리 `.hwp`를 못 읽어(`parsers.py:26`) 우위를 주장할 근거조차 없다. ② **출처 인용** — 채널톡·CustomGPT·Chatbase·한컴피디아가 전부 제공하는 평범한 기능이다. ③ 남은 "품질 평가"도 축소됐다 — Voiceflow Evaluations가 노코드 빌더 안에 LLM-as-judge 자동 채점을 이미 넣었고, 국내에도 DATUMO Eval이 거의 같은 것을 한다. 그래서 차별점을 **fallback 자체를 지표화하는 것**으로 옮겼다. 이것만이 구조적으로 방어된다 — 상용 AI 상담 제품은 대부분 많이 답해야 돈을 벌므로(Intercom Fin 아웃컴당 과금, Sierra 해결 시 과금, Zendesk 자동해결당 과금) "일부러 답하지 않는 능력"을 1급 지표로 올릴 유인이 **구조적으로 없다**. 반면 AllDap은 이미 `is_fallback`에 과금하지 않기로 정해(PRD §13.1) 지표와 과금이 같은 방향을 본다. 베끼려면 가격 모델을 바꿔야 한다 | 한글 문서 지원을 계속 차별점으로 주장(면접에서 ChatGPT 얘기가 나오면 즉시 무너짐), 차별점을 포기하고 일반 챗봇 빌더로 전환(W3을 버리면 그냥 흔한 클론이 됨), 엔터프라이즈로 상향(한컴피디아·올거나이즈와 정면 충돌 + 1인 개발로 영업·온프레미스 불가)
  ↳ **함께 격하한 것**: "문서 안에서만 답한다"(사이드톡이 문자 그대로 같은 문구를 쓴다), "fallback 무과금이 독창적"(아웃컴 과금은 Sierra가 표준화한 것 — 독창적인 건 과금 모델이 아니라 **그것을 품질 지표와 정렬시킨 것**이다).
  ↳ **지표 이름을 발명하지 않기로 함.** Ragas의 `faithfulness`·`answer relevancy` 표기를 그대로 쓴다. "표준을 구현했다"가 "새 이름을 붙였다"보다 설명력이 높고, 면접관이 Ragas를 알 때 오히려 유리하다.
  ↳ **시장 해자가 없음을 PRD에 명시적으로 인정했다.** 1인 MVP에 해자가 있는 게 오히려 이상하다. 포트폴리오에서 방어 가능한 가치는 "시장에 없는 기능"이 아니라 **"측정 → 개선 → 수치화 루프를 직접 만들었다"**는 사실이며, 이는 경쟁사가 무엇을 출시하든 훼손되지 않는다.
  ↳ **W3을 Must로 둔 판단은 유지하되 이유가 바뀌었다.** "아무도 안 해서"(거짓)가 아니라 "표준 지표를 직접 구현해 자기 시스템 개선을 증명하는 자리라서"이다.
  ↳ **확인 못 한 것**: 한컴피디아의 품질 평가·임베드 위젯 유무(가격·스펙 비공개), 한컴 챗봇 SDK의 실체(상세 페이지 404), Chatbase 로그인 후 관리자 화면. 위 판정은 이것들이 어느 쪽으로 밝혀져도 뒤집히지 않도록 짰다.

2026-07-31 | **외부 LLM 제공자를 OpenAI(임베딩)+Anthropic(생성)에서 Google Gemini로 교체** — 임베딩 `gemini-embedding-001`, 생성 `gemini-3.5-flash` | 개발자가 API 크레딧을 충전할 여유가 없어 W1이 무기한 미검증으로 남을 상황이었다. 미검증 코드 위에 W2·W3를 쌓는 것이 제공자를 바꾸는 것보다 위험하다고 판단했다. Gemini 무료 등급은 카드 없이 시작되고 한도 초과 시 429일 뿐 자동 과금 경로가 없다. 결정적으로 **출력 차원을 1536으로 맞출 수 있어**(MRL 기반, 공식 권장값) `VECTOR(1536)` 컬럼을 그대로 쓸 수 있었다 — 마이그레이션 불필요. 부수 이득으로 `task_type`(RETRIEVAL_DOCUMENT / RETRIEVAL_QUERY)이 생겼는데 OpenAI에는 없던 기능이고 RAG 검색 품질에 직접 기여한다. PRD 부록 A #4가 "임베딩 모델은 W4에서 대안과 비교 실험"으로 이미 열어둔 항목이라 확정 결정을 뒤집은 것도 아니다 | OpenAI/Anthropic 유지(크레딧 없어 검증 자체가 불가), 임베딩만 교체(생성을 못 봐서 NO_ANSWER 검증이 빠짐), 로컬 임베딩 모델(PRD §11.3이 셀프호스팅을 범위 밖으로 명시)
  ↳ **제공자 추상화를 만들지 않았다.** 인터페이스나 `if provider ==` 분기 없이 호출 두 군데를 직접 교체했다. 되돌릴 땐 git revert면 된다. 한 번 쓸 유연성을 위해 추상화를 만드는 것이 더 비싸다.
  ↳ **되돌려야 하는 시점**: 파일럿에서 실제 고객 문서를 받을 때. Gemini 무료 등급은 약관상 입력 데이터를 학습에 사용하고("files such as images, videos, or **documents**") 사람이 검토하며("human reviewers may read"), 약관이 직접 "Do not submit sensitive, confidential, or personal information to the Unpaid Services"라고 경고한다. **한국은 EEA·영국·스위스 예외 대상이 아니다.** 끄는 유일한 방법은 결제 계정 연결(선불 $10)이며, 이 시점에 Gemini 유료로 갈지 원래 제공자로 돌아갈지 결정한다.
  ↳ **함정 기록**: ① `gemini-embedding-2`는 문자열 리스트를 주면 **합쳐서 벡터 1개**를 반환한다 — 청크 100개가 에러 없이 뭉개지므로 `gemini-embedding-001`을 써야 한다(개수 검증을 코드에 넣어뒀다). ② 요금표에 무료로 적힌 `gemini-2.5-flash`·`gemini-2.0-flash` 등 2.x 계열이 신규 계정에서는 404다("no longer available to new users"). `models.list()`에는 여전히 나오므로 **목록만 보고 고르면 안 되고 실제로 호출해봐야 안다.** ③ `google-generativeai` 패키지는 폐기됐다(→ `google-genai`), `text-embedding-004` 예제는 이미 죽은 코드다.
  ↳ **주의**: 이후 측정되는 fallback 수치는 **Gemini 기준**이지 Claude 기준이 아니다. 모델을 되돌리면 다시 재야 한다.

2026-07-31 | **`max_distance`를 0.55로 유지** (실측상 0.33 근처가 적절해 보이나 지금 바꾸지 않음) | W1 검증에서 1차 방어선(검색 단계 컷오프)이 **한 번도 작동하지 않았다**. 문서에 없는 질문 10개가 전부 근거 1건을 달고 통과했고, 2차 방어선인 NO_ANSWER가 10개를 다 막았다. 즉 fallback 10/10은 통과했지만 "근거가 없으면 LLM을 호출하지 않는다"는 비용 절감 경로는 열리지 않았다. 실측 거리는 근거 있는 질문 0.27~0.28, 없는 질문 0.38~0.48로 깔끔히 갈려서 0.33 근처면 1차 방어선이 작동한다. **그럼에도 바꾸지 않는 이유**: 표본이 청크 1개·질문 7개뿐이라 지금 조정하면 이 문서 하나에 과적합된다. 이 값은 PRD가 [가정]으로 표기했고 W4에서 평가 점수로 정하기로 한 대상이다 — 근거 없는 숫자를 다른 근거 없는 숫자로 바꾸는 것은 개선이 아니다 | 즉시 0.33으로 조정(표본 부족으로 과적합), 1차 방어선 제거(환각 방어가 한 겹으로 줄어듦)

2026-07-31 | **봇 소유권 검사를 "조회 자체를 소유자로 좁히는" 방식으로 구현** — `findByIdAndUserId(botId, userId)` 하나를 통과하는 private 메서드(`findOwnedBot`)로 모든 봇 조회를 몰았다 | 흔한 방식은 `findById`로 가져온 뒤 `if (!bot.getUser().getId().equals(userId)) throw`인데, 이건 **검사를 빠뜨려도 컴파일이 통과한다**. 엔드포인트가 늘어날 때(문서·채팅·로그·평가 전부 botId를 받는다) 한 곳만 빠뜨리면 봇 간 데이터가 샌다. 조회 쿼리 자체에 소유자를 넣으면 "검사를 안 한 조회"를 쓰는 코드를 작성할 수가 없다 | 서비스에서 명시적 비교(빠뜨리기 쉬움), `@PreAuthorize` + 커스텀 permission evaluator(1인 개발에 배선 비용이 과함), Hibernate `@Filter`(전역 활성화가 필요해 위젯 공개 조회와 충돌)
  ↳ **남의 봇에 403이 아니라 404를 준다.** 403은 "그 봇은 존재하는데 네 것이 아니다"를 알려주는 셈이라, 무작위 UUID를 던져 403만 골라내면 남의 봇 존재 여부를 훑을 수 있다. `ErrorCode.ACCESS_DENIED`가 정의돼 있지만 봇 접근에는 쓰지 않는다. 테스트가 "없는 봇 응답과 남의 봇 응답이 **본문까지 동일**"임을 검증한다.
  ↳ **PATCH의 부분 수정은 `null = 안 보냄`으로 처리했다.** JsonNullable 도입은 보류 — "systemPrompt를 null로 지우기"와 "안 보냄"을 구분해야 하는 화면이 아직 없다. 설정 화면을 만들며 실제로 필요해지면 그때 넣는다.
  ↳ **`getReferenceById` 대신 `findById`로 소유자를 조회한다.** 프록시를 쓰면 SELECT 한 번을 아끼지만, 토큰은 유효한데 계정이 삭제된 경우 INSERT 단계의 FK 위반(500)이 된다. 여기서 걸러야 "다시 로그인하세요"(INVALID_TOKEN)라는 행동 가능한 안내를 줄 수 있다.

2026-08-01 | **문서 목록을 Python 호출이 아니라 Spring 의 DB 직접 조회로 구현** | ① Python 의 `DocumentOut`에 `created_at`이 없어서 Python을 거치면 목록에 업로드 시각을 못 띄운다 — 채우려면 결국 DB를 또 읽어야 하니 호출 두 번이 된다 ② **이 화면은 폴링 대상**이라 호출이 잦은데, Python이 죽었을 때 목록까지 503이 되면 사용자는 "내 문서가 사라졌나" 싶고 죽은 서비스를 초당 몇 번씩 두드리게 된다. 상태 값이 잠시 낡을 뿐 목록은 보이는 편이 낫다. `documents`는 "쓰기=Python / **읽기=Spring도 허용**" 테이블이라 소유권 규칙 위반도 아니다(SELECT만 하고 UPDATE는 여전히 Python) | Python의 `GET /internal/bots/{id}/documents` 호출(상태 판단 기준이 한 곳에 모이지만 위 두 문제), Python 호출 + DB 조회를 합치기(호출 두 번)
  ↳ **대가**: 상태 판단 기준이 두 곳(Python의 UPDATE, Spring의 SELECT)에 걸친다. Python이 상태 값을 추가하면 Spring은 모른 채 문자열로 흘려보낸다. `Document.status`를 enum이 아니라 String으로 둔 것이 이 상황에 대한 대비다.
  ↳ **결과**: `AiServiceClient.listDocuments`는 호출자 없는 죽은 코드가 됐다. **지우지 않고 미구현으로 남겼다** — Python에 엔드포인트가 실제로 있어 나중에 갈아탈 수 있지만, 호출자 없는 코드를 미리 구현하면 검증되지 않은 채 "동작한다"는 인상만 남는다.

2026-08-01 | **지원 확장자 목록을 Spring에 복제하지 않고, Python의 400 응답 문구를 그대로 사용자에게 전달** | 목록을 양쪽에 두면 반드시 어긋나고, 그때 "Spring은 통과시켰는데 Python이 거절"하는 상태가 된다. 형식 판단의 단일 기준은 Python의 `detect_type()` 하나다. 그런데 Spring이 자기 기본 문구("지원하지 않는 파일 형식입니다")로 뭉개면 **사용자가 다음에 뭘 해야 하는지를 잃는다** — Python이 주는 "구버전 .hwp는 아직 지원하지 않습니다. 한글에서 .hwpx로 저장 후 올려주세요."가 정확히 AGENTS.md 작업 규칙 4가 요구하는 문구다. 내부 서비스 에러를 밖으로 흘리지 않는 게 원칙이지만, 여기 오는 문구는 `parsers.py`의 `ParseError`가 **애초에 사용자에게 보여주려고 쓴 한국어**라 예외로 둔다 | Spring에 확장자 목록 복제(단일 기준이 깨짐), `ErrorCode`의 기본 문구만 사용(안내 손실), `.hwp`만 Spring에서 특별 처리(부분 복제라 같은 문제)
  ↳ Python의 **5xx** 메시지는 그대로 흘리지 않는다. "Internal Server Error"는 사용자용 문구가 아니고 내부 구조 노출이다. 4xx만 전달한다.

2026-08-01 | **Python 호출 실패를 `AiServiceClient` 한 곳에서 전부 `ApiException`으로 변환** (503 / 504 / 502를 구분) | 변환을 호출 지점마다 적으면 반드시 한 군데가 빠지고, 빠진 곳에서는 `RestClientException`이 그대로 올라가 `GlobalExceptionHandler`의 마지막 그물에 걸려 **500 INTERNAL_ERROR**가 나간다. 그러면 "Python이 죽었다"가 "우리 서버가 고장났다"로 둔갑해 ① 프론트가 재시도 로직을 잘못 짜고 ② 로그에 가짜 ERROR가 쌓여 진짜 장애가 묻힌다. 코드를 나누는 기준은 **누구 잘못인가**다: 연결 안 됨→503(기다린다) / 느림→504(질문을 줄여 재시도) / Python 5xx→503 / Python 4xx→사용자 입력 문제 | 호출 지점마다 try-catch(빠뜨리기 쉬움), 전부 502로 뭉개기(사용자가 할 수 있는 행동을 구분 못 함), `@ControllerAdvice`에서 `RestClientException`을 잡기(Python 호출인지 다른 HTTP 호출인지 구분 불가)
  ↳ **함정 기록**: 응답 **헤더가 온 뒤** 끊기거나 본문이 늦으면 `ResourceAccessException`이 아니라 **`RestClientException(cause=IOException)`**으로 온다(`DefaultRestClient`가 본문 읽기 중 IOException을 그렇게 감싼다). 처음엔 이걸 "응답 해석 실패(502)"로 분류해, Python 장애를 DTO 불일치처럼 보이게 만들었다. 코드 리뷰에서 잡아 `cause instanceof IOException`이면 503으로 가도록 고쳤다.
  ↳ **재시도·서킷브레이커는 아직 없다.** 특히 업로드는 재시도하면 `documents` 행이 중복 생성되므로 무조건 재시도를 넣으면 안 된다. `RestClientConfig`의 TODO에 조건이 적혀 있다.

2026-08-01 | **테스트에서 Python 자리에 JDK 내장 `HttpServer`로 진짜 가짜 서버를 세움** (Mockito 스텁 대신) | 이 슬라이스에서 검증하려는 것 대부분이 **HTTP 경계에서만 드러난다** — ① multipart에 파일명이 실려 나가는가(안 실리면 Python이 확장자를 못 읽어 멀쩡한 PDF도 거절) ② Python이 죽었을 때 실제로 어떤 예외가 오는가 ③ 읽기 타임아웃이 504로 번역되는가. `AiServiceClient`를 Mockito로 흉내내면 "우리가 상상한 예외"를 검증할 뿐이고, 실제로 무슨 예외가 오는지는 영영 모른다(위 "함정 기록"이 정확히 그 경우다). WireMock 대신 JDK 내장을 쓴 이유는 필요한 기능이 "정해둔 응답 + 요청 기록" 둘뿐이라 의존성을 하나 더 들일 값을 못 하기 때문 | Mockito로 클라이언트 스텁(실제 직렬화·예외를 못 봄), WireMock(의존성 추가), 실제 Python을 띄우기(CI에서 Python+DB+LLM 키까지 필요)
  ↳ **함정**: `HttpServer.create()` 후 `setExecutor()`를 부르지 않으면 JDK 기본값이 `task.run()`이라 **모든 요청이 스레드 하나에서 순차 처리**된다. 그래서 "느린 응답" 테스트의 `sleep`이 서버 전체를 멈춰 **다음 테스트가 엉뚱한 504를 받는다.** 게다가 JUnit 메서드 실행 순서에 따라 나타났다 사라져서, 테스트를 하나 추가하거나 이름만 바꿔도 갑자기 깨진다. 코드 리뷰에서 잡아 `setExecutor(newCachedThreadPool())`로 고쳤고, `MethodOrderer$MethodName`으로 순서를 바꿔 실행해 검증했다.
  ↳ **검증 방식**: "격리 테스트가 실제로 회귀를 잡는가"를 **회귀를 일부러 주입해** 확인했다(`findAllByBotId...` → `findAll()`). 주입 전에는 12개가 전부 통과했고, 테스트를 추가한 뒤에는 그 하나만 실패했다. 테스트가 통과하는 것과 테스트가 무언가를 지키는 것은 다르다.

2026-08-01 | **채팅의 DB 작업을 `ChatTurnStore`라는 별도 빈으로 분리** (같은 클래스 안 `@Transactional` 메서드 대신) | 채팅 한 턴은 `질문 저장(짧음) → Python 호출(수십 초) → 답변 저장(짧음)`이다. 가운데를 트랜잭션에 넣으면 그동안 DB 커넥션이 묶여, **기본 풀 크기 10 기준 동시 질문 10개면 채팅과 무관한 요청까지 전부 멈춘다** — Python 하나가 느려진 것이 서비스 전체 장애가 되는 경로다. 그래서 앞뒤만 짧은 트랜잭션으로 끊어야 하는데, **같은 클래스 안에서는 그게 안 된다**: `@Transactional`은 프록시로 동작해서 자기 객체의 메서드를 자기가 부르면(self-invocation) 프록시를 안 거쳐 **트랜잭션이 조용히 사라진다.** 컴파일도 되고 테스트도 통과하는데 트랜잭션만 없어지는 스프링 AOP의 유명한 함정이다. 빈을 나누면 호출이 프록시를 거치므로 그 사고가 구조적으로 불가능해진다 | 같은 클래스에 `@Transactional` 메서드(self-invocation으로 무효화), `TransactionTemplate` 주입(빈은 안 늘지만 트랜잭션 경계가 서비스 코드 사이에 섞여 "어디부터 어디까지"가 안 읽힘), 클래스 전체에 `@Transactional`(원래 문제로 회귀)
  ↳ **트랜잭션 밖으로 엔티티를 들고 나가지 않는다.** `openTurn`이 `Bot`이 아니라 `Turn(conversationId, fallbackMessage)` 레코드를 반환한다. `open-in-view=false`라 준영속 엔티티의 LAZY 필드를 건드리면 `LazyInitializationException`이 나는데, 필요한 값만 미리 꺼내 나가면 그 사고가 원천적으로 없다.
  ↳ **검증**: `ChatService`에 `@Transactional`을 붙여보는 회귀를 주입하니 "[트랜잭션] Python이 죽으면 질문은 남되 답변 행은 생기지 않는다" 테스트 하나만 실패했다. 트랜잭션이 하나로 묶이면 질문까지 롤백되기 때문이다.

2026-08-01 | **Python 호출이 실패해도 이미 저장한 질문을 지우지 않는다** | ① 지우려면 보상 트랜잭션이 필요한데 그 코드가 또 실패할 수 있다 ② "질문은 왔는데 답이 없다"는 기록이 장애 추적에 실제로 쓸모가 있다 | 보상 트랜잭션으로 삭제(실패 가능성이 중첩), Python 호출 후에 질문 저장(실패 시 질문 자체가 사라져 무슨 질문에서 터졌는지 모름)
  ↳ ⚠️ **이건 fallback과 완전히 다른 상태다.** fallback은 답변 행이 남고 `is_fallback=true`지만, 이쪽은 **답변 행 자체가 없다.** W3에서 미답변을 집계할 때 둘을 섞으면 "AI 서비스 장애"가 "답을 못 찾음"으로 계산돼 품질 지표가 거짓이 된다.

2026-08-01 | **`AiServiceClient`의 4xx 해석을 호출자가 정하도록 분리** (업로드용 매퍼를 채팅에 재사용하지 않음) | 같은 400이라도 의미가 엔드포인트마다 다르다. 업로드의 400은 "파일 형식이 잘못됐다"(사용자가 고칠 수 있다)지만 채팅의 400·422는 "Spring이 스키마에 안 맞는 요청을 보냈다"(사용자는 손쓸 수 없다)이다. 하나로 묶어두면 **채팅 오류에 "지원하지 않는 파일 형식입니다"가 나간다.** 문서 슬라이스에서는 호출자가 하나뿐이라 드러나지 않았고, 채팅을 붙이며 발견했다 | 매퍼 하나로 통일(위 오작동), 엔드포인트마다 call() 복사(변환 로직이 흩어져 한 곳이 빠짐)
  ↳ 교훈: **호출자가 하나일 때 옳아 보이는 추상화가, 두 번째 호출자가 생기는 순간 틀린 것이 된다.** 이 프로젝트에서 "추상화는 두 번째 사례가 생길 때 만든다"는 원칙이 실제로 값을 한 사례.

2026-08-01 | **피드백 값 검증을 DTO가 아니라 `Message` 엔티티에서 강제** | `FeedbackRequest`의 `@Min(-1) @Max(1)`은 **0을 통과시킨다.** 게다가 피드백을 남기는 경로가 앞으로 최소 둘(관리자 대시보드, 위젯 엔드유저)이라 검증을 컨트롤러에 두면 경로마다 복사해야 하고 한 번 빠뜨리면 DB에 이상값이 들어간다. 엔티티에 두면 어느 경로로 와도 지켜진다. "질문에는 피드백을 달 수 없다"도 같은 이유로 엔티티에 둔다 — 질문에 "도움이 됐다"를 매기면 품질 지표가 오염된다 | DTO 검증만(0이 통과), 컨트롤러에서 검증(경로가 늘 때마다 복사), DB CHECK 제약(V1 수정 금지라 새 마이그레이션 필요 + 에러 메시지를 한국어로 못 줌)

2026-08-01 | **대화 이어붙이기 조회에 `channel`을 포함** (`bot_id + session_id` 만으로 찾지 않음) | `session_id`는 브라우저가 만들어 보내는 값이라 관리자 테스트 화면과 위젯이 같은 값을 보낼 가능성이 0이 아니다. channel을 안 보면 **한 대화 안에 두 채널이 섞여**, 품질 지표에서 테스트 대화를 제외하려고 둔 channel 컬럼의 구분 자체가 무너진다 | `bot_id + session_id`만으로 조회(채널 혼입), 세션 키에 접두사 붙이기(프론트가 규칙을 지켜야 하므로 신뢰할 수 없는 입력에 의존)

2026-08-01 | **로그 목록 집계를 "대화 페이지 조회 + 집계 한 번" 두 쿼리로 고정** (대화마다 메시지 조회 금지) | 목록 한 줄에 `messageCount`·`hasFallback`·`firstUserMessage`가 필요한데 이건 conversations에 없는 값이다. 대화 건마다 조회하면 페이지 20건에 쿼리 21번 — **로그는 쌓일수록 커지는 화면이라 실사용에서 가장 먼저 느려지는 지점**이다. 페이지의 대화 id를 모아 `GROUP BY`로 한 번에 가져오면 페이지 크기와 무관하게 쿼리 수가 고정된다 | 대화마다 조회(N+1), 조인해서 한 번에(메시지 수만큼 행이 불어나 `DISTINCT`가 필요해지고 그러면 페이지네이션의 `totalElements`가 어긋난다), `@EntityGraph`로 messages fetch(컬렉션을 통째로 로딩하는데 정작 필요한 건 집계값 3개뿐)
  ↳ **`firstUserMessage` 때문에 네이티브 쿼리를 썼다.** "가장 이른 user 메시지 본문"은 JPQL로 표현할 수 없고 `array_agg(... ORDER BY ...) FILTER (WHERE ...)`가 필요하다. 이미 pgvector·JSONB·TEXT[]로 Postgres에 묶여 있어 여기서 DB 중립성을 지켜봐야 얻는 것이 없다.
  ↳ **별칭에 큰따옴표가 필수다.** Postgres는 따옴표 없는 식별자를 소문자로 바꿔서 `AS messageCount`는 컬럼 라벨이 `messagecount`가 되고 프로젝션 인터페이스의 `getMessageCount()`와 매칭되지 않는다.
  ↳ **검증**: 집계를 대화마다 따로 부르는 회귀를 주입하니 "[성능] N+1 없음" 테스트 하나만 실패했다. Hibernate `Statistics`로 실제 실행된 쿼리 수를 센다.

2026-08-01 | **로그 필터의 기간 파라미터에서 `IS NULL` 분기를 없애고, "제한 없음"을 넓은 경계값으로 바꿔 넘김** | 처음엔 `(:from IS NULL OR c.createdAt >= :from)`로 짰는데 Postgres가 `ERROR: could not determine data type of parameter $4`로 거절했다. **파라미터가 `IS NULL` 비교에만 쓰이면 DB가 그 자리의 타입을 추론할 근거가 없다.** 해결은 캐스팅이 아니라 조건을 없애는 것이었다 — 서비스가 null을 EPOCH/9999년으로 바꿔 넘기면 쿼리에는 항상 값이 있는 비교만 남는다. 조건이 두 개 줄어 쿼리도 단순해졌다 | HQL `cast(:from as ...)`(Hibernate 버전마다 지원 타입이 달라 불확실), 네이티브 쿼리로 `CAST(? AS timestamptz)`(엔티티 매핑과 count 쿼리를 손으로 써야 함), Specification으로 동적 조립(조건 2개에 쓰기엔 배보다 배꼽)
  ↳ **필터 조건에 `EXISTS`를 쓰고 조인하지 않은 이유**: "fallback이 하나라도 있는 세션"을 조인으로 풀면 메시지 수만큼 행이 불어나 `DISTINCT`가 필요하고, 그러면 `totalElements`가 부풀어 페이지네이션이 거짓말을 한다. `EXISTS`는 대화 한 건이 한 행으로 유지된다.

2026-08-01 | **로그 날짜 필터를 KST 기준으로 못박음** | `created_at`은 `TIMESTAMPTZ`(절대 시각)인데 사용자가 보내는 `from=2026-08-01`은 **달력 날짜**다. 둘을 잇는 데 시간대가 필요한데 서버 기본값에 맡기면 배포 환경(대개 UTC)과 사용자가 보는 날짜가 9시간 어긋나 "어제 대화가 오늘 목록에 나온다". 이 제품의 사용자는 한국 기업이라 KST로 고정한다. `to`는 **그 날짜를 포함**하도록 다음 날 00:00 미만으로 비교한다 — 그냥 `to` 00:00으로 비교하면 그날 대화가 통째로 빠지는 흔한 off-by-one이 된다 | 서버 기본 시간대 사용(환경마다 결과가 달라짐), UTC 고정(사용자가 보는 날짜와 어긋남), 프론트가 ISO 시각으로 보내기(어느 시간대인지 프론트가 정하게 되어 같은 문제가 프론트로 이동)
  ↳ TODO(W3 이후): 해외 고객이 생기면 봇별 시간대 설정이 필요하다. 지금은 필요 없다.

2026-08-01 | **`sources` JSON 파싱 실패를 예외로 만들지 않고 null로 넘김** | 이 값은 과거에 저장된 데이터라 지금 고칠 수 없다. 한 건이 깨졌다고 대화 전체 조회가 500이 되면 **사용자는 멀쩡한 나머지 메시지까지 못 본다.** 로그만 남기고 넘어간다 | 예외를 던짐(과거 로그 한 건이 화면 전체를 막음), 파싱 실패를 무시하고 빈 배열(프론트 타입이 `Source[] | null`이라 "근거 없음"과 "파싱 실패"를 구분할 여지를 남겨둠)

2026-08-01 | **대화 상세 경로를 `GET /api/bots/{botId}/logs/{conversationId}`로 신설** (PRD §10.1에 없던 경로) | 목록만으로는 "이 세션에서 무슨 대화가 오갔는지"를 볼 수 없다. 경로에 `botId`를 유지하면 소유권 확인을 봇 단위로 한 번에 끝낼 수 있고 목록→상세 화면 흐름과 주소가 일치한다 | `GET /api/conversations/{id}`(경로에 botId가 없어 소유권을 문서 삭제처럼 조인으로 거슬러 올라가야 함), 목록 응답에 메시지를 통째로 포함(목록이 무거워지고 N+1 회피의 의미가 사라짐)
  ↳ ⚠️ **봇 소유권만 확인하면 부족하다.** 대화가 <그 봇의 것인지>까지 봐야 한다. 안 그러면 내 botId에 남의 대화 id를 붙여 훔쳐볼 수 있다. 테스트로 고정했다.

<!-- 아래에 계속 추가하세요 -->
2026-08-01 | **위젯 CORS를 봇별 허용 도메인이 아니라 `*`로 열고, 판단은 애플리케이션이 하도록 되돌림** | 처음엔 봇의 `allowed_origins`를 CORS 응답에 실어 브라우저가 막게 만들었다. **동작은 했지만 잘못된 설계였다.** ① **CORS는 인가 수단이 아니다** — 브라우저에게 "이 응답을 스크립트에 넘겨도 되나"를 알려주는 정책일 뿐이라 curl·서버 호출은 신경 쓰지 않는다. 즉 CORS로 막아도 요청은 처리되고 **LLM 비용은 그대로 나간다.** ② **우리 에러 메시지를 읽을 수 없게 만든다** — CORS 필터가 먼저 끊으면 본문이 `Invalid CORS request` 평문이 되어 공통 에러 포맷이 깨지고, 위젯이 "허용 도메인에 이 주소를 추가하세요"라는 **해결 방법을 사용자에게 못 보여준다** | 봇별 동적 CorsConfigurationSource(위 두 문제 + 프리플라이트마다 DB 조회), 봇별 CORS + 우리 프론트 오리진 추가(복잡도만 늘고 ①이 그대로 남음)
  ↳ **실패해보고 배운 것**: 동적 소스를 실제로 구현해 테스트를 돌렸더니 403 응답 본문이 `Invalid CORS request`였다. 클래스 하나를 통째로 지우고 정적 규칙 두 줄로 대체했다. **CORS를 보안 경계로 착각하는 것은 흔한 실수이고, 이 프로젝트에서 직접 밟아봤다.**

2026-08-01 | **빈 `allowed_origins`는 "전부 허용"이 아니라 "전부 차단"(fail-closed)** | 봇을 만들면 목록이 비어 있다. 이걸 "아직 설정 안 했으니 열어두자"로 해석하면 **모든 신규 봇이 무방비로 태어난다.** publicKey는 고객 사이트 HTML에 그대로 노출돼 누구나 복사해 자기 사이트에 붙일 수 있고, 그러면 **LLM 비용이 봇 주인에게 청구된다.** 보안 기본값은 닫힘이어야 한다 | 빈 배열 = 전부 허용(편의는 좋지만 위 문제), 봇 생성 시 기본 도메인 자동 등록(무엇을 넣을지 알 수 없음)
  ↳ UX는 에러 메시지로 보완한다. "아직 하나도 설정 안 함"과 "설정했는데 이 주소가 없음"을 나눠 안내한다 — 관리자가 할 일이 다르다.
  ↳ **Origin 비교는 정확히 일치만.** 와일드카드·접미사 비교는 `evil-example.com`이 `example.com`으로 통과하는 고전적 실수를 부른다. 회귀 주입으로 두 규칙(fail-closed, 정확 일치)이 실제로 테스트에 걸리는 것을 확인했다.

2026-08-01 | **Origin 검증을 위젯 채팅에는 걸지 않고 설정 조회에만 검** (설계상의 구멍을 알고 남김) | 위젯 구조가 `로더 → iframe(/w/{publicKey}, Next.js)`이고 채팅은 iframe 안에서 나간다. 그러면 브라우저가 붙이는 Origin은 고객 사이트가 아니라 **우리 Next.js 도메인**이다. iframe은 cross-origin이라 부모 주소를 알 수도 없다 — **대조할 값 자체가 오지 않는다.** 반면 설정 조회는 로더가 고객 페이지에서 직접 부르므로 진짜 Origin이 온다. **설정 조회가 막히면 위젯 UI가 아예 뜨지 않으므로** 정상 브라우저 경로에서는 그 단계가 실질적 억제력이 된다. 채팅은 publicKey + rate limit으로 보호한다 | iframe이 부모 origin을 커스텀 헤더로 전달(값을 앱이 만들어 보내는 것이라 브라우저에서도 위조 가능 — 검증이 아니라 표시가 됨), 단기 토큰 발급(가장 견고하나 토큰 체계가 통째로 늘어남)
  ↳ **더 강하게 하려면 채팅도 로더가 호출**하고 iframe과는 postMessage로 주고받아야 한다(그러면 브라우저가 진짜 Origin을 붙인다). 위젯 스크립트를 고치는 아키텍처 변경이라 별도 결정으로 남긴다. **[미정]**

2026-08-01 | **요청 수 제한을 라이브러리 없이 인메모리 고정 윈도우로 직접 구현** | 필요한 규칙이 "같은 키로 1분에 N번" 하나뿐이라 Bucket4j를 들이면 버전 관리·설정 학습 비용만 붙는다. 직접 구현은 40줄이고 읽으면 바로 이해된다. **위젯은 인증 없이 열린 유일한 문이고 채팅 한 번은 외부 LLM 호출 = 실제 돈이라, 제한이 없으면 요금 폭탄이 곧 서비스 거부 공격이 된다** | Bucket4j·Resilience4j(의존성 추가), Redis(운영 대상이 하나 늘어 PRD §11.2 원칙과 충돌), 제한 없음(비용 무방비)
  ↳ **알고 택한 한계 둘.** ① 카운터가 프로세스 메모리에 있어 **인스턴스를 늘리면 각자 세므로 실질 한도가 배가 된다.** 수평 확장 시작 시점이 교체 시점이다. ② 고정 윈도우라 경계에서 2N번이 몰릴 수 있다. 목적이 "정밀한 유량 제어"가 아니라 "무한 호출 차단"이라 충분하다.
  ↳ 키는 **IP + publicKey**. IP만 쓰면 한 회사에서 여러 봇을 쓸 때 서로 한도를 잡아먹고, publicKey만 쓰면 한 명이 그 봇을 마비시킬 수 있다. ⚠️ `X-Forwarded-For`는 클라이언트가 위조할 수 있어, 신뢰하려면 프록시가 덮어쓰도록 설정돼 있어야 한다 — 배포 시 확인 필요.

2026-08-01 | **Python 호출용 `HttpClient` 를 HTTP/1.1로 고정** (`.version(HttpClient.Version.HTTP_1_1)`) | **종단 확인에서 잡은 실제 버그다.** JDK `HttpClient`의 기본값은 `HTTP_2`인데, 평문(`http://`) 상대에게는 HTTP/2를 바로 쓸 수 없어 "HTTP/1.1로 시작하되 h2c로 올려달라"는 업그레이드 요청(`Connection: Upgrade, HTTP2-Settings` / `Upgrade: h2c`)을 함께 보낸다. 그런데 Python 쪽 uvicorn(h11)은 h2c 업그레이드를 지원하지 않아 `Unsupported upgrade request`를 남기고 HTTP/1.1로 계속 처리하는데, **이때 chunked 본문이 FastAPI까지 전달되지 않는다.** 결과: multipart 본문 자체는 완벽한데 Python이 "file 필드가 없다"며 422를 냈다. 우리 상대는 내부망 uvicorn 하나뿐이고 HTTP/2로 얻을 이득(멀티플렉싱)도 없어 협상 자체를 안 하는 편이 단순하고 안전하다 | uvicorn을 h2 지원 서버로 교체(문제의 원인이 우리 클라이언트인데 상대를 바꾸는 격), Content-Length를 강제해 chunked 회피(업그레이드 요청은 그대로라 근본 원인이 남음)
  ↳ **가짜 서버로는 절대 못 잡는 버그였다.** 통합 테스트의 JDK `HttpServer` 스텁은 multipart를 파싱하지 않고 업그레이드 요청도 그냥 무시해서, "본문에 `name="file"`이 있는가"만 보고 통과시켰다. **진짜 uvicorn을 띄워보고서야 드러났다** — 종단 확인을 별도 슬라이스로 잡은 값을 여기서 회수했다.
  ↳ **회귀 테스트는 본문이 아니라 헤더를 본다.** 스텁으로 이 사고를 재현할 수 없으므로 "보내면 안 되는 헤더(`Upgrade`·`HTTP2-Settings`)가 없는가"를 검사한다. `.version(...)` 한 줄을 지우면 그 테스트만 실패하는 것을 확인했다.

2026-08-01 | **`messages.sources`는 fallback이어도 근거가 있으면 그대로 저장한다** (테스트가 현실과 어긋나 있던 것을 정정) | 종단 확인에서 **fallback인데 sources가 1건 딸려 오는 것**을 확인했다. 검색 컷오프(`max_distance=0.55`)를 통과한 청크가 있는 채로 생성 단계에서 `NO_ANSWER`가 나면 이렇게 된다 — 실제로 4개 질문 중 fallback 2개가 모두 근거 1건을 달고 왔다. 기존 테스트는 `sources: []`인 응답만 검증해 **"fallback이면 근거가 없으므로"라는 틀린 전제**를 이름에 박아두고 있었다. 근거를 저장하는 편이 옳다 — "무엇을 근거로 봤는데도 답을 못 했나"가 W3 미답변 분석의 재료다 | fallback이면 sources를 버리기(왜 못 답했는지 추적 불가)
  ↳ 테스트를 둘로 나눴다: "근거가 아예 없으면 null" + "fallback이어도 근거가 있으면 저장". **통과하던 테스트가 현실을 반영하지 못하는 경우가 있다는 사례.**

2026-08-01 | **종단 확인 실측 — `max_distance` 조정은 여전히 보류** | 실제 Gemini로 문서 1건(청크 1개)·질문 4개를 돌린 거리 실측: 근거 있는 질문 0.281·0.322 / 없는 질문 0.383·0.420. **없는 질문이 전부 1차 방어선(0.55)을 통과했고 `NO_ANSWER`가 혼자 막았다** — W1 검증과 같은 패턴이다. 경계는 0.322와 0.383 사이(≈0.35). W1에서 본 0.33과 비슷하지만 표본이 여전히 문서 1건이라 조정하지 않는다. **근거 없는 숫자를 다른 근거 없는 숫자로 바꾸는 것은 개선이 아니다** — W4에서 평가 점수로 정한다 | 지금 0.35로 조정(표본 2건으로도 과적합), 1차 방어선 제거(환각 방어가 한 겹으로 줄어듦)

2026-08-01 | **대시보드 데이터를 서버 컴포넌트가 아니라 브라우저에서 가져온다** (Next.js의 SSR 이점을 못 쓰는 상태) | **JWT를 localStorage에 보관하기 때문이다.** 서버 컴포넌트는 서버에서 실행되므로 localStorage에 접근할 방법이 없고, 따라서 "이 요청을 보낸 사람이 누구인지"를 알 수 없어 그 사람의 봇 목록을 대신 가져올 수 없다. 결국 브라우저가 토큰을 들고 직접 Spring을 호출해야 한다. **Next.js를 쓰면서 그 최대 장점을 못 쓰고 있는 셈이고, 원인은 프론트가 아니라 토큰 보관 방식이다** | httpOnly 쿠키로 전환(서버 컴포넌트가 인증 요청을 만들 수 있고 가드도 middleware로 내려감 — **더 나은 설계지만 Spring 인증 흐름까지 함께 바꿔야 해서 별도 슬라이스**), 서버에 토큰을 프록시하는 BFF 레이어(운영 대상이 하나 더 늘고 AGENTS.md가 "Next.js는 API 게이트웨이가 아니다"로 금지)
  ↳ **딸려오는 약점**: 토큰 확인이 브라우저에서 일어나 로그인하지 않은 사람에게도 화면 껍데기가 잠깐 그려진다. `checking` 상태로 내용을 안 그려 가리지만 **이건 UX 보정이지 보안 장치가 아니다** — 진짜 방어는 서버가 401을 주는 것이고 SecurityConfig가 이미 한다.
  ↳ TODO(W2 이후): httpOnly 쿠키로 옮기고 가드를 middleware로 내릴 것.

2026-08-01 | **뼈대 단계의 프론트 타입·요청 본문이 실제 서버와 어긋난 것 두 건을 정정** | 뼈대는 서버가 없을 때 PRD 표를 보고 <추정>해서 쓴 것이라 실제 구현과 어긋날 수 있다. ① `chat.feedback`이 `{ value }`를 보냈지만 Spring `FeedbackRequest`는 `{ feedback }`을 받는다 → 400 ② `bots.list()` 반환 타입이 `BotSummary[]`(문서 수·주간 대화 수·최근 평가 점수 포함)였지만 **서버는 그 집계를 안 내려준다** → 화면이 없는 필드를 있다고 믿고 `undefined`를 그린다 | 타입을 그대로 두고 화면에서 방어(없는 필드를 매번 옵셔널 처리하게 되어 계약이 거짓인 채로 굳음)
  ↳ **교훈: 타입은 "바라는 모양"이 아니라 "실제로 내려오는 모양"을 적어야 한다.** 전자로 적으면 컴파일은 통과하고 런타임에 깨진다. 집계가 붙기 전까지 `BotSummary`는 쓰지 않도록 주석으로 막아뒀다.

2026-08-02 | **채팅 화면에서 fallback 일 때는 출처 카드를 숨긴다 (저장은 그대로 한다)** | 브라우저에서 직접 보고 고쳤다. "문서에서 답을 찾지 못했어요"라고 해놓고 바로 아래에 "휴학규정.md 관련도 57%"가 붙으니 **"찾았는데 왜 답을 안 해?"로 읽힌다.** 서버가 fallback 에도 근거를 주는 건 버그가 아니다 — 검색 컷오프를 통과한 청크가 있는데 생성 단계에서 NO_ANSWER 가 난 경우다. 그 기록은 "무엇을 근거로 봤는데도 답을 못 했나"를 알려주므로 **DB 에는 남겨야 한다.** 저장의 목적과 표시의 목적이 다르므로 화면에서만 감춘다 | 서버가 fallback 이면 sources 를 비워 보내기(W3 미답변 분석의 재료가 사라짐), 그대로 보여주기(사용자가 모순으로 읽음)
  ↳ **화면을 실제로 띄워보지 않았으면 못 찾았을 문제다.** 타입도 맞고 테스트도 통과하는 상태에서, 사람이 읽었을 때만 드러나는 종류.

2026-08-02 | **fallback 답변을 에러가 아니라 중립 톤으로 표시** | fallback 은 <의도된 동작>이다 — 이 제품의 핵심 가치가 "근거가 없으면 지어내지 않는 것"이므로, 빨간 에러처럼 보이면 제품이 고장난 것처럼 읽힌다. 점선 테두리 + 회색 글씨로 두고 "이 질문은 품질 대시보드의 미답변으로 집계됩니다"를 덧붙여 **다음에 무엇이 일어나는지**까지 알려준다 | 빨간 경고로 표시(제품 가치를 스스로 부정), 일반 답변과 동일하게 표시(사용자가 답을 받았다고 오해)

2026-08-02 | **위젯 로더 스크립트를 Spring 이 정적 리소스로 서빙하도록 배선** (`processResources` 로 빌드 시 포함 + `/widget/**` permitAll) | **설정 화면을 만들고 나서야 드러났다.** 화면이 안내하는 설치 코드가 `<script src=".../widget/alldap-widget.js">` 인데 그 주소가 **401** 이었다. 원인 두 개 — ① 파일이 저장소 루트 `widget/` 에만 있어 Spring 이 서빙하지 않았고 ② `SecurityConfig` 가 `/widget/**` 를 열지 않았다. PRD §8 이 "코드 복사가 전환 포인트" 라고 한 그 코드가 붙여도 안 뜨는 상태였다 | 파일을 `api/src/main/resources/static/` 으로 복사(사본이 둘이 되어 반드시 어긋난다), Next.js `public/` 에서 서빙(로더 주소가 :3000 이 되어 "API 는 Spring" 이라는 구조 설명과 어긋남)
  ↳ **사본을 만들지 않았다.** 원본은 `widget/` 하나뿐이고 `processResources` 가 빌드 산출물에만 담는다.
  ↳ **화면을 만들어보지 않았으면 못 찾았다.** API 테스트 71건이 전부 통과하는 상태였고, 위젯 스크립트는 어느 테스트도 요청하지 않았다.

2026-08-02 | **✅ 위젯 실제 설치 검증 완료 — 프로젝트 시작부터 미검증이던 항목** | `widget/demo.html`(호스트 CSS 를 일부러 고약하게 짠 가짜 학과 홈페이지)에 실제 publicKey 로 설치해 브라우저에서 확인했다. 확인된 것: ① 스크립트 200 으로 로드 ② **Shadow DOM 격리가 실제로 동작** — 호스트의 `button { background: red !important }` 에도 위젯이 안 깨졌다 ③ 런처 클릭 → 패널 열림 ④ `GET /api/w/{publicKey}/config` **200** 이고 헤더에 봇 이름이 반영됨 = **Origin 검증 통과**(허용 도메인에 등록한 주소에서만) | —
  ↳ **아직 안 된 것**: iframe 안의 `/w/[publicKey]` 페이지가 자리표시자라 채팅 화면은 "불러오는 중" 에서 멈춘다. 위젯 채팅 페이지를 만들면 완결된다.

2026-08-02 | **effect 안에서 setState 하는 모양을 전부 걷어냄** (`react-hooks/set-state-in-effect`, eslint-plugin-react-hooks 7) | **CI 가 잡았다 — 로컬에서 `npm run lint` 를 안 돌린 게 원인이다.** `next build` 는 ESLint 를 실행하지 않아서 타입·빌드·브라우저 확인이 전부 통과해도 린트만 빨간불이었다. 규칙의 취지는 옳다: effect 에서 setState 를 하면 렌더가 한 번 더 돌고(cascading render), "바깥 세계를 읽는 일"은 상태 복사가 아니라 <구독>으로 표현해야 한다 | 규칙 끄기(취지가 옳아서 끄면 같은 실수가 계속 쌓인다), 줄마다 eslint-disable(6군데에 붙이면 규칙이 사실상 꺼진 것과 같다)
  ↳ **규칙이 무엇을 허용하는지 실험으로 알아냈다.** `await` 위치를 옮기는 것으로는 안 통했고(규칙이 함수 안까지 따라 들어간다), `.then()` 콜백과 **effect 안 async 즉시실행 + 취소 플래그**는 통과했다. 후자를 택했다 — 규칙을 피하려는 게 아니라 **화면을 떠난 뒤 응답이 와도 상태를 갱신하지 않는다**는 실제 개선이기 때문이다.
  ↳ **인증 가드는 `useSyncExternalStore` 로 바꿨다.** localStorage 는 React 바깥의 상태라 이 훅이 정확히 그 용도다. 덤으로 **다른 탭에서 로그아웃하면 이 탭도 즉시 로그인 화면으로 간다** — 예전 방식(effect 에서 한 번만 읽기)은 그걸 못 잡았다. 같은 탭 변경은 `storage` 이벤트가 안 오므로 `setAccessToken`/`clearAccessToken` 이 직접 알린다.
  ↳ **검증 절차에 `npm run lint` 를 추가한다.** 빌드가 통과해도 린트는 따로 봐야 한다.


2026-08-02 | **인증 가드의 서버 스냅샷을 `null` → `undefined` 로 바꿈** (`getAccessTokenServerSnapshot`) | **로그인한 채 새로고침하면 로그아웃되는 버그를 브라우저에서 잡았다.** `useSyncExternalStore` 의 서버 스냅샷은 SSR 뿐 아니라 **하이드레이션 첫 렌더에도** 쓰인다. 거기서 `null` 을 주면 "토큰 없음" 과 "아직 못 읽음" 이 같은 값이 되어 가드가 즉시 `/auth` 로 튕긴다. `undefined` 로 갈라 "모름" 을 표현했다 | 가드를 effect 에서 `setTimeout` 으로 미루기(깜빡임이 그대로 남고 경합이 생긴다), 토큰을 쿠키로 옮기기(옳은 방향이지만 이 버그보다 훨씬 큰 작업 — TODO 로 남김)
2026-08-02 | **위젯 봇 설정을 로더가 받아 `host-info` 로 iframe 에 내려주도록 변경** (채팅 페이지의 자체 `config` 호출 삭제) | **실제 설치에서 설정 조회가 항상 403 이었다.** iframe 에서 나가는 요청의 Origin 은 고객 사이트가 아니라 우리 앱(:3000)이라, 봇의 허용 도메인 목록과 영원히 안 맞는다. **허용 도메인 검증이 의미를 갖는 요청은 고객 페이지에서 직접 도는 로더의 것뿐이다** — 그래서 검증되는 요청을 한 번만 보내고 결과를 넘긴다 | 허용 목록에 우리 앱 주소를 자동 추가(검증이 무의미해진다), 채팅 페이지용으로 검증 없는 config 엔드포인트를 따로 열기(publicKey 만 알면 누구나 봇 설정을 읽는다)
  ↳ 대가: 로더 없이 `/w/[publicKey]` 를 직접 열면 인사말이 안 나온다(개발 중에만 해당). 채팅은 정상.
  ↳ 함께 고침: 설정 조회에 실패하면 로더가 **오버레이에 이유를 띄운다**. 조용히 기본값으로 넘어가면 설정 화면의 "허용 도메인이 없어 위젯이 뜨지 않습니다" 안내가 거짓말이 된다.

2026-08-02 | **테스트 질문 자동 생성을 동기(200)로 구현** (`POST /internal/bots/{bot_id}/eval/questions/generate`) | 업로드가 202+폴링인 이유는 `documents.status` 라는 **폴링할 행이 있기 때문**인데, `eval_questions` 에는 상태 컬럼이 없다. 202 를 줘도 프론트가 무엇을 폴링할지가 없고, 만들려면 `V2` 마이그레이션이 딸려온다. 게다가 `BackgroundTasks` 는 프로세스가 죽으면 유실되는데 **유실됐다고 적어둘 자리조차 없다.** 대신 `count` 상한(20)으로 응답 시간을 통제한다 — Spring 읽기 타임아웃이 120초, 실측 호출 1건이 약 1.2초(사고 끔 기준) | 202+폴링(상태 컬럼 신설이 선행돼야 함), 질문 개수를 폴링하는 우회(폴링 대상이 "몇 개 늘었나"라 완료 시점을 모른다)
  ↳ **재호출은 누적**이다. 표본 SQL 에 "이미 질문이 있는 청크는 제외"를 넣어 누적과 중복 방지를 한 줄로 해결했다. 전량 교체는 **관리자가 고쳐둔 질문을 날린다**(PRD F-05 는 수정·비활성을 요구하는데, `updated_at` 도 수정 플래그도 없어 무엇을 보존할지 구분조차 못 한다).
  ↳ `count` 상한 20 의 근거: 청크 1개당 LLM 1회이므로 이 값이 곧 비용·시간 상한이다. 20 × 1.2초 ≈ 24초로 120초 안에 여유가 있다.

2026-08-02 | **질문 생성 호출만 `thinking_budget=0` 으로 사고를 끔** | **성능 튜닝이 아니라 버그 수정이다.** `gemini-3.5-flash` 는 사고 토큰이 `max_output_tokens` 를 **함께** 소모해서, JSON 이 50토큰도 안 되는데 사고에 1024를 다 쓰고 본문이 중간에 잘렸다(`finish_reason=MAX_TOKENS`). 잘린 JSON 은 파싱이 안 돼 그 청크가 조용히 버려졌다 — 실제로 4개 요청에 3개만 나왔고, 버려진 청크에는 답할 내용이 멀쩡히 있었다. **같은 청크 5회씩 실측: 사고 켬 → 성공 2/5·잘림 3회·평균 3768ms / 사고 끔 → 성공 2/5·잘림 0회·평균 1164ms**(나머지는 429 쿼터) | `max_output_tokens` 만 올리기(사고 길이는 상한이 없어 근본 해결이 아니고 비용만 는다), 텍스트 파싱으로 되돌리기(잘린 JSON 은 어차피 못 살린다)
  ↳ **채팅 답변 생성(`generator.generate`)에는 같은 수정을 하지 않았다.** 거기선 사고가 실제로 필요하다. 다만 **같은 원인으로 "잘린 답변이 fallback 으로 둔갑"할 수 있다** — 근거를 찾고도 "모른다"고 답하는 것이다. 지금 고치면 W1 완료 조건으로 실측해둔 fallback 기준선(10/10)이 무효가 되므로, 재측정과 함께 별도 슬라이스에서 다룬다. `generator.py` 에 TODO 로 근거와 방향을 남겼다.
  ↳ 이 프로젝트 최초의 **구조화 출력**(`response_mime_type` + `response_schema`) 사용이다. `ground_truth` 가 NOT NULL 이라 파싱 실패가 곧 저장 실패인데, 스키마를 주면 코드펜스 벗기기·예외 처리가 통째로 사라져 텍스트 파싱보다 코드가 짧다. `gemini-3.5-flash` 에서 동작함을 실측 확인했다.

2026-08-02 | **생성 모델을 `gemini-3.5-flash` → `gemini-3.5-flash-lite` 로 교체** | ① **사고 토큰 문제가 구조적으로 사라진다** — flash 는 사고 토큰이 `max_output_tokens` 를 함께 먹어 JSON 이 잘렸는데, flash-lite 는 같은 요청에 `thoughtsTokenCount=0` 으로 답한다(실측). ② 비용 1/4 (입력 $1.50→$0.30, 출력 $9.00→$2.50 / 1M). **쓰던 3.5-flash 가 Flash 라인에서 가장 비싼 축이었다.** ③ 무료 한도가 모델별로 따로 잡혀(`GenerateRequestsPerDayPerProjectPerModel-FreeTier`) 용도별로 모델을 나누면 각각 별도 한도를 받는다 | `gemini-2.5-flash-lite`(조사 1순위였으나 **실측 404 — "no longer available to new users"**), `gemini-3.6-flash`(동작하지만 사고 242토큰 + 더 비쌈), OpenAI `gpt-5.x`(SDK 추가 필요, 단가 교차검증 실패)
  ↳ **`thinking_budget=0` 을 삭제했다.** flash-lite 는 그 인자를 **HTTP 400 으로 거부**한다 — 남겨뒀으면 모델을 바꾸는 순간 질문 생성이 통째로 죽었다. 그리고 사고를 안 하는 모델이라 애초에 필요가 없다. 되살릴 일이 생기면 구버전 이름인 `thinking_budget` 말고 현행 `thinking_level` 을 쓸 것.
  ↳ **W1 완료 조건 3번 재측정: fallback 10/10 유지** (기준 8). 지연은 평균 3.3초 → **1.2초**. 근거 있는 질문 2건도 정상 답변 확인.
  ↳ **1차 방어선(`max_distance=0.55`)은 여전히 0/10 작동.** 근거 없는 질문 10개가 전부 근거 5건을 달고 통과했다. 모델을 바꿔도 이 상태는 그대로다 — W4 에서 조정한다.
  ↳ **"목록에 있다 ≠ 부를 수 있다".** `models.list()` 에 `gemini-2.5-flash-lite` 가 나오는데 호출하면 404 다. 모델 선택은 반드시 실제 호출로 검증할 것.

2026-08-02 | **임베딩 제공자를 Google Gemini(`gemini-embedding-001`, 1536) → Cloudflare Workers AI(`@cf/baai/bge-m3`, 1024) 로 교체** | **성능이 아니라 약관과 한도 때문이다.** ① Gemini 무료 등급은 공식 가격표에 `Used to improve our products: Yes` — 입력을 학습에 쓴다고 명시돼 있고, 이 제품은 고객 사내 문서를 받는 것이 목적이다. Cloudflare 는 "고객 콘텐츠를 모델 학습에 쓰지 않는다"를 문서에 명시한다. ② 한도가 Gemini 는 비공개인데 Cloudflare 는 **10,000 뉴런/일(bge-m3 기준 약 930만 토큰/일)** 이 공개돼 있고 매일 리셋된다. ③ bge-m3 는 한국어 검색 벤치마크에 **숫자가 있다**(KURE 8종 평균 0.68723) — 반면 `gemini-embedding-001` 은 조사한 한국어 리더보드 3곳 어디에도 없어 좋은지 나쁜지 말할 근거 자체가 없었다 | 셀프호스팅(KURE-v1 0.69473 / arctic-embed-ko 82.14 로 점수는 더 높지만 **인코딩 중 peak RSS 2.4GB** — 무료 배포 티어 512MB 를 넘겨 "무료 모델 쓰려고 유료 서버 사는" 구조가 된다), Jina v3(**CC-BY-NC** — 공개 배포와 충돌, 게다가 공식 페이지가 deprecated), Cohere trial(월 1,000콜), Qwen3-Embedding-0.6B(**한국어 리더보드 최하위 75.88**), 현행 유지(약관 문제가 그대로 남는다)
  ↳ ⚠️ **"한국어 성능이 더 좋아서 바꿨다"고 말하면 안 된다.** 두 모델을 같은 한국어 벤치마크에서 나란히 잰 측정치가 존재하지 않는다(ko-leaderboard 는 오픈소스만 넣고, KURE 리더보드는 2024-12 로 gemini-embedding-001 출시보다 앞선다). 방어 가능한 답은 **"비용과 데이터 정책 때문에 바꿨고, 성능 우위는 공개 비교가 없어서 W4 에서 직접 쟀습니다"** 다.
  ↳ **마이그레이션 전에 실측으로 검증했다** — 차원 1024 / 입력 2개→벡터 2개(합치지 않음) / 유사문장 0.7369 vs 무관문장 0.5171 / 배치 100개 705ms / 응답 경로 `result.data`. 차원이 문서에 없어서 확인 없이 `ALTER` 했다면 되돌려야 했다.
  ↳ **`V2__embedding_1024.sql` 순서가 중요하다**: 인덱스 드롭 → 값 NULL → `ALTER` → 인덱스 재생성. 데이터가 있는 채로 `ALTER` 하면 `expected 1024 dimensions, not 1536` 로 실패한다(실측).
  ↳ **재임베딩 도구를 만들었다**(`app/reembed.py`). 청크 본문은 그대로 두고 벡터만 갈아끼운다. W4 에서 임베딩 A/B 를 돌릴 때 반복해서 쓴다.
  ↳ 🔴 **부수 효과 — 1차 방어선이 처음으로 작동했다.** W1 이후 계속 `max_distance=0.55` 컷오프가 한 번도 안 걸렸는데(0/10), 교체 후 **4/10** 이 근거 0건으로 걸러졌다. 그 4건은 LLM 호출 없이 119~170ms 에 fallback 됐다(나머지는 800~1100ms). **비용 절감 경로가 열렸다.** ⚠️ 다만 이건 "더 좋다"가 아니라 "거리 분포가 달라졌다"이다. 정상 질문을 잘못 자르지 않는지는 W3 평가로 확인해야 한다.
  ↳ **W1 완료 조건 3번 재측정: fallback 10/10 유지**(기준 8). 근거 있는 질문 2건 정상 답변.
  ↳ `httpx` 를 `requirements.txt` 에 **명시**했다. `google-genai` 가 이미 끌고 오지만 우리가 직접 import 하므로, 남의 패키지에 딸려온 것에 기대면 그 패키지를 빼는 순간 조용히 깨진다.

2026-08-02 | **평가 실행을 비동기(202+폴링)로 구현** (`POST /internal/bots/{bot_id}/eval/runs`) | **질문 생성은 동기로 만들었는데 여기는 비동기다. 차이는 딱 하나 — 폴링할 대상이 있는가.** `eval_questions` 에는 상태 컬럼이 없어 202 를 줘도 화면이 볼 게 없었지만, `eval_runs` 에는 `status('running'/'completed'/'failed')` 가 있다. 게다가 여기는 질문 수만큼 (검색+생성+채점)이 돌아 반드시 수십 초를 넘긴다 — 질문 20개면 호출 40번이라 동기로는 Spring 읽기 타임아웃(120초)에 걸린다 | 동기(질문 수를 제한하면 되지만 W4 에서 테스트셋을 키울 수 없게 된다), 별도 워커 프로세스(운영 대상이 늘어난다 — PRD §11.2 위반)
  ↳ **`execute()` 는 예외를 밖으로 내보내지 않는다.** BackgroundTasks 에서 예외가 나면 아무도 못 보고 `eval_runs` 가 영원히 `running` 으로 남는다. 화면은 끝나지 않는 로딩을 보여준다. 통째로 try 로 감싸 실패도 `status='failed'` 로 남긴다.
  ↳ **`config` 에 실행 시점 설정을 박제한다**(top_k · max_distance · chat_model · embedding_model · judge_model · hybrid · reranker). W4 의 before/after 비교는 이게 있어야 성립한다. `hybrid`·`reranker` 는 아직 구현 전이라 항상 false 지만 **지금부터 남겨야 나중 실행과 같은 모양으로 비교된다.**

2026-08-02 | **채점(LLM-as-Judge) 모델을 답변 생성 모델과 <다른 제공자>로 분리** — 생성 Google Gemini(`gemini-3.5-flash-lite`) / 채점 Cloudflare(`@cf/mistralai/mistral-small-3.1-24b-instruct`) | **자기 편향 때문만이 아니다. 더 큰 문제는 <같은 실수의 반복>이다** — 생성기가 환각을 낸 이유는 "그 문맥에서 그 문장이 근거에 맞다고 판단했기 때문"이고, 같은 모델이 채점하면 같은 판단을 그대로 반복한다. **반드시 잡아야 할 케이스에서 판정기가 가장 눈이 먼다.** 이 프로젝트의 최종 산출물이 정량 비교표라 채점이 틀리면 결론이 통째로 틀린다 | 같은 모델로 채점(비용은 싸지만 위 이유로 기각), Gemini 의 다른 모델(제공자가 같아 분리가 약하고, 무료 한도가 모델당 하루 20회라 실행 몇 번에 소진된다)
  ↳ **모델은 정답이 정해진 한국어 3케이스로 골랐다** (충실한 답 / 지어낸 답 / 절반만 맞는 답). `mistral-small-3.1-24b` 1·0·0.5 전부 정확 + 약 1000ms → **채택**. `qwen3-30b` 정확하지만 2.9~5.3초. `llama-3.3-70b` 는 "절반만 맞는 답"을 0 으로 깎았다. `gpt-oss-120b` JSON 파싱 실패. `glm-5.2` 무료 플랜에서 403.
  ↳ ⚠️ **아직 사람 라벨과 대조하지 않았다.** "채점을 어떻게 믿느냐"에 답하려면 사람이 매긴 30~50건과의 일치율(카파)이 필요하다. 지금 근거는 위 3케이스뿐이고 그건 "쓸 수 있다"까지만 말해준다. TODO(W4).
  ↳ **fallback 한 답변은 채점하지 않고 응답률로만 센다.** "모르겠다"를 충실성 1.0 으로 세면 평균이 거짓말이 되고, 0 으로 세면 정직한 거절이 벌점이 된다. **실측 확인**: 미답변 질문 1건을 넣자 응답률 1.0 → 0.75, 충실성은 1.0 유지.
  ↳ **채점 실패(None)는 0점이 아니라 평균에서 제외한다.** 0 으로 처리하면 채점 실패가 품질 저하로 둔갑한다.
  ↳ 🐛 **Cloudflare 가 같은 모델·같은 엔드포인트에서 응답 모양을 두 가지로 준다** — `result.response` 와 `result.choices[0].message.content`. 보통 둘 다 있지만 **한쪽이 빈 응답이 실제로 왔고, 한쪽만 보던 코드가 3건 중 1건을 통째로 날렸다.** 둘 다 훑어 내용이 있는 쪽을 쓰도록 고쳤다. 로그도 응답 dict 전체 대신 <모델이 뱉은 텍스트>를 남기게 바꿨다 — 예전 로그는 앞부분이 메타데이터라 정작 원인이 안 보였다.

2026-08-02 | **Spring 평가 API 구현** (`/api/bots/{botId}/eval/*` — 질문 목록·생성, 실행 시작·이력, 질문별 결과) | Python 컨트랙트가 실측으로 확정된 뒤에야 붙였다. `EvalController` 주석이 "무엇을 호출할지 정해지지 않은 상태에서 서비스를 만들면 존재하지 않는 컨트랙트를 코드로 굳힌다"고 미뤄뒀던 것을 이제 해소한다 | 조회까지 Python 경유(폴링 대상이라 호출이 잦은데 Python 이 죽으면 목록까지 503 이 된다 — 문서 목록과 같은 판단으로 DB 직접 조회를 택함)
  ↳ **이전 슬라이스에서 "다음에 필요하다"고 남긴 eval 전용 4xx 매퍼를 만들었다.** 업로드 매퍼를 재사용하면 400 이 `UNSUPPORTED_FILE_TYPE` 이 되어 **"질문을 만들 문서가 없습니다" 상황에 "지원하지 않는 파일 형식입니다"가 나간다.** 평가의 400 은 전부 사용자가 고칠 수 있는 상태이고 Python 이 이미 "무엇을 어떻게" 까지 담은 한국어를 주므로, 문구를 뭉개지 않고 그대로 내려보낸다. 새 코드 `EVAL_NOT_READY`. **실측 확인**: `count=999` → `400 EVAL_NOT_READY` + "한 번에 만들 수 있는 질문은 최대 20개입니다…"
  ↳ **`EvalRunResponse.config` 를 문자열 → `EvalConfigResponse` 객체로 바꿨다**(기존 TODO 해소). Python 은 snake_case, 프론트는 camelCase 라 변환은 Spring 책임이다. **모든 필드가 래퍼 타입에 null 허용** — 옛 실행에는 없던 키가 있을 수 있는데(`embedding_model` 은 W3 중반에 추가됐다) `int`/`boolean` 으로 받으면 없는 키가 0/false 로 채워져 **"기록되지 않음"과 "0이었음"이 구분되지 않는다.** 비교표에서 그건 거짓말이 된다.
  ↳ **점수 세 개도 전부 null 허용.** running 이면 아직 없고, 답변이 전부 fallback 이면 평균 낼 대상이 없다. 0 으로 채우면 "아직 없음"과 "0점"이 뒤섞인다.
  ↳ **상한(20)을 Spring 에 복제하지 않았다.** 두 곳에 두면 반드시 어긋나고 그때 "Spring 은 통과, Python 은 거절"이 된다. 하한(1)만 `@Valid` 로 막는다 — 0·음수는 어떤 설정에서도 무의미하고 왕복 한 번을 아낀다.
  ↳ **결과 정렬을 서버에 못박지 않았다.** 저장 순서로 주고 화면이 "점수 낮은 순/미채점 먼저"로 정렬한다. 정렬 기준이 화면마다 다를 수 있다.
  ↳ **격리 실측**: 남의 봇으로 5개 엔드포인트 전부 404(403 아님), 토큰 없으면 401, 그리고 **요청이 Python 로그에 0건** — "404 가 났다"가 아니라 "요청이 Python 까지 가지 않았다"를 확인했다.

2026-08-02 | **품질 대시보드 화면 구현** (`web/app/(dashboard)/bot/[botId]/quality/page.tsx`) | 자리표시자를 실제 화면으로 바꿨다. 점수 카드 · 테스트셋 관리 · 실행 이력(펼치면 질문별 상세) · 미답변(미구현 표시). 클라이언트 컴포넌트인 이유 셋: ① JWT 가 localStorage 라 서버가 대신 못 가져온다 ② 실행 상태 폴링(setInterval)이 필요하다 ③ 버튼 onClick 이 있다 | 서버 컴포넌트(위 ①때문에 불가), 실행 결과를 미리 전부 로드(안 볼 데이터까지 받는다 — 펼칠 때만 부르도록 함)
  ↳ **`fmt()` 한 곳에서 null 을 "—" 로 그린다.** 점수 null 은 <0점이 아니다> — running 중이거나, 답변이 전부 fallback 이라 채점 대상이 없었거나, 채점 호출이 실패한 경우다. 0 으로 그리면 "품질이 0점"으로 읽힌다. 브라우저에서 미채점 행을 넣어 **"—" 로 뜨고 맨 위에 정렬되며 '0점이라는 뜻이 아닙니다' 안내가 붙는 것**을 실측 확인했다.
  ↳ **질문별 상세 정렬을 서버가 아니라 화면에서 한다.** 정렬 기준이 화면마다 다를 수 있어 API 를 늘리지 않는다. `toSorted` 를 쓴다 — `sort` 는 props 배열을 그 자리에서 뒤집어 React 가 예상 못 한 변경이 된다.
  ↳ **`EvalResultResponse.retrievedChunks` 를 JSON 문자열 → 파싱된 목록으로 바꿨다.** 프론트가 문자열을 다시 파싱하게 두면 파싱 규칙이 서버·클라이언트 두 벌이 되어 언젠가 어긋난다. `MessageResponse.sources` 와 같은 방식이고, 실패해도 예외 대신 빈 목록을 준다(결과 한 건의 JSON 이 깨졌다고 리포트 전체가 500 이 되면 안 된다).
  ↳ **`EvalRetrievedChunk` 를 `Source` 와 <다른 타입>으로 뒀다.** Python 이 평가 결과에 남기는 건 chunkId·filename·score 셋뿐이라 documentId·preview 가 없다. 같은 타입인 척하면 화면이 "왜 여기만 preview 가 비지?" 를 계속 묻게 된다.
  ↳ **미구현 API 를 화면에서 부르지 않는다.** `updateQuestion`(PATCH)·`listUnanswered` 는 아직 서버에 없어 `lib/api.ts` 주석에 "화면에서 부르지 말 것"을 박고, 화면에는 TODO 문구만 남겼다. 없는 걸 있는 것처럼 그리지 않는다.
  ↳ 🐛 **알게 된 한계 — 질문을 지우면 과거 실행의 결과 행도 사라진다.** `eval_results.question_id` 가 `ON DELETE CASCADE`(V1)인데 `eval_runs` 의 집계는 남는다. 그래서 **저장된 응답률(0.750)과 보이는 상세 건수(3)가 어긋난다.** W4 비교표에서 과거 실행의 설명력을 잃는 문제다. 제대로 고치려면 질문 원문을 `eval_results` 에 스냅샷해야 한다(V3 + Python 수정). 지금은 알고 남긴다.
