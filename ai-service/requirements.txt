fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
# ⚠️ 2.10.4 로 적혀 있었는데 <실제로 설치되는 건 2.13.4> 였다.
#    google-genai 2.16.0 이 더 높은 pydantic 을 요구해 pip 이 올려버렸고,
#    이 파일만 옛 값으로 남아 있었다. 로컬 venv 는 여러 번에 걸쳐 설치돼서
#    돌아갔지만, <깨끗한 환경에서는 resolve 자체가 안 됐다>(2026-08-05 이미지 빌드에서 발견).
#    지금 값은 실측으로 돌아가는 버전이다.
pydantic==2.13.4
pydantic-settings==2.7.0

psycopg[binary,pool]==3.2.3
pgvector==0.3.6

# 생성(답변·질문). 임베딩은 Cloudflare 로 옮겼다.
google-genai==2.16.0

# 임베딩(Cloudflare Workers AI) 호출용.
# google-genai 가 이미 끌고 오지만, 우리가 직접 import 하므로 명시한다 —
# 남의 패키지에 딸려온 것에 기대면 그 패키지를 빼는 순간 조용히 깨진다.
httpx==0.28.1

pymupdf==1.25.1
python-docx==1.1.2
